# Shaping-leak fix loop journal

Cross-context memory for the loop in `doc/shaping-leak-loop/runbook.md`. Append one entry per fix and per skip. A fresh window reads this to know what's done, what's intractable, and which working-tree edits are its own. This file is tracked (it is the audit trail of an autonomous agent editing the font) and is committed as part of the loop's final batch.

Starting state (bless of commit d7f3269): 195 bad in `test/bad-leak-backlog.txt`, 242 benign in the census, gates green.

## Hard track (defer — needs second-order revert machinery, not a one-line lever)

The 17 signatures in `test/leak-force-bad.yaml` are cross-lookup-compose leaks (changed side strips to bare while an unchanged ligature neighbor keeps reaching). Do not attempt these with `not_before`/contract levers; leave in the backlog and surface at handoff. See the runbook's "hard track" section.

## General lessons (read before picking a target)

- **A family-level `not_before: {family: qsY}` on a left glyph's default baseline-exit form breaks the real qsX→qsY baseline join.** Many backlog "single exit edge before bare follower" entries (e.g. `She No Gay Out`, the `qsGay.ex-y0`, `qsMay.ex-y0`, `qsJai.ex-y0` lines) are _default-exit dangles_: the left glyph carries its normal baseline exit because something follows in the run, but across the break the follower is shaped bare (entry y5) so the exit dangles. The catch — the same family also has a real baseline-entry variant (`qsNo.alt`, `qsZoo.half`) that the left glyph genuinely joins to in connected text, and a blanket `not_before: {family}` suppresses the exit for _both_ the break and the real join, so `make test` breaks `qsX→qsY.<baseline-entry>` joins. The leak gate (`make test-leaks`) will still pass — it doesn't see real joins — so **you must run `make test` for any exit-suppression lever before trusting it.** A correct fix needs to scope the suppression to the break/non-join context only (the bare follower form), not the whole family; a coarse family `not_before` is the wrong lever for these.

- **Productive pattern (criterion A — non-joining neighbor):** the clean wins are exit/entry dangles toward a neighbor the changed glyph _never_ joins in any context. Prove it from the design itself: check the neighbor's entry/exit form `not_after`/`not_before` already excludes the changed family, and/or grep the generated FEA (`test/AbbotsMortonSpaceportSansSenior-Regular.fea`) to confirm no real-join rule produces the receiving form after that predecessor. Often a _sibling_ form of the changed glyph already carries the exclusion (e.g. May's `entry_xheight_exit_baseline.not_before` listed qsHe but the bare `exit_baseline` didn't) — adding the missing family to the existing `not_before`/`not_after` just closes an inconsistency and is low-risk. This is the opposite of the lesson-1 trap (where the neighbor _does_ have a matching-Y join variant). Still run `make test` to be sure.

- **Refinement to criterion A (entry must not be load-bearing):** before adding a plain `not_before: {family: qsNeighbor}` to suppress an exit dangle, confirm the form you are suppressing does NOT also carry an ENTRY that the changed glyph's own _predecessors_ join into. The May→He win worked because `qsMay.exit_baseline`'s entry side wasn't load-bearing in that context. The No→He attempt FAILED (12 new bad leaks) because the suppressed form `qsNo.alt` has a baseline `entry: [0,0]` that ·No's predecessors (·It/·Gay/·Roe/·They/·Vie/·Ye/·Utter/·Jai/·Excite) genuinely join into — suppressing `alt` before ·He removed that entry in the "…No He" context, collapsing every predecessor's real join into a fresh dangle. So the test is: does the promoted form gain BOTH an entry and an exit (e.g. `qsNo.alt.en-y0.ex-y0`, `qsDay_qsUtter.half.en-y0.ex-y5`)? If yes, a plain `not_before`/`not_after` is the wrong lever — it reroutes the backward-upgrade chain (lesson 2). Such a target needs an entry-PRESERVING `ex-noentry` form (`inherits` the promoted form, `anchors.exit: null`, `before:`/context-scoped to the non-joining follower) so predecessors keep their entry while the dangling exit is dropped — that is second-order authoring, not a one-liner. Prefer targets where ONLY the break-facing edge changed (`qsMay.noentry->qsMay.ex-y0` gained only an exit; that was clean).

- **Inflection point (one-liner seam ~exhausted after fix [1]):** an exhaustive criterion-A sweep (iteration 4) found NO remaining clean one-line `not_before`/`not_after` fix of the May→He shape. ·He was structurally unique — the only universal non-receiver (no x-height entry; `qsHe.entry_baseline.not_after` excludes ~everyone). Of the remaining ~194: ~30 changed forms gain BOTH an entry and an exit (`qsGay.en-y0.ex-y5` ×12, `qsUtter.alt.en-y5.ex-y0.reaches-way-back` ×6, `qsNo.alt.en-y0.ex-y0`, `qsRoe.en-y0.ex-y5`, `qsOut.en-y0.ex-y5` …) → load-bearing-entry traps (lesson 3); the exit-only/entry-only promotions (`qsMay.ex-y0`→Gay, `qsJai.ex-y0`→It, `qsGay.ex-y0`, `qsShe.ex-y0`→No, `qsHe.half.ex-y5`→It/Jai, `qsWay/qsWhy.half.ex-y0`→Gay, `qsOwe.ex-y5`, `qsTea.half.ex-y5`) all dangle toward a neighbor that DOES receive the reach in some real-join context → lesson-1 traps needing break-context-scoped suppression, not a family selector. Net: the remaining backlog needs SECOND-ORDER machinery (entry-preserving `*.ex-noentry` forms, break-scoped `before:`/`after:`, demote-overrides) — the same class the runbook defers to the hard track. Proof basis: see iteration-4 sub-agent file:line citations (May→Gay FEA :3332; Roe→No explicit `before:{family:qsNo,entry_y:0}`; It/Jai x-height entries :2584/:1411). **Confirming deep sweep (also iteration 4):** the last uncleared exit-only families — qsOwe.ex-y5, qsTea.half.ex-y5, qsPea.half.ex-y5.ex-dips, qsWhy.ex-y5, all reaching x-height toward qsIt/qsJai/qsYe (backlog L115-119, L132-133, L192-193) — are ALSO real x-height joins (rendered with `tools/inspect_join.py`: every pair completes at y=5; qsIt `entry_xheight.not_after` accepts them, qsPea even courts qsIt/qsJai via `before:` at :334-335, qsJai `contract_entry_after` names qsTea/qsHe/qsPea at :1415). Not dangles → lesson-1 traps. **The one-line seam is exhausted; fix [1] was the only clean one-liner. All remaining backlog needs second-order machinery.**

## Log

<!-- One entry per fix/skip. Format:
- [N] SIG `il->lc | ir->rc` — FIXED: lever (qsX.form field) | make test-leaks: targeted gone, 0 new | make test: <green @ batch K | not-run-this-batch>
- [N] SIG `...` — SKIPPED: reason
-->

- [skip] Ah ZWNJ No He `qsNo.noentry->qsNo.alt.en-y0.ex-y0.noentry | qsHe->qsHe` (backlog L110) — SKIPPED: looked like a criterion-A twin of the May→He fix (·He never receives ·No; `qsHe.entry_baseline.not_after` lists qsNo at :2155), but it's second-order. Tried `not_before: [{family: qsHe}]` on `qsNo.forms.alt.select` (quikscript.yaml ~2159). `make all` clean but `make test-leaks` reported **12 NEW bad leaks** — all "…No He" predecessors (Awe It No He, Gay Gay No He, He Vie No He, Awe Ye No He, …). Cause: `qsNo.alt` gains a load-bearing baseline `entry: [0,0]` that ·No's predecessors join into; suppressing `alt` before ·He stripped that entry and cascaded. Reverted. See the "entry must not be load-bearing" refinement above; needs an entry-preserving `qsNo.alt.en-y0.ex-noentry` form, not a plain `not_before`. Defer.
- [1] Ah ZWNJ May He `qsMay.noentry->qsMay.ex-y0 | qsHe->qsHe` (was backlog L100) — FIXED: added `- family: qsHe` to `qsMay.forms.exit_baseline.select.not_before` (quikscript.yaml ~2114, between qsZoo and qsNo in code-point order). Provably safe (criterion A): qsMay→qsHe is never a real join — `qsHe.entry_baseline.not_after` already excludes qsMay (quikscript.yaml:1804) and the FEA backward rule for `qsHe.en-y0` omits qsMay as a predecessor; the fix just closes an inconsistency since May's sibling form `entry_xheight_exit_baseline.not_before` already lists qsHe (:2076). | make test-leaks: targeted gone, 0 new bad | make test: green (6653 passed) | backlog 195→194, census stable at 242.
- [skip] She No Gay Out / She Zoo Tea Oy `qsShe->qsShe.ex-y0 | qsNo->qsNo` (backlog L131-132) — SKIPPED: default-exit dangle, not a clean one-liner. Tried adding `{family: qsZoo}, {family: qsNo}` to `qsShe.forms.exit_baseline.select.not_before` (quikscript.yaml L1364). `make all` + `make test-leaks` both passed (targeted gone, 0 new bad), but `make test` failed with 8 broken joins — real `She→No.alt` / `She→Zoo.half` baseline joins (`Eight_She_b_No_alt`, `Owe_She_b_No_alt_b_Zoo_half`, etc.). Reverted. See the general lesson above; needs break-context-scoped suppression, not a family `not_before`.
- [probe/skip, 2026-06-04] `qsGay.en-y0.ex-y5` cluster ×12 (backlog L38-49, "They May Gay …" / "Day Eat Gay Owe") — SKIPPED after an empirical probe (user asked to gauge batch viability on one cluster). Diagnosis (sub-agent): the in-context form `qsGay.entry_baseline` (quikscript.yaml:789-797) carries BOTH a load-bearing baseline `entry: [0,0]` (real ·May→·Gay back-join) AND an x-height `exit: [4,5]` that is ALSO load-bearing (real ·Gay→·Ye/·Oy/·Jay/·Et/·Awe/·Ox/·Llan/·Eight x-height joins, all gap=0; FEA :3959/:4201 inside `calt_cycle`). Correct lever shape = entry-preserving `entry_baseline_exit_noentry` (inherits `entry_baseline`, `exit: null`, `before:` the 12 bare followers, modifiers `[en-y0, ex-noentry]`). Applied it (minimal `before:` scoped to exactly the 12 backlog followers) → `make all` clean; the 12 targeted bad leaks all RESOLVED (became benign `qsGay.en-y0.ex-noentry` census entries, +11 benign). BUT `make test-leaks` reported **4 NEW bad leaks** — the `before:` gate rerouted the generated backward-upgrade (calt_cycle) exactly as the `Et Tea Day Utter` skip did: `Day Eat Gay Gay`/`Day Eat Gay May` push the dangle onto the _follower_ (R grows `en-y0.ex-y5`), and `Utter May Gay Ah`/`They Zoo Gay Ah` give ·Gay-as-follower the new `en-y0.ex-noentry` whose `en-y0` now dangles backward. Reverted. **Viability verdict for the user's probe:** the biggest second-order cluster is NOT a single-form fix; it needs predecessor-scoped `not_after`+`except` plus `trailing_demote_overrides` (the `qsIt.en-y0.ex-noentry.before-day-exam` machinery, quikscript.yaml:2639-2663 / overrides :182-183) and a build-and-bisect batch pass — confirming the runbook's "deliberate batch pass, not the per-leak loop" guidance.
- [skip] Et Tea Day Utter `qsDay->qsDay_qsUtter.half.en-y0.ex-y5 | qsUtter->qsDay_qsUtter.half.en-y0.ex-y5` — SKIPPED: second-order. The whole `qsDay_qsUtter.half` block (11 backlog entries: Et/Awe/Tea Day Utter ± May/Jai/Bay) shares one unconditional `ex-y5` exit on `qsDay_qsUtter.forms.half` (quikscript.yaml ~645-656). Tried the entry-preserving `ex-noentry` two-form split (gate `half`'s exit behind `before: [Pea/Gay/Ye/May/It @ entry_y 5]` + add `half_exit_noentry` sibling with `exit: null`). `make all` clean but `make test-leaks` reported 3 NEW bad leaks — the `before:` gate rerouted the generated backward upgrade rule, so e.g. `They Zoo Day Utter` now upgrades the bare ligature to `.half.en-y0.ex-y5`. Reverted. Needs the second-order downstream-revalidation machinery, not a one-line lever — treat as hard-track-adjacent. Defer.

## Second-order batch pass (2026-06-04, fresh window)

**Net result: backlog 194 → 70 (124 removed, ~64%), 0 new bad, `make test` green throughout, census stable at 242. Uncommitted — awaiting approval.** The drained families: F2 ·May (14), F3 ·Excite (9), F8 ·Utter (6), F1 ·They-before-may (38), trailing backward-entry F5a/F6/F7/F9 (6+19), X→·Gay predecessor (7), exit-only predecessor dangles Batch A (12), exit-only+entry-bearing predecessor dangles Batch B (13). All via the `predecessor_demote_overrides` / `trailing_demote_overrides` post-pass lever (no new forms, no cycle rerouting).

**The remaining 70 are the genuinely-hard residue (all need machinery the flat demote lacks):**

1. **Force-bad / hard-track (14):** the `qsTea_qsOy` / `qsThaw.ex-y0` / `qsThey_qsZoo` cross-lookup-compose signatures in `test/leak-force-bad.yaml`, plus the 4 force-bad ·Excite rows. Skip by design.
2. **Ligature-formation compose (~25):** `Et/Awe/Tea Day Utter` (→`qsDay_qsUtter`), `He Day Eat` (→`qsDay_qsEat`), `See Eat/Utter` + `Ye See …` (→`qsSee_qsEat`/`qsSee_qsUtter`), `Vie/Way/Why Utter Bay`, `He Vie Utter` (→`qsX_qsUtter`). The changed side strips INTO a ligature; not a settled dangle a post-pass can revert. The journal's earlier `Et Tea Day Utter` skip showed the `before:` approach reroutes.
3. **·Gay context-dependent cluster (~20):** `They May Gay X` (×12, en-y0 load-bearing only for some predecessors — needs predecessor-of-predecessor awareness), `Awe Gay It/Tea` (×6, inverted direction — `lc` more self-contained than `il`, the fix would have to ADD ink), `Gay Tea No Ah`, `No Gay` (×2, No.alt lesson-3).
4. **Inverted / lesson-3 / ordering (~11):** `Awe He It Zoo` & `Excite Tea Utter Ah` (inverted: `en-y0->ex-y0`), `They Zoo No No` & `Ah ZWNJ No He` (No.alt entry load-bearing, lesson-3), `He Et Tea Day` / `Et Tea Day Utter` / `Jai No It Ah` (demote authored but **does not fire — the demote post-pass runs before the trigger neighbor settles to its bare form**, a lookup-ordering limit of the current emitter), `They Zoo Gay Owe/Jai` (one trailing form, two follower-dependent targets — unresolvable by a single keyed rule), `Ye See Fee Cheer` (·Fee promote, not a demote).

All four buckets are exactly what the prevention plan reserves for the **uniform downstream-revalidation pass that was never built** — they want predecessor/follower-of-neighbor context or ligature-aware reverts that the keyed post-pass tables structurally cannot express. The lookup-ordering limit in bucket 4 is new evidence: even a correctly-authored demote can no-op because `calt_pred_demote`/`calt_trailing_demote` are not strictly last (some cycle lookups settle the neighbor afterward).

**The lever that works where the journal's earlier probes failed: `predecessor_demote_overrides` / `trailing_demote_overrides` (the top-of-file YAML tables, emitted by `_emit_pred_demote_lookups` / `calt_trailing_demote_*` in `tools/quikscript_fea.py:6014-6187`).** Every prior second-order attempt in this journal (the Gay probe, Et Tea Day Utter, No→He) reached for an entry-preserving `ex-noentry` form gated by `before:`/`not_before` — and every one rerouted the `calt_cycle` backward upgrade and grew NEW dangles on the follower. The demote-override tables avoid that entirely: they compile to a **final post-pass** `sub predecessor_form' trigger_form by isolated_form;` that fires only after every cycle/upgrade lookup has settled, matching the exact buffer state. It adds no new form to the cycle, so there is nothing to reroute. It is the right tool for a settled dangle whose neighbor never joins the changed edge. Read the worked entries at quikscript.yaml:157-185 before authoring; author one `{predecessor_form, trigger_form, isolated_form}` triple per (changed-form, settled-follower) pair straight off the backlog signature (`predecessor_form` = `lc`, `trigger_form` = `rc`, `isolated_form` = `il`). Build validates the triple (skips silently if any glyph name is absent), so confirm names exist in the FEA first.

- [F2 batch] ·May baseline-exit dangle ×14 (backlog L86-99: `Ah May See …` / `I May Tea Oy` / `I May See …`) — FIXED: 14 `predecessor_demote_overrides` triples, `predecessor_form` `qsMay.en-y5.ex-y0` (and `…after-i`), `trigger_form` each settled follower (`qsSee.ex-y0`, `qsSee.ex-y0.ex-con-1`, `qsSee.ex-y0.ex-ext-2`, `qsSee.ex-y0.ex-y0-right`, `qsSee_qsEat`, `qsSee_qsUtter`, `qsTea_qsOy`), `isolated_form` `qsMay.en-y5` (keeps the load-bearing en-y5 entry from ·Ah/·I, drops only the dangling baseline exit). The canonical `·Ah·May | ·See·At` dangle from the prevention plan. quikscript.yaml after the existing qsMay demote block (~:171). | make test-leaks: 14 targeted gone, 0 new bad (backlog 194→180) | make test: green (6653 passed).

- **Lesson — `isolated_form: qsGay.en-y0` silently no-op'd; bare revert breaks load-bearing predecessor joins.** Attempted the ·Gay cluster (backlog L38-49, `qsGay->qsGay.en-y0.ex-y5` × 12) with `predecessor_demote_overrides` reverting to bare `qsGay`. Two traps, both general: (1) **Reverting to bare orphaned the predecessor's join.** `qsGay.en-y0.ex-y5`'s en-y0 is a real ·May/·No/·They/·Way/·Why/·Jai/·Gay → ·Gay baseline join; reverting the whole form to bare dropped that entry, so the predecessor's exit dangled → 9 NEW bad leaks (`May Gay Ah`, `Gay Gay Ah`, `No Gay Ah`, `Way/Why Gay Ah`, `Ah/I They Gay Ah`, …). Need an entry-PRESERVING revert (keep en-y0, drop only the dangling ex-y5). (2) **`qsGay.en-y0` is NOT a real glyph** — `qsGay`'s `entry_baseline` form compiles straight to `qsGay.en-y0.ex-y5` (the exit is auto-derived); there is no standalone entry-only `qsGay.en-y0`. The build silently skips a demote whose `isolated_form` isn't in `glyph_names` (quikscript_fea.py:6053), so the rule never emitted (the `by qsGay.en-y0.ex-y5` self-subs you see in the FEA are pre-existing **derived** no-ops). **Verify a target glyph really exists with an anchored grep — `grep '\bqsGay.en-y0\b'` is fooled because `.` matches any char and `qsGay.en-y0` is a prefix of `qsGay.en-y0.ex-y5`.** A correct ·Gay fix needs a newly-authored entry-preserving `qsGay.en-y0.ex-noentry` form (mirror `qsGay.forms.entry_xheight_exit_noentry`) used ONLY as a demote target (no `select`/`before`, so it can't reroute the cycle). Reverted; deferred pending that authoring.

- [trailing-safe batch] backward-entry dangles with no forward exit to lose ×6 — FIXED: 6 `trailing_demote_overrides` triples. `qsExcite.en-y0.noexit` → `qsExcite` after leaders `qsExcite`/`qsIt`/`qsJai`/`qsNo` (drops only the dangling en-y0; `.noexit` already has no exit); `qsOwe.en-y5` → `qsOwe` after `qsMay.en-y5`/`qsNo.alt.en-y0.ex-y0` (entry-only, no exit). Safe because the trailing form carries no forward exit, so reverting to bare cannot break a forward join — the trailing-side mirror of the ·Gay lesson. quikscript.yaml trailing_demote_overrides (~:186). | make test-leaks: 6 targeted gone, 0 new bad (backlog 180→174) | make test: green (6653 passed).

- [trailing-exit batch, F5a/F6/F7/F9] backward-entry dangles whose trailing form carries a forward exit ×19 — FIXED: 19 `trailing_demote_overrides` reverting to the bare isolated form (e.g. `qsNo`/`qsExcite`/`qsJai`/`qsIt` leaders before `qsDay.half.en-y0.ex-y0`/`qsZoo.half.en-y0.ex-y0`/`qsGay.en-y0.ex-y5`/`qsYe.en-y5.ex-y0`/`qsNo.alt.en-y0.ex-y0`; `qsMay.en-y5` before `qsPea`/`qsYe`/`qsOy` en-y5 forms; `qsNo.alt.en-y0.ex-y0` before `qsOy`/`qsYe`; `qsOut_qsTea` before `qsDay_qsUtter.half.en-y0.ex-y5`). The triage's high-confidence keying argument held: a real forward join gives the trailing glyph a _different, exiting_ predecessor, changing leader_form, so the keyed post-pass never fires on a genuine join. Two entries removed during bisection (see lesson below). quikscript.yaml trailing_demote_overrides (~:193). | make test-leaks: 19 targeted gone, 0 new bad (backlog 174→155) | make test: green (6653 passed).

- [F3 batch] ·Excite vertical-exit dangle ×9 — FIXED: 9 `predecessor_demote_overrides` reverting `qsExcite.en-y0.ex-y0.before-vertical.after-baseline-letter` → `qsExcite.en-y0.noexit` (entry-preserving; drops only the dangling baseline exit) before non-joining followers `qsDay_qsEat`, `qsGay.ex-y0`/`.ex-y0.ex-ext-1`/`.ex-y5`/`.ex-y5.ex-ext-1`/`.ex-y5.ex-ext-2`, `qsSee_qsEat`, `qsSee_qsUtter`, `qsTea_qsOy`. The plan's "enumerate unlisted demote triples" — extends the existing qsExcite demote block. 4 force-bad rows (Tea Jay/Tea I/It Day/It Utter) left for the hard track. quikscript.yaml (~:166). | make test-leaks: 9 targeted gone, 0 new bad (backlog 155→146) | make test: green (6653 passed).

- [F1 batch] ·They before-may baseline-exit dangle ×38 — FIXED: 36 `predecessor_demote_overrides` reverting `qsThey.ex-y0.before-may.en-y5` (and `…en-ext-1`) → `qsThey.en-y5` (resp `…en-ext-1`) before 18 non-·May followers each (Day_Eat, Gay.ex-y0{,.ex-ext-1}, It.ex-y0{,.before-day…,.before-utter}, See.ex-y0{,.ex-con-1,.ex-ext-2,.ex-y0-right}, See_qsEat, See_qsUtter, Tea.ex-y0, Tea.half.ex-y5{,.ex-con-1}, Tea_qsOy, Thaw.ex-y0, Utter.alt.ex-y0). The 2 both-changed `Ah/I They Tea I` rows took an extra 2 `trailing_demote_overrides` (revert ·Tea's backward `qsTea.en-y8.ex-y0.ex-ext-1` → `qsTea.ex-y0.ex-ext-1`, fired before the demote) plus the matching `qsTea.ex-y0.ex-ext-1` They-trigger. Entry-preserving (keeps the x-height entry), so no forward join at risk; keyed on non-·May followers so the genuine ·They·May cosmetic tuck is untouched. **Note: these survived the per-rule Phase-2 contract precisely because `before-may` is a cosmetic-opt-out modifier — the contract keeps the form before every follower, so the second-order demote is the only reach.** quikscript.yaml (~:175). | make test-leaks: 38 targeted gone, 0 new bad (backlog 146→108) | make test: green (6653 passed).

- [F8 batch] ·Utter reaches-way-back dangle ×6 — FIXED: 6 `predecessor_demote_overrides` reverting `qsUtter.alt.en-y5.ex-y0.reaches-way-back` → bare `qsUtter` before `qsSee.ex-y0{,.ex-con-1,.ex-ext-2,.ex-y0-right}`, `qsSee_qsEat`, `qsSee_qsUtter`. The F8 bundled-entry+exit nuance: the form's x-height entry is emergent (the ·Fee·Utter join only appears in this cascade), so revert the whole form to bare, not just the exit. The triage F8 listed only the 2 ligature followers; the 4 `qsSee.ex-y0` followers are the same mechanism and were folded in. quikscript.yaml (~:215). | make test-leaks: 6 targeted gone, 0 new bad (backlog 108→102) | make test: green (6653 passed).

- [Gay cluster, mostly deferred] Only `Gay No It Ah` (`qsGay.ex-y0` → bare, initial ·Gay, no predecessor to orphan) FIXED via one `predecessor_demote_overrides` triple. The other ~18 ·Gay rows are **confirmed hard, context-dependent, deferred:**
  - **`They May Gay X` (×12, `qsGay->qsGay.en-y0.ex-y5`):** the bundled `en-y0` is load-bearing for SOME predecessors (·May exiting baseline) but dangles for others (·May exiting x-height after ·They). Revert-to-bare orphans the baseline-join predecessors (9 new dangles); revert-to-`qsGay.en-y0.ex-noentry` (authored an entry-preserving form mirroring qsMay's, demote-only — works as a glyph, never selected by the cycle) keeps `en-y0` but then it dangles at the OTHER break (`May|Gay`) where ·May exits x-height → 1 new dangle (`They May Gay Ah [break 1]`). The right target depends on what precedes ·Gay; the flat demote can't see it. Form removed again.
  - **`Awe Gay It/Tea X` (×6, `qsGay.en-y0.ex-y5->qsGay.ex-y0`):** **direction is inverted** — in-context (`lc`) is `qsGay.ex-y0` and isolated (`il`) is the fuller `qsGay.en-y0.ex-y5`, so the "fix" would re-ADD `en-y0` (a promote, not a demote), which dangles backward for initial ·Gay. Context-dependent; the both-changed `Awe Gay Tea I` is similarly inverted on the ·Tea side. **Lesson: always confirm leak direction by shaping the example before authoring — `il->lc` is isolated->chosen, and the demote target is `il` (revert chosen back to isolated); when `lc` is already the _more self-contained_ form, no demote can help (it would have to add ink).** These need the genuine downstream-revalidation pass (predecessor-of-predecessor awareness), per the prevention plan.

- [X-Gay batch] predecessor exits dangling toward a downstream-stripped bare/x-height ·Gay ×7 — FIXED: `predecessor_demote_overrides` for `qsMay.ex-y0`, `qsWay.half.ex-y0`, `qsWhy.half.ex-y0` (→ bare ·X) and `qsGay.ex-y0`→`qsGay` (Gay No It Ah) before trigger `qsGay` / `qsGay.ex-y5`. These are the journal's "lesson-1 traps" — but the form-specific demote (keyed on the bare/ex-y5 ·Gay that has no baseline entry) is exactly the break-context scoping the journal said was needed; the genuine ·May→·Gay join keeps ·Gay in an `en-y0` form, so the demote never fires on it (confirmed by shaping `May Gay` → join intact). **The ·No siblings (`qsNo.alt.en-y0.ex-y0` → bare) DID break (lesson 3): reverting drops No.alt's load-bearing `en-y0` and orphans ·No's predecessor (·It/·Utter) → 2 new dangles; removed.** quikscript.yaml (~:170). | make test-leaks: 6 May/Way/Why Gay rows + Gay No It Ah gone, 0 new bad (backlog 102→95) | make test: green (6653 passed).

- [Batch A: exit-only predecessor dangles] ×12 — FIXED: `predecessor_demote_overrides` reverting exit-only forms to bare before downstream-stripped bare/exit-only followers: `qsHe.half.ex-y5`→`qsHe` (before It/Jai), `qsPea.half.ex-y5.ex-dips`→`qsPea` (It/Jai), `qsOwe.ex-y5`→`qsOwe` (It/Jai/Ye), `qsRoe.ex-y0`→`qsRoe` (No), `qsShe.ex-y0`→`qsShe` (No/Zoo), `qsJai.ex-y0`→`qsJai` (It.ex-y0/Ye.ex-y0). **These include the journal's earlier `She No Gay Out` skip — the form-specific demote succeeds where the family `not_before` failed, because the genuine ·She→·No.alt / ·He→·It joins keep the follower in an `en` form (not the bare trigger), so the demote never fires on them (confirmed: make test green, the No.alt/junction tests pass).** quikscript.yaml (~:235). | make test-leaks: 12 gone, 0 new bad (backlog 95→83) | make test: green (6653 passed).

- [Batch B: exit-only + entry-bearing predecessor dangles] ×13 — FIXED: `predecessor_demote_overrides`. Exit-only: `qsTea.half.ex-y5`→`qsTea` (It/Jai), `qsWhy.ex-y5`→`qsWhy` (It/Jai), `qsYe.ex-y0`→`qsYe` (No). Entry-bearing (revert to bare; cascade-induced entry, predecessor run-initial in the dangle): `qsExcite.en-y0.ex-y0.before-vertical`→`qsExcite` (No/Zoo), `qsJai.en-y5.ex-y0`→`qsJai` (No), `qsOut.en-y0.ex-y5`→`qsOut` (Jai/Oy/Ye), `qsRoe.en-y0.ex-y5`→`qsRoe` (Jai/Oy), `qsVie.en-y0.ex-y0`→`qsVie` (No). The entry-bearing reverts all held — the form-specific trigger means a real left join (predecessor keeps its `en` form from a genuine exiter) never matches the bare/exit-only trigger. quikscript.yaml (~:247). | make test-leaks: 13 gone, 0 new bad (backlog 83→70) | make test: green (6653 passed).

- **Lesson — two trailing reverts that DID break a real forward join / junction (caught by the gates, removed):** (1) `{qsMay.en-y0.ex-y5, qsGay.en-y0.ex-y5 → qsGay}` (F7 `Ah May May Gay`): reverting ·Gay to bare after ·May dropped ·Gay's forward `ex-y5`, which genuinely joins ·Oy (Oy receives Gay's x-height exit), so `They May Gay Oy` grew a NEW backward dangle (`qsOy->qsOy.en-y5.ex-y0`). Follower-dependent → the demote lever (no follower lookahead) cannot fix it; left in backlog. (2) `{qsNo, qsNo.alt.en-y0.ex-y0 → qsNo}` (F5a `They Zoo No No`): ·No→·No IS a real junction (`test_context_reshapes_to_junction` failed: `qsNo|qsNo` second No must be `qsNo.alt`); reverting the trailing No to bare broke it. Left in backlog. **Takeaway: a trailing revert to bare is unsafe whenever the (leader, trailing-base) pair is itself a real junction OR the trailing's dropped forward exit joins ITS follower. test-leaks catches the second (new forward/backward dangle); only `make test` catches the first (real junction). Always run make test for exit-bearing trailing reverts.** The ·Excite→·No sibling stayed (Excite never joins No), so prune surgically by the specific failing pair, not the whole trailing family.

## Uniform downstream-revalidation pass — the "real Phase 4" (2026-06-04, fresh window)

**Net result: backlog 70 → 69 (1 removed: `·Jai·No`, the canonical lookup-ordering dangle), 0 new bad, `make test` green (6653 passed), census stable at 242. Uncommitted — awaiting approval. The genuinely-last pass is now built; the remaining 69 are proven beyond any pairwise downstream-revalidation (see the exhaustive classification below).**

**What was built (`tools/quikscript_fea.py`, end of `_emit_quikscript_calt`, just before `} calt;`):** a genuinely-last re-emission of the demote post-passes — `_emit_trailing_demote_lookups("calt_final_trailing_demote")` then `_emit_pred_demote_lookups("calt_final2_pred_demote")` — placed after the `calt_when_entered_*` lookups, i.e. after _every_ neighbor-rewriting lookup has settled the buffer. The trailing-demote emission was factored into a reusable `_emit_trailing_demote_lookups(prefix)` helper (mirroring the existing `_emit_pred_demote_lookups(prefix)`) so it can be emitted twice, exactly as the pred-demote already was (`calt_pred_demote` early, `calt_final_pred_demote` mid). This is the prevention plan's "iterate to a fixpoint": one extra full sweep.

**The lookup-ordering bug it cures (the prompt's key finding, now reproduced and fixed).** The demote `sub qsJai.en-y5.ex-y0' qsNo by qsJai;` was emitted at FEA lines 13604 (`calt_pred_demote`) and 20023 (`calt_final_pred_demote`), yet `·Jai·No` stayed in the backlog. Cause: `calt_final_pred_demote_qsNo` (FEA 22145, `sub qsNo.alt.en-y0.ex-y0' … by qsNo;`) settles ·No to its bare form _after_ both Jai demotes have already run (within one `_emit_pred_demote_lookups` sweep, bases emit alphabetically — qsJai before qsNo). So when Jai's demote fires, the ·No neighbor is still `qsNo.alt.en-y0.ex-y0`, the rule keyed on bare `qsNo` can't match, and the dangling `qsJai.en-y5.ex-y0` exit survives. The new `calt_final2_pred_demote_qsJai` runs after `calt_final_pred_demote_qsNo`, sees bare ·No, and fires the authored revert. Two sweeps reach the fixpoint here because ·No settles in sweep ≈1.5 and ·Jai reacts in sweep 2.

**Why two sweeps is exactly the fixpoint (no third needed).** A re-emission only re-runs _authored_ demote triples; it can only newly-fire a triple whose `(predecessor_form, trigger_form, isolated_form)` is authored but was order-blocked. A cross-reference of the 69 remaining backlog signatures against the authored `predecessor_demote_overrides` / `trailing_demote_overrides` tables found **zero** remaining exact-authored triples (`·Jai·No` was the only one, now drained). So no further sweep can fire anything new — fixpoint confirmed by construction, not just by one empirical run.

**Exhaustive classification of the remaining 69 — none is reachable by pairwise downstream-revalidation** (the signature `*L il->lc | *R ir->rc` literally gives the only pairwise revert triple `{lc, rc, il}`, and for every row that triple is unsafe or nonsensical):

| Bucket | Count | Why a genuinely-last pairwise revert cannot reach it |
| --- | ---: | --- |
| Ligature-formation compose | 25 | `lc`/`rc` is a ligature (`qsDay_qsUtter…`, `qsSee_qsEat`, `qsVie_qsUtter`, `qsWay/qsWhy_qsUtter`, `qsDay_qsEat`) that `il`/`ir` is not part of. The pair _fused_; "revert to the single glyph `il`" can't un-fuse a ligature. |
| Force-bad (accepted, `test/leak-force-bad.yaml`) | 14 | Changed side strips to **bare** while an unchanged ligature neighbor (`qsTea_qsOy`, `qsThaw.ex-y0`, `qsThey_qsZoo`) keeps reaching, plus the 4 `Awe Excite Tea/It …` rows. The defect sits on the _unchanged_ neighbor; there is no `lc` edge to demote. Deliberately accepted. |
| ·Gay / ·No context-dependent | 17 | `qsGay.en-y0.ex-y5` and `qsNo.alt.en-y0.ex-y0` bundle a **load-bearing entry** with the dangling exit; reverting to bare `il` orphans a real predecessor join (lesson 3), and the entry-preserving target dangles at the _other_ break — the correct choice needs predecessor-of-predecessor awareness, not pairwise. |
| Inverted / predecessor-aware | 10 | `il` is `*.en-y0` (entry-only) and `lc` is `*.ex-y0` (exit-only) — different edges; the revert `lc→il` would restore an entry that then dangles _backward_ for a word-initial predecessor (`Awe He It Zoo`, `Excite Tea Utter Ah`). Needs predecessor context. |
| Real join / junction | 3 | The "dangle" is the formation of a genuine connected-text join/junction (the second-·No `qsNo.alt` junction; the `qsTea.en-y0` → `qsDay.half.en-y0` baseline join; the `qsFee.en-y8` promote) that only the boundary-faithful isolation reference treats as a non-join. Reverting would break the real join (`make test`). |

**Bottom line.** The genuinely-last pass _is_ the real Phase 4 for everything pairwise downstream-revalidation can express, and it now drains the order-blocked-authored class to empty (the 1 case that existed). The residual 69 are structurally beyond a pairwise revert — they need ligature-aware reverts, predecessor-of-predecessor context, or are deliberately accepted (force-bad) / genuinely-correct joins. A fully _derived_ geometry contract (compute `exit_ys ∩ entry_ys` from anchors instead of reading authored triples) would compute the **same** pairwise triples and so reduce the backlog no further; its only remaining payoff is architectural — enabling the Phase-3 retirement of the hand-authored demote tables — which is separate, lower-priority work and was **not** undertaken here.

## Phase-3 retirement re-sweep against the genuinely-last pass (2026-06-05, fresh window)

**Net result: ZERO tables retired. All four candidates remain load-bearing even with the genuinely-last downstream-revalidation pass (commit c43fd16) in place. `make test` 6653 passed and `make test-leaks` green on baseline; every probe reverted byte-identical (six OTFs + Senior FEA confirmed identical to the captured snapshot). Working tree clean, nothing committed.**

The premise this sweep tested: the "Phase 3 as attempted" sweep found four tables load-bearing, but they may have been load-bearing _only_ because the genuinely-last `calt_final_trailing_demote` + `calt_final2_pred_demote` re-emission did not yet exist. It does now. So each table was re-disabled with the smallest no-op (empty it / early `return`), rebuilt with `make all`, and judged by Senior-FEA byte-diff + six-OTF `shasum` against `make snapshot-before`, then `make test` (full) and `make test-leaks` by **signature set-diff** (never `git diff` line count — the measurement trap). Verdict rule: byte-identical OTFs + green gates = redundant (retire); any divergence = load-bearing (revert, journal the signatures it holds). Baseline captured at c43fd16: six OTFs sha + a copied `test/before/*.fea`.

| Candidate | Disable | Build delta | Gate result | Verdict |
| --- | --- | --- | --- | --- |
| Forward-strip guards — whole emitter | early `return` in `_emit_narrow_mid_entry_strip_guards` | Senior FEA ±4,258 lines; Senior OTFs changed | **114 NEW bad** (·Excite before-vertical, `qsMay.ex-y0`, `qsNo.alt`, `qsRoe/qsHe/qsIt.ex-y0`, `qsGay.ex-y0.ex-ext-1`, the `qsX_qsUtter.ex-ext-1` ligature family, `qsOwe`, …) | Load-bearing — keep |
| Forward-strip guards — data table only | `derive_pending_fwd_strip_guards` → `{}` | Senior FEA ±15,314 lines; Senior OTFs changed | **27 NEW bad** (prior sweep's 23 grown by sweep-label churn; same families: She Gay Tea Oy, She Gay Out, ·Excite·Gay, ·X·Utter·They·May) | Load-bearing — keep (final pass did **not** shrink the surface) |
| `_PENDING_BK_ENTRY_GUARDS` | emptied to `{}` | Senior FEA ±223 lines; Senior OTFs changed | **12 NEW bad** (Excite Tea I/Ah/No-Ah/Awe/Jay, Et Tea May Awe/Fee/Ah, Et Tea Roe, Et Tea Vie Utter, Et Tea Vie Excite) | Load-bearing — keep (final pass subsumed **3**: prior sweep's 15 → 12) |
| Early `calt_pred_demote` (alone) | `pass` at the early emission | Senior FEA ±4,926 lines; Senior OTFs changed | **1 NEW bad** (`Jai No It Ah`) | Load-bearing — keep |
| Mid `calt_final_pred_demote` (alone) | `pass` at the mid emission | Senior FEA ±6,412 lines; Senior OTFs changed | **1 NEW bad** (`Jai No It Ah`) | Load-bearing — keep |
| Early `calt_trailing_demote` (alone) | `pass` at the early emission | Senior FEA −108 lines (pure deletion, 0 added); Senior OTFs changed | `make test` green (6653), `make test-leaks` green — **but Senior OTF changed** | Keep — test blind spot (uncovered shaping change), not byte-identical, so not proven subsumed |
| All three early/mid demote emissions | `pass` at all three | Senior FEA ±12,946 lines; Senior OTFs changed | **4 NEW bad** (Excite No Gay Out, Gay No It Ah, Jai No Gay Out, Owe It Tea Oy) | Load-bearing — keep |
| ZWNJ firewall, coverage pass (`_ensure_zwnj_coverage_for_calt_lookups`) | `return lines` early | uni200C rules 4,482 → 17; Senior FEA ±5,193 lines | **43 `test_calt_regressions` ZWNJ failures** | Load-bearing — keep |
| ZWNJ firewall, two-position pass (`_add_zwnj_guards_for_two_position_forward_rules`) | `return lines` early | exactly the 6 `ignore sub qsIt(.noentry)' qsDay uni200C;` rules removed (3 lookups × 2); Senior OTFs changed | `make test` green, `make test-leaks` green | **Keep anyway** — same known test blind spot the prior sweep flagged: the removed rules are genuine `qsIt qsDay ⟨ZWNJ⟩` cross-boundary protection, and the demote sweep cannot emit ZWNJ guards, so green gates are blind-spot, not subsumption |

**Why nothing retired.** The genuinely-last pass is a _demote-revert_ mechanism: it re-emits `sub pred' trigger by iso` / `sub leader trailing' by iso` triples after the buffer settles. It is structurally incapable of (a) emitting the preventive `ignore sub` rules that the forward-strip and ZWNJ firewalls produce, or (b) reverting a form _early_ so an intermediate lookup reads the reverted shape. The four candidates all live in one of those two classes the final pass cannot reach:

- **Forward-strip guards and the ZWNJ coverage pass** are preventive `ignore sub` machinery. Disabling either re-opens dangles (114 / 27 new bad) or breaks ZWNJ's hard boundary (43 test failures). The final pass reverts settled dangles; it never blocks a substitution from firing, so it cannot stand in for an `ignore`.
- **`_PENDING_BK_ENTRY_GUARDS`** is the closest to being trimmed — the final pass subsumed 3 of its 15 signatures (15 → 12) — but 12 backward-entry dangles (·Excite·Tea, ·Et·Tea·May/Roe/Vie families) still survive because the curated `before_bases` narrowing they encode has no equivalent in the pairwise revert.
- **The early/mid demote emissions are NOT redundant with the genuinely-last re-emission**, contrary to the hypothesis. Bisected individually: dropping early `calt_pred_demote` _or_ mid `calt_final_pred_demote` each re-opens `Jai No It Ah` and changes the OTF, because each fires before a later neighbor-rewriting lookup (the noentry contracts / `calt_when_entered`) that reads the demoted form. They are a genuine three-point fixpoint iteration, not three copies of one pass — the final sweep alone catches only the order-blocked tail (`·Jai·No`), not the mid-stream reverts. Dropping all three at once gives only 4 new bad (the early ones mask each other's regressions) but still changes the OTF.
- **The early `calt_trailing_demote` is the one ambiguous case**: disabling it produces 0 new bad leaks and green `make test`, yet the Senior OTF changes (a pure 108-line FEA deletion — the early `calt_trailing_demote_*` blocks vanish, their `calt_final_trailing_demote_*` twins remain). The byte change means firing the trailing revert early vs. only at the end alters some rendered sequence that no current gate exercises. Per the byte-identity bar (and the prior sweep's ZWNJ-two-position precedent), an uncovered shaping change is a test blind spot, not proven redundancy — kept. **Follow-up worth a coverage test:** find the exact sequence whose Senior shaping flips when only the late trailing demote remains; if it turns out cosmetically equivalent, the early `calt_trailing_demote` becomes the single genuinely-retirable emission this whole effort has surfaced.

**Bottom line for the Phase-3 goal.** The genuinely-last pass closed the order-blocked-_authored_ gap (its stated purpose) but did not move the Phase-3 retirement needle: the demote _tables_ and the two firewalls remain load-bearing. As the prevention plan predicted, retiring the hand-authored machinery needs a fully _derived_ geometry contract (and, for the early `calt_trailing_demote`, a coverage test that proves the late-only timing equivalent) — neither exists, so all four tables stay.
