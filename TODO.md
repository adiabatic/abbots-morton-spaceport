# TODO

- Make a ·Bay·Utter ligature (`qsBay_qsUtter.prop`)
- Make a ·Gay·Utter ligature (`qsGay_qsUtter.prop`)
- Make a ·Jay·Utter ligature (`qsJay_qsUtter.prop`)
- Add a mechanism (possibly variation selectors) for writers to explicitly force display of specific glyph variants that are currently only selectable via automatic `calt` rules
  - Alternate letterforms:
    - `qsNo.alt`
    - `qsUtter.alt`
  - Half-letters:
    - `qsPea.prop.half`
    - `qsTea.half.*`
    - `qsDay.half`
    - `qsWay.half`
    - `qsHe.half`
    - `qsWhy.half`
    - `qsZoo.half`
  - Investigate how Arabic fonts handle explicit glyph variant selection for inspiration
