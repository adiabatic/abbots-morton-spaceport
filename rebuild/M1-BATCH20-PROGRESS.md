# M1 batch 20 — qsGay

Scratch for the ·Gay migration. Delete when the batch closes, lifting any surviving forward-pointer into `WHATNEXT.md`.

## Parked

- The drafted `ductus` awaits the author's vet; the `# DRAFT` marker is the worklist.
- **The ·It yield after ·Gay is the ·Bay ruling's shape applied on the oracle's evidence, not a fresh user ruling — confirm or strike at the sitting.** After ·Gay the old font has ·It give up the backward join only to serve ·Day, ·Day+Utter and ·Utter — exactly the followers qsIt's pre-wired after-·Gay refuse already permits the entryless exit toward — and M1's entered-vs-entryless tie otherwise breaks backward. Expressed as a yielding prefer on qsIt (left qsGay) mirroring the ·Bay record verbatim, ·Utter carve included: on the ·Utter chains toward ·Vie/·Vie+Utter/·See/·May/·Low, ·Utter's own records rule the seam, and before ·Vie/·See/·Low the old font has ·It joining nothing at all, a fewer-joins shape M1's floor cannot pick, so those windows keep the ·Gay ~b~ ·It join and diverge as seam gains for the sitting, exactly as ·Bay's do.
- **The backward-yield prefer on qsGay is agent-transcribed from the old record's unconditional `not_before`, not a fresh user ruling — confirm or re-scope at the sitting.** Before the seven reach-for followers the old font never lets ·Gay keep a backward baseline join (`entry_baseline`'s `not_before` was context-free), so the rune carries a yielding prefer of the entryless baseline exit over the entered cell, right-scoped to the same list as the extend. Where the forward join dies anyway — ·Tea busy with its ·Day bond, ·Tea+Oy taking no entry, ·It yielding onward — the old font isolates ·Gay while M1's floor keeps the backward join, so those windows diverge as seam gains for the sitting, the ·Bay/·Key precedent. A `mode: absolute` variant would match the old font exactly but demands a user-voiced `why:`; the sitting can choose it. The record needs an own-chain twin — a follower's prefer votes at the seat one position left, except at the one seat a vote structurally cannot reach, ·Gay after ·Gay (the qsUtter·Utter limitation) — so a second yielding prefer reads `right: qsGay then` the same follower list and makes a preceding ·Gay release the self-join the way every other left does (`·Gay·Gay·Tea` probes break,y0, the old font's shape). Confirm both at the sitting.
- Expected sitting divergences with no record authored, all standing shapes replicated behind the new letter: ·Gay·Utter·May keeps M1's `y0,y5` rendering where the old font breaks to the alternate ·Utter (the ·Bay precedent, riding earlier sittings' verdicts); ·Utter·Gay·{J'ai,Oy} carry qsUtter's flat by-1 exit extend where the old font's compiled forms skip it (a 1px position delta; the rune's extend list is the old YAML's own flat target list); and the ·See·At entry-extension drift, the ·See·Out fusion, the ·Key exit contraction, and the smaller-loop ·I recur with ·Gay in the flanks, where the standing approvals and the echo prefill are expected to absorb them.

## Recorded design overrides

- ·Gay is one stance: a Deep crown-and-loop — a short crown bar at the x-height, a zigzag descent to the baseline, and a loop closing below it. Two entries and two exits, and an entry at one height always pairs with the exit at the other (`pairings.never` on both same-height pairs): the old record's four anchor sets carry exactly the crossover pairs, and no same-height entered form exists in any config's rows.
- The two old join-summoned shapes re-spell per the rubric as join-localized ink, not stances: the x-height entry's narrowed crown is a stub (`cols: [3], inks_when: withdrawn`, the qsMay.grounded-loop shape), and the baseline entry binds the `reachable-at-the-baseline` sibling (ink added at cols 0–1 and dropped at col 3, which one stub cannot say) with `joined_x: 0`.
- Allowlists transcribed from the old font's pair rows, migrated partners only: nine baseline enterers (·Gay itself included — ·Gay·Gay joins at the baseline), ·Utter alone at the x-height (the four migrated `*_qsUtter` ligatures ride in through left-facing trail transparency), eighteen entryless baseline receivers plus ·Ooze-when-entered — a `self: {entry: none}` refuse holds the entryless ·Gay·Ooze break the old font draws while the entered form serves ·Ooze — and six x-height receivers.
- The baseline by-1 exit extend is entryless-only (`self: {entry: none}`): entered ·Gay joins ·Tea/·Day/·Roe/·It/·I unextended in every old-font window, and no `en-y5.ex-y0.ex-ext` form exists in any config. The x-height by-2 extend toward ·J'ai fires entered and entryless alike, meeting qsJai's and qsJai_qsUtter's pre-wired by-2 entry extends (left qsGay), which go live now — and are then suppressed in every realized window by the section 6.2 same-seam non-summing law, because ·Gay's exit always carries the seam's ex-ext-2. The old font summed both sides, so the rebuild draws that connector one pixel shorter; the rows ride `same-seam-extension-non-summing` (the classifier's same-seam token generalized to the by-2 grain, and the qsJai-side `en-ext-2` aliases now carry the adjustment they had normalized away, restoring the name-grain truth the class needs).
- Dead old directives not transcribed, the qsAwe/qsBay no-evidence precedent: the x-height extend's qsIt target and the family-level qsOwe target (no such join in any pair row; ·Gay·Owe breaks everywhere), and `extend_entry_after` toward the x-height halves (no half enters ·Gay in any config). qsCheer's by-2 and qsFoot's by-1 x-height reaches are deferred to those families' migrations.
- ·Gay yields its ·Tea join wherever ·Tea gains a forward join by it — the qsJai yielding prefer, verbatim, on the same oracle shape as ·Awe, ·Ox, ·Eight, ·Ooze, ·Bay and ·Key. The owed ·Utter spot-check passes: `·Gay·Utter·Tea·Day` lands old-font (y0, break, y0) through qsUtter's standing vote with no new record.
- qsGay stays out of `SS10_UNCOVERED_BY_OLD_FONT` on direct evidence: bare qsGay carries no anchors in the old record, so every qsGay seam breaks under the old ss10 overlay on both sides, matching the new overlay exactly. No conform arm and no new ledger class.
- The ·Day/·No ~b~ ·Tea yield lists got their owed glance: ·Tea cannot reach ·Gay (·Gay's baseline entry admits nine lefts and ·Tea is not among them), so no `·Day·Tea·Gay` / `·No·Tea·Gay` regrouping window exists to adjudicate.
- qsAh's recon-derived from-list membership for qsGay is now oracle-verified (·Gay ~b~ ·Ah joins in every non-ss10 config); the WHATNEXT re-verify tail shrinks to five.
- The declined baseline exit declares `withdrawal: safe`: the foot's terminal pixel continues vertically into the loop below the baseline. The crown's ink on a declined x-height exit is unconditional letterform ink (bare ·Gay draws the full crown at every word edge); its dangle blessings land from the gate's own signatures.

## Verification recipe

```zsh
uv run pytest rebuild/test_spec_load.py -n auto --dist worksteal
uv run python -m rebuild.pipeline.run_m1
PYTHONPATH=. uv run python rebuild/tools/probe.py E67A:E655
PYTHONPATH=. uv run python rebuild/tools/probe.py E651:E655:E65D
PYTHONPATH=. uv run python rebuild/tools/probe.py E655:E652:E653
PYTHONPATH=. uv run python rebuild/tools/probe.py E655:E67A:E652:E653
PYTHONPATH=. uv run python rebuild/tools/probe.py E655:E670:E653
PYTHONPATH=. uv run python rebuild/tools/probe.py E655:E670:E67A:E667
PYTHONPATH=. uv run python rebuild/tools/probe.py E653:E655:E652
PYTHONPATH=. uv run python rebuild/tools/probe.py E67A:E655:E67E
make test-rebuild
make test
make artifact-cycle
make verdict-ready
```

## Resume

```zsh
make review-cycle
```
