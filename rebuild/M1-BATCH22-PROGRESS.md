# M1 batch 22 — qsThey (and qsThey_qsUtter)

Scratch for the ·They migration. Delete when the batch closes, lifting any surviving forward-pointer into `WHATNEXT.md`.

## Parked

- The drafted `ductus` on both runes awaits the author's vet; the `# DRAFT` markers are the worklist.
- **The hook's reach is transcribed from the shipped font's behavior, not from the old YAML's stance name — confirm at the sitting.** The old record calls the hooked baseline exit `exit_baseline_before_may` and its inline note calls it an author-declared cosmetic tuck for ·May, but the entered stance that inherits it lost the before-·May gate, so the shipped font lifts the hook into every baseline-entering follower whenever ·They is entered (`E676:E657:E668` lands y5,y0 into ·Roe, likewise ·Tea, ·Day, ·Gay, ·Thaw, ·Vie, ·See, ·No, ·Low, ·It, ·At, ·I, ·Ah, ·Out, ·Ooze and the ·Day+Utter, ·Vie+Utter and ·Out+Tea ligatures), and its demote patch drops the hook only where the follower cannot take the join. The rune keeps that: the baseline exit's toward-list is the wide one, and a `self: {entry: none}` refuse scoped to every follower but ·May holds the unentered pair rows to the ·May tuck alone (`E657:E668` stays a top-side y5, `E657:E652` stays a break). The one-line alternative — toward `[qsMay]` and no refuse, the old YAML read literally — turns every entered ·They seam except ·May into a seam-loss divergence.
- **·They+Utter's crown contracts ·J'ai by one pixel everywhere, where the shipped font drew it only after ·May, ·No, ·Low and ·At — confirm at the sitting.** The old ligature record declares `contract_exit_before: by 1` toward qsJai, but the compiled font realizes it only on the `.noentry` form those four lefts summon (`E665:E657:E67A:E65D` advances 500; bare `E657:E67A:E65D` advances 550, uncontracted), an emission-order accident the rune does not reproduce: the declared pixel lands as ·J'ai's own by-1 entry contraction after qsThey_qsUtter (the qsJai lesson, on the record that carries the other left-side tucks), and the ligature is carved out of qsJai's and qsJai_qsUtter's by-2 records that trail transparency would otherwise hand it. The windows where the old font left the seam uncontracted become one-pixel position divergences and stay UNMATCHED, to land through verdicts; the opposite ruling (no contraction anywhere) trades them for the after-·May/·No/·Low/·At windows.
- **The ·It yield after ·They is the ·Key ruling's shape applied on the oracle's evidence, not a fresh user ruling — confirm or strike at the sitting.** After entered ·They the old font has ·It give up the backward baseline join to serve every baseline receiver (`E676:E657:E670:E653` lands y5,break,y0; likewise ·Thaw, ·Vie, ·See, ·May, ·No, ·Low, ·At, ·I, ·Ah, ·Utter, ·Out, ·Ooze) while keeping it before the x-height receivers, so qsIt carries the qsKey yielding prefer verbatim with left qsThey, ·At-then-·May and ·Utter carves included.
- Expected sitting divergences with no record authored, all standing shapes recurring behind the new letter: the ·They+Utter ligature's ss10 rows diverge at ligation grain and ride the Junior-equivalence machine approval as qsJai_qsUtter's do (the classifier's ss10 ligature arm still names only its three pairs; widening it is the conform-side decision `WHATNEXT.md` already parks); entered ·They ~b~ ·Gay at a word end lands under `dangling-anchor-dropped` (the old font names ·Gay's unserved x-height exit); and the X·Utter reach-back gains and `bare-name-live-join` name-grain rows recur with ·They in the flanks.

## Recorded design overrides

- ·They is one stance: a Deep down-stroke and hook — the stroke leaves the x-height slanting down-left through the baseline and curls right and back up below it. One x-height entry at the top (anchor [2, 5]), admitting ·I, ·Ah and ·Utter (the four `*_qsUtter` ligatures ride in through left-facing trail transparency, every one oracle-witnessed); one x-height exit from the same top pixel ([3, 5]) toward the seven migrated x-height receivers the pair rows join; and the hooked baseline exit ([6, 0] with `ink_y: -1`, the old `exit_ink_y`). Entering and exiting at the top at once is impossible for one stroke, and no `en-y5.ex-y5` form exists in any config, so `pairings.never` forbids it.
- The old `before_may` shape re-spells per the rubric as join-localized ink, not a stance: the same motion continued, the hook climbing two pixels further to meet the baseline. It is a bound sibling (`hooked-up-to-the-baseline`) named by the two baseline-exiting cells, so the isolated drawing and every top-side cell stay on the bare bitmap.
- Allowlists transcribed from the old font's pair and triple rows, migrated partners only: three x-height enterers; seven x-height receivers; nineteen baseline receivers (the entered-·They evidence above), with qsUtter deliberately absent because ·They·Utter always forms the ligature. qsLlan, qsOwe and qsFoot (x-height side) and qsEat, qsExam, qsExcite and qsIng (baseline side) wait for their own migrations.
- The old `extend_entry_after` toward `halves_and_i_exit_xheight` transcribes as nothing: no half enters ·They in any config (·Pea·They and ·Tea·They break everywhere, the qsGay no-evidence precedent), and the `en-ext-1` the old font spells after ·I is a no-op in ink — the shipped glyph adds its connector pixel at the anchor column without moving the anchor, and that column is ·I's own last pixel, so `qsI|qsThey.en-y5.en-ext-1` places every pixel exactly where the unextended join does (the same holds for `qsThey_qsUtter.en-ext-1`). A transcribed by-1 record drew the seam a pixel longer than the old font on every ·I·They window, so the runes carry no entry extend and the three `en-ext-1` aliases fold onto the unadjusted entered cells. After ·Ah and ·Utter the reach is theirs (`qsAh.ex-ext-1`, `qsUtter.ex-ext-1`), carried by the pre-wired exit extends on those runes, and the four `*_qsUtter` ligature runes gain qsThey on their by-1 exit extends on the same evidence.
- Right-facing lists never match a ligature by its lead, so every exit-side record the old font fires before both ·They and ·They+Utter names the ligature beside the letter: the exit extends on qsAh, qsUtter and the four `*_qsUtter` runes, and qsJai_qsUtter's toward-list (`E65D:E67A:E657:E67A` lands lig,y5,lig).
- The ligature always forms after ·They, so qsUtter refuses its mono x-height exit after an unjoined ·They before the x-height receivers the ligature does not serve (qsBay, qsThey, qsThey_qsUtter, qsFee — the `left: qsJai, joined_at: none` refuse's shape): without it M1 un-forms ·They+Utter to buy the ·Utter ~x~ ·Bay join the old font never draws there (`E657:E67A:E651` is lig,break in every config). Widening the ligature's toward-list instead would create those joins rather than hold the old breaks; the sitting can choose that shape.
- ·They yields its ·Tea join wherever ·Tea gains a forward join by it — the qsJai yielding prefer, verbatim, on the same oracle shape as ·Awe, ·Ox, ·Eight, ·Ooze, ·Bay, ·Key and ·Gay (`E676:E657:E652:E653` lands y5,break,y0; `E676:E657:E652:E650` keeps the join). ·They never enters ·Utter except as the ligature, so the owed `X·Utter·Tea·Day` spot-check has no window; `E657:E67A:E652:E653` (the ligature standing aside for ·Tea·Day) is probed instead.
- The ligature is a single `hapax` stance with ·They's entry (same from-list, the same no-op `en-ext-1` after ·I folded by alias) and ·Utter's x-height exit at [11, 5] toward the sixteen migrated receivers the rows join, qsTea among them for the ss03 half-·Tea reach (qsTea's half-stance unlock left-list gains the ligature). The old `extend_exit_before_gated` toward ·Tea is not transcribed: unlike its four siblings the shipped ligature reaches half-·Tea unextended under ss03 (`E657:E67A:E652` advances 550). The old `noentry_after` list re-spells as nothing: ·May, ·No, ·Low and ·At are simply outside the entry's from-list, the old `.noentry` spelling aliases to the bare ligature (the qsThaw.after-tall shape, since it is never a ZWNJ shadow), and ·May's withdrawn exit before it aliases to the entered loop cell.
- qsJai's dead by-1 arm after bare qsThey came off its guarded contract record, and qsJai_qsUtter's likewise: ·They's top-side exit never reaches ·J'ai in any config, so the old target was only ever the ligature's, which now carries it under its own name (the qsIt-toward-qsJai precedent).
- qsAh's recon-derived from-list membership for qsThey is oracle-verified, entered-·They side (`E675:E657:E676` lands y5,y0); the WHATNEXT re-verify tail shrinks to four.
- qsThey stays out of `SS10_UNCOVERED_BY_OLD_FONT` on direct evidence: bare qsThey carries no anchors in the old record, so every qsThey seam breaks under the old ss10 overlay on both sides, matching the new overlay exactly. No conform arm and no new ledger class.
- Both declined exits declare `withdrawal: safe`: the top pixel continues down the stroke, and the hook's ink exists only in the bound sibling, so the bare drawing has nothing to withdraw.

## Verification recipe

```zsh
uv run pytest rebuild/test_spec_load.py -n auto --dist worksteal
uv run python -m rebuild.pipeline.run_m1
PYTHONPATH=. uv run python rebuild/tools/probe.py E676:E657
PYTHONPATH=. uv run python rebuild/tools/probe.py E675:E657
PYTHONPATH=. uv run python rebuild/tools/probe.py E657:E668
PYTHONPATH=. uv run python rebuild/tools/probe.py E657:E665
PYTHONPATH=. uv run python rebuild/tools/probe.py E657:E652
PYTHONPATH=. uv run python rebuild/tools/probe.py E676:E657:E668
PYTHONPATH=. uv run python rebuild/tools/probe.py E676:E657:E652:E653
PYTHONPATH=. uv run python rebuild/tools/probe.py E676:E657:E652:E650
PYTHONPATH=. uv run python rebuild/tools/probe.py E676:E657:E670:E653
PYTHONPATH=. uv run python rebuild/tools/probe.py E676:E657:E655:E652
PYTHONPATH=. uv run python rebuild/tools/probe.py E657:E67A
PYTHONPATH=. uv run python rebuild/tools/probe.py E676:E657:E67A
PYTHONPATH=. uv run python rebuild/tools/probe.py E665:E657:E67A
PYTHONPATH=. uv run python rebuild/tools/probe.py E657:E67A:E65D
PYTHONPATH=. uv run python rebuild/tools/probe.py E665:E657:E67A:E65D
PYTHONPATH=. uv run python rebuild/tools/probe.py E657:E67A:E659
PYTHONPATH=. uv run python rebuild/tools/probe.py E657:E67A:E652
PYTHONPATH=. uv run python rebuild/tools/probe.py E657:E67A:E652:E653
make test-rebuild
make test
make artifact-cycle
make verdict-ready
```

## Resume

```zsh
make review-cycle
```
