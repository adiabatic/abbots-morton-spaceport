# Policy round 1 plan: applying the verdict round

The apply-phase plan for the round-1 verdicts (441 verdicts over 725 human review units, `verdicts-10.42.06PM.json`). Inputs and proofs, both recon files since deleted and in git history only: `rebuild/recon/policy-round-1-rejects.md` (recon A — every edit below was validated by re-running the full section 13.1 oracle gate, 51,546 rows × 11 configurations, against scratch rune copies; the combined variant `tmp/lab/v-final2` passes with 0 unmatched rows and the control reproduces the committed audit byte-identically) and `rebuild/recon/policy-round-1-reconcile.md` (recon B — contradictions, skips, and the machine-readable assertion inventory `rebuild/out/policy-round-1-assertions.json`).

Binding interpretation: the user’s final taste likes the ·May pulled-back bitmap generally; the earlier “·May should use the loop stance” skip-notes are superseded. Hard rules restated: edits only under `glyph_data/runes/`, `rebuild/`, and `tmp/`; the old pipeline stays byte-identical (Senior Sans OTF SHA-256 `3211a7a76be0e3c032c06eead1dace2d5cbf4f05c63a9a742c23c3117625cf35`); `make test` stays green; Python via `uv run` only, broad pytest always `-n auto --dist worksteal`; nothing committed or staged; no verdicts fabricated — the 284 unverdicted units stay unverdicted and the 8 skips get PROPOSED resolutions only.

## 1. The YAML edits

Six edits total: one record deletion and five added `prefer` records, across four rune files. All five additions are yielding prefers (no `mode: absolute`, no new `refuse`); every record carries a `why:` quoting the user’s note. This is exactly recon A’s settle-validated `v-final2` set — apply it verbatim, then prove equivalence with the gates of section 3 rather than re-deriving anything by hand.

### Edit 1 — `glyph_data/runes/qsIt.yaml`: delete `policy.extend[0]` (the halves entry-extension record)

Delete the record `{stance: bar, entry: x-height, by: 1, when: {left: {class: halves-that-exit-at-x-height, except: [{family: qsPea}], joined_at: x-height}}}` together with its current transcription comment (the “Transcribed faithfully from the old YAML…” line above it), and put the verdict on record in a replacement comment — a deletion carries no record, so the `why:` obligation transfers to the comment:

```yaml
  extend:
  # The old YAML's halves-minus-qsPea x-height entry-extension record was transcribed here for M1 and deleted in the round-1 verdict pass: the shipped font never realizes it (probed: both ·Tea·It and ·He·It join un-extended today), and the reviewer rejected every window where M1 realized it — "this widens the ·Tea·It extension for no reason".
  - {stance: bar, entry: baseline, by: 1, when: {left: {family: qsKey, joined_at: baseline}}}
```

Delete rather than narrow, per recon A section 1.3: (i) within the M1 alphabet a narrowed record is byte-identical to deletion; (ii) the only out-of-alphabet trigger is half-·He, where the shipped font also never draws the extension (probed), so deletion is what ground truth says and narrowing would preserve a dead letter; (iii) the note rejects the realized ink as such, not one trigger of it. This is the revert-the-authoring-decision shape, preferred over any additive counter-record — the contract counter-record was built and disproven (entry-side extend and contract do not net at name grain; 6 UNMATCHED oracle rows; fires inside 31 approved `ss03-chain-join-gains` windows).

- **Rejected windows flipped (51):** u-0444…u-0458, u-0475…u-0489, u-0491…u-0510 (50, to byte-identical old ink) and u-0490 (to old seams; residual is only the final-taste-approved pulled-back ·May cell).
- **Either/unverdicted windows also flipped (tolerated, reported):** either u-0443 (to old ink); 124 unverdicted halves units — 105 to old ink, 19 to a residual that is only the approved ·May withdrawal cell (full id lists in recon A section 1.4).
- **Approved windows:** zero approved windows change except the five documented carve-outs u-0383, u-0424 (`entered-it-baseline-join-gain`), u-0272 (`pre-ligature-cleanup-regularized`), u-0356 (`ss03-chain-join-gains`), u-0267 (`zwnj-follower-exit-restored`) — in each, the gained join survives and the only change is the deletion of the exact pixel the user rejected by name; the two skips u-0397/u-0399 (“the extension between ·Tea and ·It shouldn’t change because ·It joins to something else next”) demand precisely this. u-0468, the lone approve inside the rejected class, is **unchanged** under this edit set (it is a phenomenon-1b window; recon B’s expectation that the halves revert would touch it is corrected by recon A section 1.4). The other 35 windows of recon B’s 41-window entry-extension watch list are untouched (their extension ink comes from `qsMay.policy.extend[3]`, which this round does not modify). Proof: variant `v-it`, identical inside `v-final2`, row-by-row diff in `rebuild/evidence/v-final2/diff.json`.

The 10 rejects of phenomenon 1b (`qsMay.policy.extend[3]` composing `en-ext-1+ex-ext-1` under ss03-before-·Tea: u-0461…u-0467, u-0469, u-0471, u-0474) are **deliberately not edited** — no clean revert exists in the closed vocabulary, the counter-record was disproven, and the verdict signal is internally contradictory (the identical cell was approved in u-0468 and in the 40 `ss03-chain-join-gains` units). They go to the out-of-scope register (section 4) with a one-question proposal.

### Edit 2 — `glyph_data/runes/qsOy.yaml`: add `policy.prefer[0]`

`policy.prefer` is currently `[]`; it becomes:

```yaml
  prefer:
  - {cell: {exit: none}, over: {exit: baseline}, when: {right: {family: [qsTea, qsIt]}}, why: 'Round-1 verdict, 37 windows: when the groupings tie, ·Oy leaves its baseline exit unrealized so the follower pair joins instead — "I think I would rather prefer ·Tea·It to join" (u-0280); "The old way seems nicer to write out by hand" (the rest). Yielding, so a strict-gain ·Oy·Tea/·Oy·It join is kept.'}
```

- **Rejected windows flipped (34):** the ·Oy-steals-the-follower’s-join shape of `regrouping-floor-drift` — the 37 rejects of recon A section 2.1 minus the 3 ligature-twin windows covered by edit 3 (unit ids in recon A section 2.3). For u-0280 specifically this is the class-level fix; the unit-local drafted record in its shard (an anti-join `prefer` with no `over:`) must **never** be applied (recon B section 1.4).
- **Either/unverdicted windows also flipped (tolerated, reported):** the regrouping eithers that share the shape (within u-0279…u-0338, see recon A section 2.3), and — the one genuine cost — 4 unverdicted machine-approved units (u-2068, u-2224, u-2225, u-2386; 20 rows) become visible regrouping drift in the user-preferred direction (more follower joins; u-2068 is a two-for-one). They are the only machine-approved rows anywhere in the audit that change, they classify cleanly under `regrouping-floor-drift`, and the regenerated review surface presents them for fresh verdicts.
- **Approved windows:** none. The record is yielding — it sits at the tier between window join-count and the structural floor, so it flips only structural-floor ties and leaves every strict-gain ·Oy·Tea/·Oy·It join (e.g. the bare ·Oy·Tea pair, which the old font joins) untouched; no refuse and no absolute prefer anywhere. Proof: variant `v-oy` inside `v-final2`.

### Edit 3 — `glyph_data/runes/qsTea_qsOy.yaml`: add `policy.prefer[0]` (the ligature twin)

The same record verbatim, with the `why:` noting the twin relationship:

```yaml
  prefer:
  - {cell: {exit: none}, over: {exit: baseline}, when: {right: {family: [qsTea, qsIt]}}, why: 'Ligature twin of qsOy.policy.prefer[0] — ligatures do not inherit component policy (proven by u-0283/u-0284/u-0285), and forming ·Tea·Oy must not change the grouping: when the groupings tie, the ligature leaves its baseline exit unrealized so the follower pair joins — "The old way seems nicer to write out by hand".'}
```

- **Rejected windows flipped (3):** u-0283, u-0284, u-0285 — the windows where `qsTea_qsOy` has formed and the qsOy record is invisible (variant `v-oy` left exactly these unchanged; `v-oy2` flips them).
- **Either/unverdicted also flipped:** u-0285’s seam-grain siblings per recon A section 2.3; nothing else.
- **Approved windows:** none, same yielding argument as edit 2; proof `v-oy2` inside `v-final2`.

### Edit 4 — `glyph_data/runes/qsIt.yaml`: add `policy.prefer[0]` (decline ·It·It before ·May)

`policy.prefer` is currently `[]`; it becomes:

```yaml
  prefer:
  - {cell: {exit: none}, over: {exit: baseline}, when: {right: {family: qsIt, then: {family: qsMay}}}, why: 'Round-1 verdict, ·X·It·It·May (u-0278, u-0282, u-0297): before a ·May that needs the baseline entry, the entered ·It declines the ·It·It join so ·It·May keeps it — "the former looks less awkward to right" (sic), "The old way seems nicer to write out by hand". Yielding and then-scoped, so the approved ·It·It join gains everywhere else are untouched.'}
```

`right.then` is legal on `prefer` (the schema forbids it only on `refuse`/`require`); a bare refuse here would overreach onto the 33 approved `entered-it-baseline-join-gain` windows.

- **Rejected windows flipped (3):** u-0278, u-0282, u-0297.
- **Either/unverdicted also flipped:** none.
- **Approved windows:** none — variant `v-itit` changes exactly these 3 units and **nothing else in the entire audit** (the strongest surgical proof in the set).

### Edits 5 and 6 — `glyph_data/runes/qsMay.yaml`: add `policy.prefer[0..1]` (the greedy ·May·May pairing)

`policy.prefer` is currently `[]`; it becomes:

```yaml
  prefer:
  - {cell: {exit: baseline}, when: {left: {is: boundary}, right: {family: qsMay}}, why: 'Round-1 verdict on ·May·May·May·May — "the old way seems nicer to write out by hand": at word start, pair up — the grounded baseline join into the next ·May beats declining when the join counts tie.'}
  - {cell: {exit: baseline}, when: {left: {family: qsMay, joined_at: none}, right: {family: qsMay}}, why: 'Chain interior of the same verdict ("the old way seems nicer to write out by hand"): after an unjoined ·May, pair with the next ·May. The acceptance oracle''s window universe tops out at four letters, where the word-start record alone already reproduces every outcome (including the u-0341 quad flip), so this record is invisible to the divergence audit — do not delete it as redundant. Its real load is ·May chains of five or more, which without it regress to the rejected defer-to-the-tail grouping instead of the old greedy y0 | break | y0 | break pairing the shipped font draws at every length; the length-5 and length-6 settle assertions in rebuild/test_settle.py pin it. Like the word-start record, its left: condition can never match a non-·May predecessor seam.'}
```

The scoping is the subtle part and is settled empirically (recon A section 3): the naive `{cell: {exit: baseline}, when: {right: {family: qsMay}}}` poisons 65 ·X·May·May windows (28 approved) through the section 5.9 follower vote, and a follower-side record is a no-op between same-rune neighbors. These two records’ `left:` conditions can never match a non-·May predecessor candidate, so they are invisible to the follower path.

Minimality nuance (round-1 adversarial-audit finding, closed): the chain-interior record is **byte-invisible to the entire acceptance audit** — removal-and-rerun of the section 13.1 oracle leaves `divergence-audit.tsv` byte-identical, because the oracle’s window universe tops out at four letters and there the word-start record alone reproduces every outcome, including the u-0341 quad flip. The record is genuinely load-bearing only for ·May chains of length ≥ 5, which without it regress to the rejected defer-to-the-tail grouping (settle-probed at lengths 2–7; the shipped old font shapes every length as the greedy `y0 | break | y0 | break` pairing, HarfBuzz-probed). A remove-if-audit-invisible minimality cleanup must therefore not delete it; the length-4/5/6 settlement rows in `rebuild/test_settle.py` pin the greedy pairing so any such deletion fails the pytest gate, and the record’s `why:` states the real justification.

- **Rejected windows flipped (1):** u-0341 (·May·May·May·May settles `y0 | break | y0`, the old pairing, with the middle ·May wearing the final-taste-liked pulled-back cell).
- **Either/unverdicted also flipped (tolerated, reported):** the 11 ·May·May·May eithers u-0286…u-0296 (back to the old first-pair grouping, which their either verdicts permit); unverdicted u-0857 (◊ZWNJ ·May·May·May) joins its first pair post-ZWNJ, exactly like its space-boundary sibling u-0287 and consistent with the six approved `zwnj-follower-exit-restored` units.
- **Approved windows:** none — variant `v-maymay4` moves no other row in the audit; conformance stays green. The `may-quad-order-deferral` ledger class goes to zero and its entry is retired (section 2).

### Cross-cutting consequences (validated in `v-final2`)

- **13 rejects flip at seam grain with a 1 px residual** (u-0281, u-0285, u-0300, u-0303, u-0327…u-0333, u-0336, u-0340; plus u-0316 whose residual is the approved ss03 ·May withdrawal cell): in `[X]·Oy·It·May` shapes the restored ·It·May baseline join carries ·May’s `en-ext-1` because “the predecessor’s predecessor is entered” is not expressible in the closed `when:` vocabulary. The residual is the same single-extension connector the user approved 28 times in `same-seam-extension-non-summing`, and it errs in the direction the user favored. Those 91 rows reclassify into `halves-entry-extension-restored`; the re-review pass presents them.
- **Total flip arithmetic, of the 107 rejects:** 79 return to byte-identical old ink; 16 return to old seams with only final-taste-approved residual cell deltas (the pulled-back ·May or the single-pixel entry extension); 12 are surfaced as contradictions/questions with no edit (10 phenomenon-1b + 2 `same-seam-extension-non-summing`, see section 4).
- **Audit totals:** divergent rows 15,528 → 15,525 (0 unmatched, 0 multi-matched); 3 rows (u-0576’s ss03 configurations of ·Tea·It·May·Tea) become fully conformant; 1,738 rows change class or cells.

## 2. Ledger updates — `rebuild/m1-divergences.yaml`

### Status vocabulary

The verdict round introduces two review statuses: `reviewed-approved` (the class behavior was triaged by the user and approved; the divergence is now user-ratified, not merely drift-accepted) and `reviewed-rejected` (the class behavior was rejected and this round’s records restore the old behavior; the surviving rows are the documented residue). The ledger header comment and whatever validates the status enum in `rebuild/pipeline/conform.py` must admit them alongside `intended | drift-accepted | triaged`. Each flipped entry’s `why:` gains a sentence recording the verdict round (quoting the note where one exists) and stale exemplars are refreshed to surviving rows.

### Per-entry changes

| Entry                               | Status before  | Status after      | Rows before | Rows after | Notes                                                                                                                                                                                                                                                                                                                                                                           |
| ----------------------------------- | -------------- | ----------------- | ----------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `halves-entry-extension-restored`   | drift-accepted | reviewed-rejected | 1,218       | 160        | Departures: 1,055 rows → `dangling-anchor-dropped` (ink-identical), 3 rows fully conformant, 91 rows → `may-exit-withdrawal-generalized`. Stays: 69 phenomenon-1b rows. Arrivals: 91 regrouping-residual rows (section 1, cross-cutting). Exemplar `E652:E670` flips — replace with a 1b ss03 window. `why:` rewritten to record the deletion verdict and the open 1b question. |
| `regrouping-floor-drift`            | drift-accepted | reviewed-rejected | 435         | 34         | Stays: u-0277 (7 rows). Arrivals: u-0857 (7 rows, via edits 5–6) + the 4 machine-approved units (20 rows, via edit 2). Departures: 428 rows to `dangling-anchor-dropped` / `bare-name-live-join` / `may-exit-withdrawal-generalized` / the halves residual bucket. Both ·Oy exemplars flip — refresh.                                                                           |
| `may-quad-order-deferral`           | drift-accepted | **retired**       | 7           | 0          | Delete the entry and its `may_quad_order_deferral` predicate from `conform.classify_divergence` (its “flagged for review at qsMay’s next policy pass” sentence is now discharged); assert zero rows would have classified to it.                                                                                                                                                |
| `dangling-anchor-dropped`           | intended       | intended          | 9,283       | 10,586     | +1,303 ink-identical arrivals from the reverts.                                                                                                                                                                                                                                                                                                                                 |
| `may-exit-withdrawal-generalized`   | drift-accepted | reviewed-approved | 1,227       | 1,390      | +163 arrivals whose only remaining delta is the final-taste-liked pulled-back cell. Class verdict: 85 approve / 52 either / 0 reject.                                                                                                                                                                                                                                           |
| `bare-name-live-join`               | intended       | intended          | 917         | 914        | −3.                                                                                                                                                                                                                                                                                                                                                                             |
| `entered-it-baseline-join-gain`     | drift-accepted | reviewed-approved | 319         | unchanged* | 33 approve. Exemplar’s new-side cell loses `en-ext-1` after edit 1 — refresh the exemplar string.                                                                                                                                                                                                                                                                               |
| `marker-staging-ligature-formation` | intended       | intended          | 187         | unchanged* | 32 approve ratifies the intent; note the verdict in `why:`.                                                                                                                                                                                                                                                                                                                     |
| `ss03-chain-join-gains`             | drift-accepted | reviewed-approved | 120         | unchanged* | 40 approve.                                                                                                                                                                                                                                                                                                                                                                     |
| `pea-chain-regularized`             | drift-accepted | reviewed-approved | 101         | unchanged* | 15 approve.                                                                                                                                                                                                                                                                                                                                                                     |
| `pre-ligature-cleanup-regularized`  | drift-accepted | reviewed-approved | 38          | unchanged* | 6 approve. Exemplar shows `en-ext-1` — refresh after edit 1.                                                                                                                                                                                                                                                                                                                    |
| `zwnj-follower-exit-restored`       | drift-accepted | reviewed-approved | 30          | unchanged* | 6 approve.                                                                                                                                                                                                                                                                                                                                                                      |
| `same-seam-extension-non-summing`   | intended       | reviewed-approved | 225         | unchanged* | 28 approve / 2 reject / 3 either; the 2 rejects are the open question of section 4, recorded in `why:` as presumed noise pending one ruling.                                                                                                                                                                                                                                    |
| `zwnj-word-initial-unification`     | intended       | intended          | 1,421       | unchanged* | No human verdicts; definitional.                                                                                                                                                                                                                                                                                                                                                |
| `kern-channel-out-of-scope`         | triaged        | triaged           | 0           | 0          | Unchanged; retire at cutover if still zero.                                                                                                                                                                                                                                                                                                                                     |

\* “unchanged*” = recon A’s table lists these classes as count-unchanged at class grain; individual rows inside them may change cells (the section 1.5 carve-outs lose the deleted pixel). The authoritative counts are whatever the Phase 3 conformance run writes to `divergence-audit.tsv`; the table above is the recon-validated expectation the run must match. Counts in the ledger are filled from the run and reviewed as a diff — the run never writes the ledger file.

Arithmetic check: net change across the six count-changing entries is (160−1218) + (34−435) + (0−7) + (10586−9283) + (1390−1227) + (914−917) = −3, matching 15,528 → 15,525 with the 3 fully-conformant rows.

**Oracle-conformance invariant after the round:** every remaining divergent row still matches exactly one entry — `conform.classify_divergence` keeps partitioning by construction, the retired predicate is removed, and the run must report 0 unmatched / 0 multi-matched. `v-final2` already demonstrated this end state.

## 3. Acceptance gate sequence (apply phase)

In order; each gate must be green before the next is meaningful. Steps 1–5 are the inner loop (iterate on YAML/ledger to green); 6–10 are the outer verification.

1. **Schema + lints.** `spec_load` over the edited rune files: the five new records validate against `rebuild/schema/rune.schema.json` (yielding prefers need no `why:` by schema, but all five carry one; `right.then` appears only on a `prefer`), no dead-policy regressions introduced by the deletion.
2. **`run_m1` pipeline green** — `uv run python -m rebuild.pipeline.run_m1`: tables + TSVs, outcome-partition invariant, glyph minting, the section 9 E-gates (`E-STRANDED`, `E-DANGLE`, `E-UNREALIZED`, `E-ANCHOR`, off-anchor contact, dead policy, budget gate), FEA emit, mini-font build. Specific watch: re-verify the `E-UNREALIZED` gap arithmetic on the former halves windows — the old ledger note (“gap arithmetic is clean with the one-pixel-longer connector”) was written with the extension present; the unextended join is today’s shipped geometry at those heights, so no gap is expected (recon A section 1.5).
3. **Oracle conformance with the updated ledger** (`compare_against_baseline`): 0 unmatched, 0 multi-matched over the 8 acceptance configurations plus the ss06/ss07 identity assertion; expected totals 15,525 divergent rows with the per-class counts of section 2 (treat any deviation from the `v-final2` numbers as a defect in the application, not a new finding).
4. **The four assertion buckets from `rebuild/out/policy-round-1-assertions.json`** (recon B section 5), with the documented carve-outs:
   - _rejected (107):_ 79 shape identically to the baseline tables; 16 match at seam grain with only the documented residual cell deltas (pulled-back ·May, single `en-ext-1`); 12 are carved out pending the user’s answers (10 phenomenon-1b + u-0636 + u-0656) and must shape identically to **M1**, i.e. unchanged.
   - _approved (246):_ shape identically to the verdicted M1 build for 240; the six carve-outs are u-0383, u-0424, u-0272, u-0356, u-0267 (joins must survive, only the `en-ext-1` pixel deleted — diff these individually) and u-0468 (must be **unchanged**, correcting recon B’s expectation). Diff the 41-unit watch list window by window: every gained join survives.
   - _either (77):_ report every change (expected: u-0443, the regrouping eithers, the 11 ·May-triple eithers); all changes must land on a bucket-listed acceptable rendering or the old behavior.
   - _skip (8) / neither (3) / unverdicted (284):_ report changes, never record verdicts; expected: u-0397/u-0399 lose the widening and keep the join, ≈130 unverdicted units re-converge (≈828 of 1,549 rows), u-0857 + the 4 machine-approved units become visible.
5. **Font conformance sweep** on the rebuilt M1 mini-font: exhaustive length-1–5 HarfBuzz-vs-`settle` sweep exact (the ledger never applies font-side), per-transition shortest-example gate with ZWNJ-interleaved variants, RuleCoverage total, split-buffer equivalence, gap-0 pen positions, ZWNJ structural checks; CoreText smoke over the extended sequence set.
6. **Explain spot-checks on 5 flipped windows** (`uv run python -m rebuild.pipeline.explain …` per configuration), asserting the new provenance is cited:
   - `E652:E670` (default; an edit-1 window, e.g. u-0444’s): the entered ·It cell carries no `en-ext-1` and no extend record fires at the seam.
   - `E650:E679:E652:E670` (default; u-0280): ·Oy’s decline is attributed to `qsOy.yaml policy.prefer[0]` and ·Tea·It joins at the x-height.
   - u-0283’s window (ligature twin): the decline is attributed to `qsTea_qsOy.yaml policy.prefer[0]`, not the component record.
   - u-0297’s window (·X·It·It·May): the first ·It’s decline is attributed to `qsIt.yaml policy.prefer[0]` with the `right.then` match shown.
   - `E665:E665:E665:E665` (default; u-0341): settles `y0 | break | y0` with the pairing attributed to the two `qsMay.yaml policy.prefer` records.
7. **`make test` green** (full suite, xdist) and `uv run pytest rebuild/ -n auto --dist worksteal` green. After any Python change (conform status enum, predicate retirement, review tooling): `make prettier` **and** `uv run --with black black -q rebuild/`.
8. **Old-font byte identity:** `make all`; `shasum -a 256 site/AbbotsMortonSpaceportSansSenior-Regular.otf` == `3211a7a76be0e3c032c06eead1dace2d5cbf4f05c63a9a742c23c3117625cf35`. The rune files do not feed the old pipeline, so any drift here is a hard stop.
9. **Review-surface regeneration** (`rebuild/review/` → `rebuild/out/review/`, served on 7294): new manifest, new unit shards, after-font rebuilt (its SHA changes from `750f89…80ce` — record the new hash in the manifest and the report).
10. **Verdict re-import sanity.** `verdicts-10.42.06PM.json` must import against the new manifest with correct carry-over. Two expected frictions, both handled, neither silently passed: (a) the manifest-timestamp mismatch warning (`rebuild/review/export.py:107`) **will** fire — expected, documented; (b) **unit ids shift**: unit ids are positional over the audit, and ≈130 units re-converge and vanish while residual reclassifications and the 4 newly-visible machine units mint new ids. Re-import must therefore key on the stable window identity (codepoints + configuration set + class lineage), not raw `u-NNNN` ids, and the report must include the old-id → new-id mapping plus three reconciliation counts that must sum to 441: verdicts carried onto surviving units, verdicts whose windows are now fully conformant (resolved-by-revert — the 50 + 29 byte-identical reject flips and any either that re-converged), and verdicts on units whose window changed enough to need re-presentation (the re-review queue: the 16 residual rejects, the section 1.5 carve-outs, u-0397/u-0399, the eithers that moved). No verdict may be dropped without landing in one of the three counts.

The re-review queue handed to the user afterward is exactly recon A’s hand-off list: the 13 + 91-row residual-extension windows, the 4 newly-regrouping machine units, u-0857, u-0356, u-0267, u-0272, the entered-·It carve-outs including skips u-0397/u-0399 (expected approvable per their own notes), and the 12 unresolved rejects with their questions.

## 4. Out-of-scope register

Nothing below is edited or recorded this round.

1. **The 10 phenomenon-1b rejects** (u-0461…u-0467, u-0469, u-0471, u-0474) — one question for the user: “Under ss03, when ·May joins from ·Pea/·Tea at the baseline and also extends toward a following half-·Tea, M1 lengthens the baseline connector by one pixel (the old font did not). You rejected this on ·Pea·May·Tea-shaped words but approved the identical ·May cell on ·Tea·May·Tea-shaped words (u-0468 and the 40 ss03-chain approvals). Keep M1’s composed extension everywhere, or is this worth engine work (a feature-scoped suppression) to drop it?” Until answered they stay divergent under `halves-entry-extension-restored` and the rejected-bucket assertion carves them out.
2. **The 2 same-seam rejects** (u-0636, u-0656) — re-present next to u-0637/u-0654/u-0663 and ask for one ruling; the rejects contradict 28 same-phenomenon approvals at window grain and honoring them would repeal the section 6.2 non-summing law. Presumed noise until ruled.
3. **The 8 skips — PROPOSED resolutions only, never recorded** (recon B section 1.7): u-0223/u-0224 approve (the post-ZWNJ `qsTea_qsOy` ligature is genuinely 2 px narrower — highlight 550 → 450 units, advance 700 → 600); u-0243/u-0244/u-0245/u-0250 approve (·May already settles on the loop stance with the pulled-back exit bind; the note is superseded by the final taste); u-0397/u-0399 re-render after edit 1 and take a fresh verdict.
4. **The 284 unverdicted units** — stay unverdicted. ≈130 (the 129 halves + 1 regrouping; 828 of 1,549 rows) are expected to re-converge with baseline and leave the workload entirely; the remaining ≈154 stay as siblings under approved classes; the 4 machine-approved units of edit 2 plus u-0857 become newly visible and join the human queue. All changes counted and reported, none auto-verdicted.
5. **The follow-up ledger** (recon B section 4): indefinite ·It join chains (u-0421, approved with a wish); the forward-looking settle-order preference “keep an existing ·Tea·It join rather than floor-regroup ·Oy·Tea” (u-0280’s note — and its unit-local drafted record must never be applied); the ·Tea·It-widening cross-reference (u-0397/u-0399 — expected discharged by edit 1, confirm in Phase 3 before closing); the 3 neithers needing third designs (u-0215 ·May joining into the `qsTea_qsOy` ligature; u-0683/u-0716 the word-initial locked ·May before ·Tea, recorded after the change of mind and not superseded).
6. **u-0468** — expected **unchanged** this round; it flips only if the user later rules against phenomenon 1b (item 1).
