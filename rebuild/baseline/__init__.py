"""Baseline extraction for the rebuild (doc/rebuild-design.md section 13.1): shape every string in the depth-2 basis through the built Senior Sans font as a black box, and write each string's outcome to a diff-stable table under rebuild/out/. rebuild/BASELINE-PLAN.md is the plan and describes the public interfaces."""
