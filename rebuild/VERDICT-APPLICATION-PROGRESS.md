# Verdict-application progress

The living checkpoint for turning the round-3 review verdicts into rune-data edits, one logical change at a time. Each change is made, eyeballed, and committed on its own before the next begins — commits are the real checkpoint; this file tracks what each commit accounted for, what remains, and the open questions.

> **Resume here (2026-07-03, updated again after the phase-end re-baseline).** This is an in-flight pass _within_ the M1 round-3 adjudication — finish it before resuming WHATNEXT.md's main migration thread. **The extensions/contractions phase is COMPLETE** (E1 `8749048`, E3 `5ffff0e`; E2 parked; same-seam + contractions no-op). **Phase 2 (`seam-loss-withdrawal`, 83 rejects) is FULLY DISCHARGED and fully COMMITTED — Groups A, B, D-1, and C are all in** (Group C landed as the user-directed alt-chain shape `·Tea ~b~ ·No.alt`, one line in `qsNo.yaml`, committed in `6ab6309`; D-2 and the "6 engine-limited" park dissolved — ZWNJ targets retired by the boundary rule, the ·Oy·Tea·No pair restored by Group C's acceptor). **The phase-end review-surface rebuild + census-pin re-baseline is DONE (2026-07-03):** `run_m1` recompiled M1.otf at HEAD (oracle 5,477 unmatched, `multi_matched` 0, boundary gate 885,808 runs / 0 divergences, Manual pins 17/17), the surface rebuilt in place into `rebuild/out/review` (15,972 units / 80,990 rows / 20 batches, stale shards cleared first), and every review census pin re-baselined — see "Gates / known state". **Next: the human blessing adjudication on the fresh surface** — the verdict families now sum 1,149 windows, including the ~980-row ·Tea·No chain (in `seam-loss-withdrawal`, now 373 units), Q2's ·Pea·No chains, and the Q1★/Q4 new-join gains. **(2026-07-04) That adjudication is 2,815/4,010 units in, and applying its fresh verdicts is under way — see "Phase 4" below** (DT1 committed as `658a3c3`; DT2 committed as `362122f`; **FT1 — the ss03 full-·Tea gap, prescribed by the fresh u-6645/6646/6647 neithers — is COMMITTED `a60d780` (user-eyeballed, the except list dropped on the rendered evidence); **IT1 — ·It never joins to itself — is COMMITTED `4589d20`**, its artifact cycle (surface rebuild, verdict carry, census re-pin) done the same day**; the two remaining WHATNEXT asks from the blessing pass — ·Pea·Pea into ·No.alt, an x-height-enterable ·Utter.alt — stay untriaged). Workflow rules: one logical change per commit, verify each with the **full-rebuild recipe** (not just the settle probe — see "Resume commands"), show the user before committing, and keep this file honest. Working style the user likes: scope each edit tightly, prove it surgical, present pros/cons, don't commit without approval. **(2026-07-04, later) The blessing pass is COMPLETE — every available unit verdicted, exported as `verdicts-01.46.12AM.json` — and applying that final export is Phase 5 below: IU1, PIT1+DID1, DTIU1, INO1, NALT-TEA, PPN1, IU2, and the SS10 engine work (SS10-LIG, superseded same day by SS10-FORM — formation itself now suppressed) are all COMMITTED (nine commits, `26d2927..f7cfae0`, each independently A/B-verified and gated; oracle 7,882 → 8,423, `make test` 6,753 green) and the commit-time artifact cycle is done (2026-07-04 — fresh surface, verdict carry, census re-pin; see the Phase-5 closing update); SS03-MAYTEA and UALT1 are parked on user decisions.**

## Source of truth

- **Verdicts:** `verdicts-11.33.00PM.json` (format `ams-review-verdicts/1`, manifest `2026-06-29T04:45:25Z`, 4,828 unique-unit verdicts: 4,146 approve / 332 reject / 318 neither / 20 either / 12 identical). The manifest it pins matches the live `rebuild/out/review/manifest.json`.
- **Units:** `rebuild/out/review/units/*.json` — each unit (`u-NNNN`) carries `class`, `notation`, before/after cells + seams + `extensions`, the `explain` settle trace, and a `drafts.policy` suggestion (target rune file, keypath, a concrete record). The drafts mostly frame everything as `prefer`/`refuse`, so the **review class** is the reliable signal for what a window is about, not the draft keypath.

### Verdicts are evidence, not gospel

Rune edits are **rule-level** (one `extend`/`contract`/`refuse` record with a `when:` predicate); verdicts are **window-level evidence**. A single record decision usually subsumes dozens of windows. Where a recorded verdict no longer reflects intent (a mind-change), the override is captured **here**, at the rule level — the gitignored verdict export is left untouched.

**Recorded overrides:**

- **·It·Day connector = 1px, not 2px.** ~108 windows carry `approve` for the entered-·It exit extension before ·Day (`qsIt.yaml` `extend[1]`, the `ex-ext-1` 2px connector), plus 7 `either` and 10 already-`reject`. Mind-changed to: ·It·Day reverts to the legacy single-pixel connector. Handled by change **E1** below.
- **·Tea·No = the baseline alt chain, not the x-height join (2026-07-03).** The user's stated preference: absent other considerations, ·Tea·No should render `·Tea ~b~ ·No.alt` (full ·Tea joining alternate ·No at the baseline) rather than the old font's default `·Tea.half ~x~ ·No`. This supersedes, at rule level: the x-height-first reading of the u-14734–14742 reject notes ("if ·Tea doesn't join ·No at the x-height then we should use a full-size ·Tea and a ·No.alt" — the fallback clause is now the preference), the 8 trailing-pair approvals that pinned ·X·Tea·No at y5 (u-15258, u-1842, u-1848, u-1863, u-1881, u-1887, u-15496, u-15137), and the old-parity T8 shape that was briefly applied and then replaced the same day. Handled by the final Group C landing below.

## Phases

Working order (user's call): **extensions/contractions first** (most inconsistent, cheapest YAML), then the larger `prefer`/`refuse`/gain piles.

The extension/contraction story is concentrated in three review classes plus one cross-cutting record:

| Class | windows | approve | reject | other | meaning |
| --- | ---: | ---: | ---: | --- | --- |
| `halves-entry-extension-restored` | 183 | 1 | 172 | 10 identical | receivers (·Pea/·May/·Day/…) extending their **entry** for a half that exits at x-height — overwhelmingly rejected |
| `same-seam-extension-non-summing` | 88 | 88 | 0 | — | keep as-is |
| `extension-non-summing` | 30 | 20 | 10 | — | mixed (·Tea+Oy→·Day keep; ·May·Day·Utter reject) |
| _cross-cutting_ `qsIt` `extend[1]` (entered-·It exit extension) | — | see below | — | — | fires before ·Day/·It/·May/·Utter/·No; votes differ sharply by right-neighbor |

`qsIt` `extend[1]` firings by right-neighbor (windows with a verdict): ·Day 108a/10r/7e · ·It 93a/2r · ·May 89a/0r · ·Utter 63a/12r/48n/6e · ·No 1a · qsDay_qsUtter 4a/1r/1e.

## Extensions/contractions checklist

- [x] **E1 — ·It·Day single-pixel connector. COMMITTED `8749048`.** Narrow `glyph_data/runes/qsIt.yaml` `policy.extend[1]` with `right: {except: [family: qsDay, family: qsDay_qsUtter]}` so the entered-·It exit extension no longer fires before ·Day **or the ·Day+·Utter ligature** (same half-·Day baseline seam) — 1px connector — while ·It·It / ·It·May / ·It·Utter keep the 2px extension. **Verified by full rebuild** (see recipe below): net +1 UNMATCHED window in default/ss02/ss05, zero elsewhere. E1 makes ·It·Day **uniformly** 1px — a _deliberate divergence from the old font_ in the one window where the old font drew 2px (`u-14916` ◊ZWNJ·May·It·Day, ·It entered at x-height post-ZWNJ): **user-confirmed LGTM 2026-06-29, keep uniform 1px.** That window + its boundary siblings are intended divergence to bless at the next re-baseline, not a regression.
- [ ] **E2 — `halves-entry-extension-restored` (172 rejects). PARKED (decision 2026-06-29: skip, do easy items first).** Stays documented; revisit as either the empirical sibling-deletion test (~103) or the engine milestone (~27 wall). 0 approve / 172 reject (10 identical = pixel-identical, harmless). Traced to two distinct phenomena:
  - **The round-2 engine wall (~27, ·May).** `qsMay.extend[3]` (baseline entry, the `en-ext-1`) in the `pea-may` (composed `en-ext-1`+`ex-ext-1` under ss03) and `it-may` ([X]·Oy·It·May, predecessor's-predecessor entered) windows. `m1-divergences.yaml`'s `halves-entry-extension-restored` entry + `POLICY-ROUND-2-REPORT.md` record an **exhaustive, build-tested 25-candidate lever hunt that found no closed-vocabulary rune record can revert these without collateral** — the discriminator is a depth-2 left-context predicate the frozen `when:` grammar cannot express. Honoring these = the deferred **extension-suppression engine milestone**, not a rune edit.
  - **The x-height-halves siblings (~103, NEW).** `qsDay.extend[0]`, `qsOy.extend[0]`, `qsNo.extend[0]`, `qsPea.extend[0]`, `qsMay.extend[4]` — all the `{entry: x-height, by: 1, when: {left: {class: halves-that-exit-at-x-height, joined_at: x-height}}}` shape. These are the **same transcription-artifact record round-1 deleted from `qsIt`** ("the shipped font never realized it"); these siblings just weren't reached, and ~103 of them only surfaced as divergent after the 2026-06-27 pulled-back re-baseline, so they **predate the round-2 analysis**. **Hypothesis: cleanly deletable parity-revert.** Unconfirmed — needs an empirical oracle test (delete each record on a scratch build via `rebuild/tools/scratch_build.py`, confirm UNMATCHED drops with zero collateral on currently-matched windows) before any deletion, because the review surface contains only divergent windows and can't by itself prove a record never fires in a matched one.
- [x] **E3 — ·May·(Day+Utter) x-height exit extension restored.** The 10 `extension-non-summing` rejects are all ·May·Day·Utter (`ext=0`, decided by order, no record): the old font drew `qsMay.ex-ext-1` but the new font dropped it because `qsMay.extend[0]`'s trigger list had bare `qsDay` (and `qsFee/qsJai/qsJay/qsRoe/qsIt`) but **not** the `qsDay_qsUtter` ligature. Fixed by adding `qsDay_qsUtter` to that list (code-point order, right after bare `qsDay`) — the mirror of E1's ligature point. Verified: ·May·(Day+Utter) and its follower variants regain `ex-ext-1`; bare ·May·Day unchanged. The 20 approves (·Tea+Oy→·Day etc.) are a different record, untouched. **COMMITTED `5ffff0e`.** Trips two expected baseline pins (`test_review_enrich`, `test_review_build::test_secondary_seam_census` −6 units) that clear on the phase-end review rebuild.
- [x] **`same-seam-extension-non-summing` (88 approve) — NO-OP confirmed.** All approve; the engine's behavior is liked, no record changes.
- [x] **Contractions sweep — NO-OP this batch.** No unit cites any `policy.contract` record and no cell carries a contraction marker: every `contract:` trigger (·J'ai/·Zoo/·Jay/·Fee on `qsNo`/`qsUtter`/`qsTea`/`qsMay`) is an **unmigrated family**, so the records can't fire in the M1-batch-2 corpus. Re-adjudicate when those families migrate.

**Extensions/contractions phase: COMPLETE** (2026-06-29) — E1 + E3 committed, E2 parked, same-seam + contractions no-op.

## Phase 2 — seam-loss-withdrawal (FULLY DISCHARGED 2026-07-03; all groups committed)

The 83 `seam-loss-withdrawal` rejects (the 2026-06-27 pulled-back-removal aftermath, biggest actionable reject pile) triage into **three root causes / five logical changes**. Triaged + empirically settle-tested 2026-06-29 (workflow `seam-loss-withdrawal-triage`; harness `rebuild/tools/seam_loss_probe.py` and the clean-vs-candidate differ `rebuild/tools/seam_loss_diff.py`). **Use `seam_loss_diff.py` as the collateral arbiter** — it re-settles BOTH the real runes and a candidate dir under the default config and flags only windows that actually move; the bare shard comparison reads STALE shard `after` seams and reports phantom regressions (the trap two triage agents nearly fell into).

| # | Logical change | Rune edit | Restores | Collateral | Status |
| --- | --- | --- | ---: | --- | --- |
| **A** | word-initial ·Utter·No keeps its x-height join | `qsUtter` refuse `{stance: alternate, exit: baseline, when: {left: {is: boundary}, right: {family: qsNo}}}` | 43/config (44 ss03) | 0, all 8 configs | **COMMITTED** (`e300b13` + the `flipped`→`alternate` rename; user-framed as "'un-' shouldn't use alternate ·Utter", The Manual p21). Full-rebuild verified −304 oracle rows; `make test` green (6753). |
| **B** | ·It·It·Utter joins again | `qsIt` policy.refuse (the before-·Utter veto whose `when` is `self: {entry: none}`, `right: {family: qsUtter}`): drop `qsIt` from its `left.family` list | 16/16 | 0, all configs | **COMMITTED (2026-07-02, one commit with D-1).** Re-verified at HEAD (16/16, 0 collateral); full-rebuild diff: ss04 −15 UNMATCHED, each ss03-bearing config −1, default/ss02/ss05 net 0, zero new UNMATCHED; all 17 moved units are ·It·It·Utter windows landing in blessed ledger classes. Taste-note: under default, ·It·It·Utter·May now regroups to the ·It·Utter join and breaks ·Utter·May (old chose the opposite; both single-join; lands in `regrouping-floor-drift`). |
| **D-1** | ·No·May·May·May joins | `qsNo` policy.prefer `{cell: {exit: baseline}, over: {exit: x-height}, when: {left: {is: boundary}, right: {family: qsMay}}}` — **amended 2026-07-02: the `left: {is: boundary}` guard is load-bearing** | 1/3 | 0, all configs | **COMMITTED as amended (2026-07-02, one commit with B).** The triage's unguarded record predates the Group A commit and now regresses 15 restored ·Utter·No·May windows (y5 → break) — ·No flips for ·May and a joined predecessor loses its x-height entry. Boundary-guarded, the settle differ shows 1/3 restored (the u-15162 target; the other 2 are D-2's) with zero other movement; full-rebuild diff: −1 UNMATCHED in all 7 configs (the target resolves into `dangling-anchor-dropped`), all 15 moved units are word-initial ·No·May·May windows in blessed classes. |
| **C** | word-initial ·Pea·No / ·Tea·No join | see the "Group C resolved" section below — the ·Pea half was superseded by Q2's alt chain; the ·Tea half landed 2026-07-03 as **one line**: `qsTea` added to flipped ·No's baseline-entry `from:` list | every adjudicable reject discharged (·Tea·No now chains; the ·Oy windows byte-exact) | the 8 y5-pinned approvals flip by design (recorded override) | **COMMITTED `6ab6309` (2026-07-03, user-directed alt-chain shape: `·Tea ~b~ ·No.alt` absent other considerations).** The 2026-06-29 hold reason dissolved: the 4 ◊ZWNJ windows are definitionally word-initial under the ratified boundary rule and their rows are absorbed by `boundary-echo`, never adjudicated. |
| **D-2** | 2× ◊ZWNJ·Utter·May | — | — | — | **RETIRED BY RULE (2026-07-03):** both targets are ZWNJ windows — post-ZWNJ is definitionally word-initial and the rows ride `boundary-echo`; the old font's post-ZWNJ-only behavior is unrecoverable by design. No rune work remains. |
| **—** | 6 engine-limited | — | — | — | **DISSOLVED (2026-07-03):** the 4 ◊ZWNJ windows are retired by the boundary rule (above), and the 2 ·Oy·Tea·No windows are **restored byte-exact by Group C's acceptor record** — the "depth-2 wall" framing was an artifact of hunting for a `when:` predicate; making bare `qsTea` an acceptor on flipped ·No's baseline entry sidesteps it (the floor then chains exactly where the old font chained). Nothing is left to document as engine-limited residue from this phase. |

**Root causes:** A+C are the **qsNo chain-flip** (·No takes its baseline `flipped`/`alternate` stance to gain a forward join, lowering/breaking the predecessor join); B is the **·It·It·Utter withdrawal** (the second ·It, entered by the first, is vetoed before ·Utter by a carried-over before-·Utter refuse); D is the **·May tail**. The settle probe is **default-config only** — the commit gate is always the full per-config rebuild diff.

### Group C resolved (2026-07-03, committed `6ab6309`): ·Tea·No takes the baseline alt chain

The final landed shape is **one line**: `qsTea` added to flipped ·No's baseline-entry `from:` list in `qsNo.yaml` (code-point order, right after Q2's `qsPea`; the `qsTea_qsOy` ligature was already there). With the acceptor in place, the settlement floor and stance order make `·Tea ~b~ ·No.alt` the default wherever the chain can form — the user's stated preference, ratified the same day this landed ("absent other considerations, ·Tea·No should be `·Tea ~b~ ·No.alt` rather than `·Tea.half ~x~ ·No`"; see the recorded override above). The mirror of Q2's ·Pea decision.

The facts that dissolved the 2026-06-29 hold: (1) the ratified boundary rule makes the 4 ◊ZWNJ windows definitionally word-initial — the schema cannot address them, `boundary-echo` absorbs their rows, and post-ZWNJ ·Tea·No now equals bare ·Tea·No (which, chained, happens to match the old font's post-ZWNJ alt chain exactly); (2) Q2 already resolved the 7 word-initial ·Pea·No windows; (3) the "6 engine-limited" framing was an artifact — the ·Oy·Tea·No·May/·Utter windows restore **byte-exact** from the acceptor alone, no depth-2 predicate ever needed.

**The standing exceptions ("other considerations") the engine keeps:** before ·Day/·It/·Oy the pair stays `[y5, y5]` — `qsNo.prefer[1]` (exit x-height over baseline toward those followers) holds, ·No joins both sides at the x-height, and those windows remain **matched to the old font**; extending the chain to them would mean narrowing that record's `left: except` to include `qsTea` — a separate decision, not taken. And after an entered ·Tea (·Day·Tea·No and kin) the chain structurally cannot form (full ·Tea never pairs a baseline entry with a baseline exit), so those windows are unchanged too.

**Verification (full A/B scratch oracles, `tmp/t7-pre` = pre-change HEAD, 4,514 unmatched, vs `tmp/t1-post` = 5,477; gates re-run via `run_m1` on the real tree):** 17 rows leave the audit byte-exact (the two ·Oy·Tea·No windows × 7 configs, plus ·May·Tea·Tea·No under the 3 ss03-bearing configs); **980 rows / 152 unique windows enter as embraced divergence, every one containing a ·Tea·No adjacency** (perfect scoping — nothing else in the font moved): 690 rows previously matched the old font's y5 join and now draw the chain (bare ·Tea·No included — the deliberate look change), 290 complete already-divergent unverdicted `regrouping-floor-drift` windows into 3-letter chains, 289 of the total involve the namer dot (post-dot ·Tea·No chains like bare ·Tea·No; the dot does not split runs, so these need blessing like the rest). The 8 previously-approved y5 trailing-pair windows flip by design (the recorded override). Boundary gate 885,808 runs / 0 divergences; Manual pins 17/17 (no pin carries a ·Tea·No adjacency; the ·No-led pins like `·Day ~b~ ·No.alt ~b~ ·Tea` and `·It ~b~ ·No ~b~ ·Tea` are untouched and pass); `multi_matched` 0; defect gates clean. All 980 rows land in the phase-end review for family-level blessing, like Q2's ~1,850.

**The superseded interlude, kept for the record:** before the user stated the preference, a full old-font-parity shape (T8: the acceptor + four steering records — word-initial refuse/prefer on `qsTea`, word-final and before-·Pea entry-prefers on `qsNo`) was built, fully verified (63 rows restored byte-exact, +119 reshapes, 0 collateral), and briefly applied. It was replaced the same day by the one-line shape above. The candidate ladder survives in `tmp/cand-t{1,2,12,3,6,7,8}` with the mechanics documented: a `prefer` cannot beat the floor once the chain is live (T12), a refuse alone falls to the exitless-full order tiebreak (T6), and the steering-record kit in `tmp/cand-t8/` is the recipe if per-context ·Tea·No shaping is ever wanted back. Diff harness: `rebuild/tools/t7_audit_diff.py`.

## Phase 3 — the 2026-07-03 regrouping-floor-drift batch (IN PROGRESS)

The user recorded 32 new verdicts (`verdicts-12.04.13AM.json`) — **against the B+D-1 preview surface** (`tmp/review-preview-bd1`, manifest `2026-07-03T06:13:47Z`), not the served one. **ID caveat: that export re-stamps the old 4,828 verdicts onto the new manifest, but only 273 of those IDs mean the same window on both surfaces.** Rule: old verdicts resolve via `verdicts-11.33.00PM.json` + `rebuild/out/review`; the 32 new ones via the 12.04 file + the bd1 preview. Never join the merged file by ID. Harness for this batch: `rebuild/tools/regroup_diff.py` (targets = the new rejects/neithers with desired seams; guards = the batch's approves on bd1 plus every old latest-verdict window on the old surface, default config). **(2026-07-03, later) Both halves of that ID caveat are now mechanized:** `rebuild/tools/carry_verdicts.py` performs the content-keyed join for the blessing pass (its `verdicts-carried-forward.json` seeds the live surface with the 2,572 prior verdicts whose units are render-identical — the derived `explain` / `drafts` / `provenance` fields are excluded from the content key after the `flipped`→`alternate` settle-trace rename was found blocking 1,311 carries), and the review server now autosaves the in-tab store to `verdicts-autosave.json` after every mutation, refusing to restore across a manifest change — see WHATNEXT.md's 2026-07-03 "seatbelts" update and REVIEW-REPORT.md's "Triage flow".

- [x] **N1 — ·It·It before ·May/·No/·Utter (u-1318, u-1319; u-1317 keeps ·Day out).** Extended `qsIt` prefer[0]'s `then:` from `qsMay` to `[qsMay, qsNo, qsUtter]` — the round-1 "entered ·It declines so the follower keeps the join" record, per the user's note that these repeat standing opinions. APPLIED; settle-verified 2/2 restored, zero collateral both guard pools. Full rebuild pending.
- [x] **N4 — ss03 ·Utter·Tea yield (u-1327/1328/1330/1331; u-1329 keeps ·No out).** New `qsUtter` prefer: `{cell: {exit: none}, over: {exit: x-height}, when: {right: {family: qsTea, then: {family: [qsDay, qsMay, qsIt, qsUtter]}}}}`. APPLIED; settle-verified 4/4 restored under ss03, zero collateral. Full rebuild pending.
- [x] **Q1/N2 — SUPERSEDED by Q1★.** The ·It-steps-aside lever (`tmp/cand-n2b`) traded joins; Q1★ adds them instead. Not applied.
- [x] **Q1★/UXM2 — entered ·Utter reaches ·May at the x-height (the user's sidequest, 2026-07-03). APPLIED, user-approved ("a GREAT improvement"), with the stubless amendment.** The user's rule: use ·May's pulled-back x-height entry when ·Utter holds a baseline join from its predecessor and ·May isn't about to join its own follower at the x-height. Landed as: (1) `qsUtter` refuse[2] (mono x-height exit before ·May) narrowed with `self: {entry: none}`; (2) new `qsMay` prefer[0] `{cell: {exit: x-height}, over: {entry: x-height}, when: {left: {family: qsUtter}}}` for the condition-2 tie; (3) **the `pulled-back` bitmap deleted outright** — the x-height entry now binds `pulled-back-stubless` with `joined_x: 2` (user request: the baseline-tail stub is pointless when entered at the x-height), which also retires the dormant qsFee `contract` bind record; (4) the newly-exercised bare `qsMay.loop.en-y5` declined-exit terminal blessed in `m1-contact-allow.yaml`. Verified: u-1320/·No·It·Utter·May/·Utter·It·Utter·May → three joins; ·It·It·Utter·May gains ·Utter·May; ·Pea·Utter·May realizes u-1323's "even better" `[y0, y5]`; ·Pea·Utter·May·May → `[y0, y5, y0]`; bare ·Utter·May, ligature-fed ·May, the condition-2 approves (·It·Utter·May·Day/·No), and the ·May·May·May·May pin unchanged. Blast radius: 10 new-surface + 42 old-surface approves move — 50 pure gains, 2 Q3-shaped regroups (·No·Utter·May·Day, ·No·Utter·May·No) folded into the open Q3 question.
- [x] **Q2/N3 — the ·Pea·No.alt baseline chain (u-1280–1284, neither×5). APPLIED, user-approved ("excellent").** The broad lever (the grammar forbids `right.then` on refuses): qsNo dropped from `qsPea` refuse[0] + qsPea added to flipped-·No's baseline-entry `from:` list. Every ·Pea·No pair now starts an alt chain; the 5 wishes + 2 held Group-C ZWNJ windows restore to the old font's chain; ~17 old approves move (mid-word y5→y0 flips + 3-chain gains, all user-seen in the demo); adds ~1,850 rows of embraced new-vs-old divergence to bless at re-baseline.
- [x] **Q3 — resolved via Q1★ + two amendments (2026-07-03).** The ·Pea-yield lever (`tmp/cand-may`) was discarded. The user's refinement: ·Pea·Utter·May takes normal mono ·Utter + the x-height ·May reach (Q1★), _unless_ ·May has a higher-priority forward x-height join — and the ss03 ·May·Tea.half join is NOT higher-priority (it loses). Landed as `right: {except: [family: qsTea]}` on the condition-2 qsMay prefer, so under ss03 ·Pea·Utter·May·Tea → `[y0, y5, break]` (u-1337 resolved). **The ·May drawing question (Q3★): tuck-under won** — the user compared a back-reaching long-bar bitmap against the stubless pulled-back nesting under ·Utter in rendered fonts and chose the tuck for all windows; the invented `reaches-way-back` bitmap + contract bind were removed again. **Residue:** the follower-case rejects (·Pea·Utter·May·Pea/·Day/·No/·It, u-1335/1338/1340/1341) stay at `[y0, break, y5]` per the user's "unless" rule — the original wish for the old `[break, y0, y5]` grouping is superseded by the reformulated rule; the 2 ·No·Utter·May·X regroups ride the same rule.
- [x] **Q4/P1344 — ·Pea·Pea + ·Pea·Utter both join (u-1344, "the new way is broken"). APPLIED, user-approved ("looks great"), plus the ·May·Pea·Pea amendment.** qsUtter added to `qsPea` refuse[1]'s except list: ·Tea·Pea·Pea·Utter → `[break, y6, y0]`, 21 old approves gain the top join. The user's follow-up: in ·Pea·May·Pea·Pea the ·May-entered ·Pea should ALSO chain into the final ·Pea at y6 — landed by deleting `qsPea.half`'s `{entry: x-height, exit: y6}` never-pairing (the left dip comes from entry-stub arithmetic; the y6 exit bar already existed). ·Pea·May·Pea·Pea → `[y0, y5, y6]`; ·May·Pea·Pea / ·Utter·Pea·Pea gain the same y5→y6 chain; bare ·Pea·Pea and ·Pea·Pea·Pea unchanged. This supersedes the recorded u-1279 approve. Blessed: 3 contact signatures for the y6-entered ·Pea's baseline meeting with ·Utter (user-approved look), and 8 y7-corner siblings for the new en-y5.ex-y6 half-·Pea cells (the same corner long blessed on the bare ex-y6 cells).

- [x] **P-Indian — ·Day yields ·It to ·Utter, the Manual-pin gate's first catch (2026-07-03). APPLIED, Manual-mandated (site/the-manual.html:3828, "Indian" = ·It·No·Day·It·Utter·No).** New `qsDay` prefer `{cell: {exit: none}, over: {exit: baseline}, when: {right: {family: qsIt, then: {family: qsUtter}}}}` — the qsDay mirror of N1's ·It record: ·It cannot join at the baseline on both sides, so the floor-tiebreak ·Day·It join was foreclosing the Manual-mandated ·It·Utter join (the pin wants `·Day | ·It ~b~ ·Utter`; the engine gave `·Day ~b~ ·It | ·Utter`). Under ss04 the pass-through unlock outranks the yielding prefer, so ·Day·It·Utter keeps both joins there (probed: y0,y0). A/B scratch oracle diff (`tmp/pin-fix-pre` = HEAD runes vs `tmp/pin-fix-post`): 108 rows move `regrouping-floor-drift` → `bare-name-live-join`/`dangling-anchor-dropped` (their seams now match the old font — the restoration), zero rows regress, and exactly 6 rows — ·Day·It·Utter·May in each non-ss04/ss10 config — go `dangling-anchor-dropped` → UNMATCHED: the old font kept ·Day·It and reached ·May through the alternate ·Utter with ·It's exit dangling, while the fixed engine renders ·Day | ·It~b~·Utter~x~·May (the Manual's break composed with Q1★'s entered-·Utter reach); preserving the old shape would need a depth-3 right predicate the grammar forbids (`then` can't nest), and it leans on a dangling exit besides — the 6 rows go to the next review pass as intended divergence. Supersedes u-1278's "either" on ·Pea·Day·It·Utter (regrouping-floor-drift); the two deferred-ss04 rejects on ·Tea/·Oy·Day·It·Utter ("the new way is broken") stay parked with their bucket. No default-config approve contradicted the fix — the wrongly-joined windows were the unverdicted floor-drift stragglers.

Browser demo for Q1–Q4: `tmp/demo/index.html` + scratch-built fonts (`tmp/demo-build/{base,n2,n3,may,p1344}/M1.otf` via `rebuild/tools/scratch_build.py`), old font = `tmp/review-preview-bd1/fonts/before.otf`.

## Phase 4 — the blessing-pass verdict application (2026-07-03/04, IN PROGRESS)

**Source of truth for this phase:** the user's export `verdicts-06.50.46PM.json` (kept on their Desktop; 2,815 records on the live manifest `2026-07-03T23:31:04Z` = 2,568 carried-forward + **247 fresh blessing-pass verdicts**: 76 approve / 131 either / 30 reject / 10 neither). The autosave `verdicts-autosave.json` at the repo root matches it. Coverage when this phase started: 2,815 of the 4,010 human-queue units verdicted, 1,195 remaining, no pair-family fully adjudicated.

- [x] **DT1 — un-entered ·Day yields ·Tea to its own forward join. COMMITTED `658a3c3`.** The dominant fresh-reject cluster (22 of the 30 fresh rejects, all noted "the old way seems nicer to write out by hand"): the engine's structural floor greedily realized ·Day~b~·Tea in ·Day·Tea·X, and full ·Tea cannot pair a baseline entry with a baseline exit, so the greedy join foreclosed ·Tea~b~·X — where the old font grouped `·Day | ·Tea ~ ·X` (and joined ·Day·Tea only when ·Tea had no forward join; bare ·Day·Tea is `bare-name-live-join`). Landed as one `qsDay` yielding prefer (the sibling of P-Indian's): `{cell: {exit: none}, over: {exit: baseline}, when: {self: {entry: none}, right: {family: qsTea, then: {family: [qsDay, qsDay_qsUtter, qsMay, qsIt, qsUtter]}}}}`. Scope choices, each deliberate: **`self: {entry: none}`** keeps the fresh-approved entered-·Day chains (·Oy·Day·Tea·X, all 5 approved u-6601–6605) — an entered ·Day never yields; **qsNo absent from the peek list** keeps the fresh-approved greedy ·Day·Tea·No (u-6429); **qsDay_qsUtter listed** because the ligature is its own family at settle time (the E1 lesson; u-6446 ·Day·Tea·Day·(Day+Utter) restores through it). Verified by settle differ (`daytea_diff.py`, session scratchpad) — 22/22 in-scope rejects restored to the old font's exact seams in every config, 0 approve guards moved, broad-guard movement = fresh eithers only — then by full rebuild at HEAD: oracle 5,477 → **5,487** (+10 rows over exactly 2 windows), `multi_matched` 0, boundary gate 885,808 runs / 0 divergences, Manual pins 17/17. Restored rows: 116 → `dangling-anchor-dropped`, 21 → `bare-name-live-join`, ·Day·Tea·Day·Tea fully MATCHED (byte-exact); bonus restoration: ·Day·Tea·Utter·Tea at default/ss02/ss04/ss05 was floor-drift and is now `bare-name-live-join` (the old font yielded there too). The +10: **·Day·Tea·Utter·May** ×7 configs (was blessed `dangling-anchor-dropped`; the old font kept greedy because entered ·Utter couldn't reach ·May — with Q1★ the yield now renders `·Day | ·Tea~b~·Utter ~x~ ·May`, the Q1★ flagship shape, headed to `seam-loss-withdrawal` review) and **·Day·Tea·Utter·Tea** ×3 ss03-configs (the yield composes with the ss03 ·Utter·Tea x-height join into `·Day | ·Tea~b~·Utter ~x~ ·Tea.half`, headed to the `deferred-ss03` bucket). Preview surface: `tmp/review-preview-daytea` (15,971 units / 80,983 rows — one unit fewer than served: the fully-matched window left). Suites: `make test` 6,753 green; rebuild suite 401 passed / 13 failed = the 4 documented batch-1 spec pins + 9 review census pins tripped by the audit shift (15,972 → 15,971 units, 80,990 → 80,983 rows) that re-pin at the phase-end re-baseline, exactly the E3 pattern. The served surface at `rebuild/out/review` is untouched, so the in-flight blessing session and its autosave stay valid.
- [x] **DT2 — after ·Tea, an entered ·Day yields exactly like a bare one. COMMITTED `362122f` (user-picked from the dt2.html side-by-side).** The three fresh rejects DT1 deliberately left alone — u-6415 ·Tea·Day·Tea·It, u-6416 ·Tea·Day·Tea·Utter, u-6418 ·Tea·Day·It·Day, each an entered-from-·Tea ·Day chaining on where the old font split — are honored by two more `qsDay` yielding prefers scoped `left: {family: qsTea, joined_at: baseline}`: one mirroring DT1's before-·Tea record (same peek list, same ·No carve-out), one for the ·Day·It flavor with `right: {family: qsIt, then: {family: qsDay}}` (P-Indian's then-list can't just be widened to qsDay — that would flip the approved ·Oy·Day·It·Day chain). The left-·Tea scoping is the whole point: the fresh ·Oy-led approvals of structurally identical chains (u-6601–6605, u-6607) stay untouched, so the rule reads "three-in-a-row starting from a ·Tea bar is too much; the same chain after ·Oy is fine." Ride-alongs: the ·Tea·Day·Tea·Day / ·Tea·Day·Tea·May eithers split too; ·Tea·Day·Tea·No keeps its chain (·No carve-out). Verified: settle differ 3/3 targets to old seams in every config with only the 2 eithers as other movement; scratch-build A/B vs HEAD — **zero UNMATCHED movement, oracle unchanged at 5,487 / 80,983** (the six windows shuffle between blessed classes), `multi_matched` 0, defect gates clean; font-level shaping check confirms the six flips and four guards (after a first false "no diff" from a harness bug — the heredoc dropped the PUA chars; codepoint-built strings are the fix). Carried reject u-6398 ·Pea·Day·Tea·May (2026-06-29) stays unhonored: contradicted by the fresh ·Oy·Day·Tea·May approve, recorded here as superseded-by-newer-taste.
- [x] **FT1 — the ss03 full-·Tea x-height entry: an x-height exiter reaches the full bar when a follower with a baseline entry comes next. COMMITTED `a60d780` (2026-07-04, amended same day to drop the except list at the user's direction).** Honors the three fresh neither-asks with prescriptions (u-6645 ·Utter·Tea·Utter "`·Utter ~x~ ·Tea.!half ~b~ ·Utter`", u-6646/u-6647 the ·Day.half/·May flavors) plus the earlier ss03 full-·Tea rejects (u-6550/6551/6553 ·No·Utter·Tea·X, u-6642/6643 ·Utter·Tea·Day/·May) and neithers (u-6581/6582, either u-6584) — the WHATNEXT "ss03 full-size ·Tea when a baseline-joiner follows" gap, now built. **The landed shape is one unlock** on `qsTea.yaml`'s full stance: `{entry: x-height, feature: ss03, when: {left: {family: <the half unlock's broad set>, joined_at: x-height}, right: {class: can-enter-at-baseline}}}`, plus a third `full` ductus bullet (user-edited wording). **The right gate is deliberately bare** — the first cut carried `except: [qsPea, qsTea, qsNo, qsIt]` per WHATNEXT's over-trigger caveat, but a rendered side-by-side of both worlds (`tmp/except-demo/`, old vs with-except vs without, all four follower groups) led the user to drop the whole list: before a follower that cannot accept the bar's exit (·Pea — baseline entry from-scoped to qsEt/qsAwe; a second ·Tea — entry refused after qsTea; ·It — the two-verticals exit refuse) the entered full bar stands with its lower half dangling instead of the half stub, and **·X·Tea·No now chains into the alternate ·No under ss03 exactly as default does** (the Group-C acceptor), making ss03 consistent with default rather than preserving u-1329's half-stub shape. **N4 needed no edit**: once the unlock offers the 2-join cell, the settlement floor outvotes the yield tiebreak; for ·It followers after ·Utter the 1-join tie remains and N4 still yields, so ·Utter·Tea·It is byte-identical with or without the gate — the tea-it-xheight shapes are untouched. Verified by full A/B scratch oracles (`tmp/ss03tea-pre` 5,487 → `tmp/noexcept-build` 5,490) + `run_m1` at the applied tree: movement confined to the 3 ss03-bearing configs and identical across them (194 unique windows, every one carrying a ·Tea); 216 newly-divergent rows land in blessed classes (87 `ss03-chain-join-gains`, 57 `bare-name-live-join`, 72 `boundary-echo`), 366 rows reclassify (the verdict windows land in the gains class), 20 windows resolve out of UNMATCHED (the ·May·Tea·Tea family, ·No·May·Tea·X, ·Utter·Tea·No·Tea, ·May/·Utter·Tea·Utter·Tea kin) and 21 enter it for review (63 rows: the ·Pea/·Tea·Utter·Tea·X 4-chain families, the ·X·Tea·No·Y continuations, ·May·Tea·Utter·May composing with Q1★, and the ·X·Utter·Tea·Tea mono-vs-alternate regroups). Gates: boundary 885,808 runs / 0 divergences, Manual pins 17/17, defect gates clean — the no-except world needed no new contact blessings beyond FT1's 3 `qsTea.full.en-y5.ex-y0`+·Utter signatures — `multi_matched` 0, `make test` 6,753 green, rebuild suite at the documented 13 (4 spec pins + 9 census pins) after re-pinning `test_unlock_only_cells_resolve_with_their_record` (two ss03 unlocks on the full x-height cell) and the ledger exemplar count (21). The `ss03-chain-join-gains` ledger entry carries the third exemplar, the no-except rationale, and the refreshed count (120 → 696 measured). **Superseded verdicts, recorded here at rule level (all seen rendered before the call):** ~48 approvals sat on flipped windows — the u-1329 lineage (u-6406 ·Pea·Utter·Tea·No and kin) that pinned the half-stub-and-break before ·No, the alternate-·Utter·Tea·Tea baseline-chain approvals (·May·Tea·Tea family, ·X·Utter·Tea·Tea), the 2-join shapes that now gain the chain join (·No·May·Tea·X, ·Pea·Utter·Tea·X, ·May·Tea·Utter·X, ·Utter·Tea·No·Day/·No), and 4 fresh 2026-07-04 approvals (u-6633/6634/6636 ·Oy·Utter·Tea·X, u-6549 ·No·Utter·Tea·Tea) recorded before the user formed the prescription. The ·Pea·May·Tea·X carried rejects ("worse-looking extension") are E2's composed-extension complaint — untouched (the windows gain the chain but keep the extension; the complaint stays parked with E2). **Pre-existing condition surfaced, not caused here:** `conform_summary` reports 2 settlement rules per ss03-bearing config never exercised by the sweep (uncovered_rules 6, pass false) — an identical 2 exist at the pre-change tree (`tmp/conform-pre`), so this predates FT1; the suspects are qsMay-backtrack→qsUtter rules. **Ductus wording note:** the user's bullet says the bar appears "when … the next letter joins at the baseline"; with the bare gate the bar also appears before a capability-class follower that ends up not joining (·Pea/·It/second-·Tea) — the bullet may want "when the next letter can join at the baseline" at some point. Preview surface: `tmp/review-preview-ss03tea`; demo: `tmp/except-demo/` (port 7296); the served surface and the in-flight blessing autosave are untouched.
- [x] **IT1 — ·It never joins to itself. COMMITTED `4589d20` (2026-07-04, user-ratified off the rendered side-by-side).** The user's ruling mid-blessing: ban the ·It·It self-join everywhere, at both heights, in every config. The landed shape is one refuse on `qsIt.yaml` — `{entry: baseline, when: {left: {family: qsIt}}}`, the exact mirror of the long-standing x-height refuse, so ·It accepts no entry after ·It at either height — plus the removal of the now-dead prefer (exit-none over baseline before ·It-then-May/No/Utter), proven dead weight by a second scratch build whose audit came back byte-identical without it. Verified by scratch A/B (`tmp/itit-runes` → `tmp/itit-post` vs the HEAD oracle at 5,490): **695 rows change in place, zero rows enter or leave the audit, and every moved row contains an ·It·It adjacency** — perfect scoping. The reviewed-approved `entered-it-baseline-join-gain` class empties 492 → 0: 485 rows re-land in `dangling-anchor-dropped` (the old font draws the first ·It's now-unjoined exit nub, M1 drops it per the blessed pattern), 7 in `pea-chain-regularized`; the ledger entry stays at count 0 with a why postscript, and its 33 round-1 approvals are **superseded at rule level**. UNMATCHED 5,490 → 5,496: the +6 are the ss04 ·X·It·It·Utter windows (X ∈ Pea/Tea/Day/May/No/Utter) — the one corner where the old font really chains ·It·It (the baseline pass-through into ·Utter; confirmed by direct old-font shaping) — structurally impossible to carve out because refuse records cannot be feature-scoped, so they go to review as intended divergence. `multi_matched` 0, defect gates clean. Renders: `tmp/itit-demo/index.html` (old vs current vs candidate). Note for the census re-baseline that follows: the machine-approved histogram shifts (~485 rows join `dangling-anchor-dropped`) and the 6 new windows join a verdict family.
- [x] **UM1 — ·Utter reaches a pulled-back-grounded ·May when another ·May follows. LANDED (2026-07-04, user-prescribed from u-6648 ·Utter·Utter·May·May on the live surface and ratified off the rendered demo).** No new stance and no new bitmap: `grounded-loop` already carried `pulled-back-grounded` with the right pairing (x-height entry from the ·Utter set, baseline exit, ·May absent from the grounded-exit refuse). The landed shape is one record swap in `qsUtter.yaml` — the "unentered mono ·Utter refuses its x-height exit before ·May" veto becomes a **yields-to-joins prefer** for the baseline exit (`cell: {exit: baseline} over {exit: x-height}, when: {self: {entry: none}, right: {family: qsMay}}`) — so before a lone ·May the 1-join tie still goes to the old alternate baseline join, and before ·May·May the x-height reach buys a second join and the floor takes it: `·Utter ~x~ ·May.pulled-back-grounded ~b~ ·May`. qsMay needed no edit (its exit-over-entry prefer also yields to joins). Verified by scratch A/B (`tmp/umm-runes` → `tmp/umm-post` vs the post-IT1 oracle at 5,496): **112 rows change in place over 16 windows, every one containing the ·Utter·May·May substring, identical across the 7 non-ss10 configs, zero rows entering or leaving the audit**; 84 rows (the 12 letter-flanked windows × 7 configs) leave `regrouping-floor-drift` for UNMATCHED as intended new-join divergence awaiting blessing, 28 reshape inside `boundary-echo`; `multi_matched` 0, defect gates clean. Guarded contexts proven unchanged by probe: bare ·Utter·May (alternate join stands), ·Utter·May·Tea, and the entered ·It·Utter·May reach. CoreText shaping verified identical to HarfBuzz on the three key windows. **Accepted tie, recorded here:** ·Utter·May·May·May stays right-grouped — the reach lands on the first ·May (stubless, chain-breaking) and the trailing pair keeps its grounded chain, leaving the second ·May's stub dangling; the user saw the strips and accepted, and a left-grouping prefer remains available if that ever grates. **Methodology note:** the demo page (`tmp/umm-demo/`) now carries font-free ground-truth strips — inline SVG composed from the compiled outlines and shaper positions — after a stale cached webfont on the reused port 7296 made the correct candidate look wrong; hard-reload plus the strips is the check.

- [x] **DT3 — before ·Tea·May, every ·Day yields, entered or not. LANDED (2026-07-04, user-prescribed from echo group e-0046 `#units=u-6084,u-6120,u-6163,u-6176,u-6195,u-6221,u-6271` on the live surface: "I'd rather have the ·Tea join with ·May like it used to").** The entered-·Day cases DT1's `self: {entry: none}` guard deliberately kept greedy — the seven ·X·Day·Tea·May windows (X ∈ Pea/Day/May/No/It/Oy/Utter) — now yield too, restoring the old font's `·X ~b~ ·Day | ·Tea ~b~ ·May` with ·May's `en-ext-1` extension. This supersedes the fresh ·Oy·Day·Tea·May approve (u-6221, the very verdict DT2 cited to keep u-6398's ·Pea·Day·Tea·May reject unhonored) plus the four ride-along eithers — the echo-group view put all seven renderings of the identical change on one page and the user reversed the whole set at once. The landed shape: one new `qsDay` yielding prefer with no self or left guard, `{cell: {exit: none}, over: {exit: baseline}, when: {right: {family: qsTea, then: {family: qsMay}}}}`, and `qsMay` pruned from DT1's and DT2's peek lists as now-subsumed (proven byte-equivalent: the candidate audit is byte-identical to one keeping the lists). Verified by scratch A/B (`tmp/dt3-runes` → `tmp/dt3-post` vs the HEAD oracle at 5,580): **49 rows change in place = exactly the 7 windows × 7 non-ss10 configs, every one carrying the ·Day·Tea·May substring, zero rows entering or leaving the audit, all 49 moving `regrouping-floor-drift` → `dangling-anchor-dropped`**; ink-comparator check confirms all 7 windows render pixel-identical to the old font under every config, so they left the human queue as machine-approved units (3,948 → 3,941; the fresh echo audit's conflict list drops 28 → 27 with e-0046 discharged). Gates at the applied tree: oracle unchanged 5,580 / `multi_matched` 0, boundary 110,726 sequences / 0 divergences, Manual pins 17/17, defect gates clean (`uncovered_rules` 6 = the documented pre-FT1 ss03 condition). Surface rebuilt in place (census pins re-baselined: machine 9,581, dangling-anchor-dropped 3,951 units / 3,935 machine, secondary seams 1,608/380/1,471/336, echo groups 1,709; name-grain ink census 10,180/5,887/4,012); verdicts carried `tmp/review-pre-dt3` × `verdicts-12.45.26AM.json` → **`verdicts-carried-dt3.json`** (2,603 of 2,608 — the 5 shortfall is exactly the discharged e-0046 verdicts). `make test` 6,753 green, rebuild suite at the 4 documented spec pins, JS suite 89.

- [x] **ME1 — ·May's baseline-entry extension dropped after ·Tea and after ·It. COMMITTED `a2c3334` (with the artifact cycle: DT3 in `10d71ff`, the echo-group/ink-merge surface work in `3a0dd5f`/`0aea656`, the census re-pin in `eb85c83`). Originally landed 2026-07-04, user-prescribed live during the blessing pass: "·Tea ~b~ ·May should have one fewer pixel of extension", then "also remove qsIt from that list, too".** The landed shape is a two-token deletion in `qsMay.yaml`: `qsTea` and `qsIt` leave the left-family list of the loop stance's baseline-entry extend record (`[qsPea, qsTea, qsTea_qsOy, qsYe, qsHe, qsIt]` → `[qsPea, qsTea_qsOy, qsYe, qsHe]`), so every baseline ·Tea·May and ·It·May join now takes ·May's bare `en-y0` entry — the joins stand, one pixel shorter, in every config and every context (probe-confirmed for bare pairs, ·Day·Tea·May, ·Oy·Tea/·It·May, the ss03 FT1 full-bar chains, and the Manual-pinned ·It·May chains). ·Pea·May and ·Tea+Oy·May keep `en-ext-1` (as do the unmigrated ·Ye/·He when they arrive), and ·May's separate exit-side `ex-ext-1` toward a following ·It/·Day/·Roe/… is untouched. Verified at the applied tree by `run_m1` + a non-destructive surface preview (`tmp/review-preview-may-ext`, 15,351 units / 81,213 rows): oracle 5,580 → **7,882** (+1,505 from the qsTea removal measured alone, +797 more from qsIt), `multi_matched` 0, boundary gate 885,808 runs / 0 divergences, Manual pins 17/17. Per-config UNMATCHED diff vs the served surface: +272 in each of default/ss02/ss04/ss05, +221 in the three ss03-bearing configs (the FT1 chains reshuffle some windows), 0 under ss10; **389 windows moved in all, every one containing a ·Tea·May or ·It·May adjacency — zero off-scope collateral.** Transition census: 1,924 window-configs `dangling-anchor-dropped` → UNMATCHED and 364 `bare-name-live-join` → UNMATCHED (the embraced look change, headed to review — the bare pairs land in `extension-non-summing`, family partition proven total at 1,545 windows); 14 previously byte-matched window-configs enter UNMATCHED (the bare pairs plus DT3's seven ·X·Day·Tea·May windows, whose pixel-identical restoration this supersedes by exactly the deleted pixel — they return to the human queue); and 143 window-configs **leave** `halves-entry-extension-restored` for benign classes (127 → `dangling-anchor-dropped`, 16 → `bare-name-live-join`; ledger count 805 → 662) — the windows where the old font never extended, which the runes' extension had been diverging from. That last bucket dissolves the ·It share of the round-2 E2 wall: the "it-may" rejects wanted the extension gone but needed an inexpressible depth-2 predicate to scope the drop — dropping it _everywhere_ needs no predicate at all; the ·Pea/ss03 share of E2 stays parked. **Manual-pin flag for the qsZoo migration:** `·Tea ~b~ ·May.en-ext-1 ~x~ ·Zoo` (site/the-manual.html:2376, the word "times") explicitly pins the extended ·May after ·Tea; it is out of gate scope today (·Zoo unmigrated) but will trip the no-waiver gate when qsZoo migrates and must be re-transcribed then (`·He.!half ~b~ ·May.en-ext-1` is unaffected — ·He stays in the list). Suites: `make test` 6,753 green; rebuild suite 404 passed / 15 failed = the 4 documented spec pins + 11 census/artifact pins tripped by the audit shift (partition total, only the pinned sums stale) — they re-pin at the commit-time artifact cycle. **The served surface, the in-flight blessing session, and its autosave are untouched**; the in-place surface rebuild, census re-pin, and verdict carry are deliberately deferred to the commit so the live verdicting session isn't disrupted mid-pass.

## Phase 5 — the everything-verdicted export (2026-07-04, COMMITTED `26d2927..f7cfae0`)

**Source of truth for this phase:** `verdicts-01.46.12AM.json` — the user's COMPLETE blessing pass ("I've just verdicted everything available to me"): 3,941 unique-unit verdicts on the live manifest `2026-07-04T07:57:50Z` (3,498 approve / 192 neither / 158 reject / 87 either / 6 identical; every verdicted unit resolves on the served surface). The fresh design asks triaged out of the reject/neither notes, then applied one logical change at a time, each scratch-A/B-verified (`rebuild/tools/scratch_build.py` + audit diff keyed by (config, codepoints)) and then gated at the applied tree with `run_m1` (boundary, Manual pins, conform, defects, `multi_matched` 0 throughout). Oracle trail: 7,882 → 8,272 (IU1) → 8,260 (PIT1+DID1) → 8,260 (DTIU1, class moves only) → 8,260 (INO1, class moves only) → 8,248 (NALT-TEA) → 8,353 (PPN1). The served review surface, its verdict export, and the census pins are untouched — the surface rebuild + re-pin + verdict carry are deferred to the commit-time artifact cycle, per the ME1 precedent.

- [x] **IU1 — the ·It·Utter extension pixel dropped (15 fresh notes: "the join is a good thing but ·It·Utter shouldn't have that extra pixel of extended join between them", "I'm gonna stop mentioning the extra pixel…").** One token: `qsUtter` joins `qsDay`/`qsDay_qsUtter` in the `qsIt` entered-exit extension's `right.except` (`extend[1]`), plus the 3 `qsIt.bar.en-y5.ex-y0`+`qsUtter.mono.en-y0*:y1` contact twins blessed in `m1-contact-allow.yaml` (the x-height-entered siblings of the long-blessed bare corner — the unextended junction touches ·Utter's foot at y1 exactly as the bare one always has). A/B: 774 rows moved, every one an ·It·Utter adjacency, 0 off-scope; +390 UNMATCHED = the embraced look change (the old font extends the entered ·It before ·Utter; under ss04 the change actually _restores_ old parity — the old `en-y0.ex-y0.before-utter` chain is unextended).
- [x] **IU2 — ·May joins the IU1 except list: the ·It·May join gets closer by one more pixel (2026-07-04, off the vetting page: "actually, I do want ·It·May to get closer by another pixel").** ME1 already removed ·May's entry-side extension after ·It; this drops the other pixel — the entered ·It's exit extension. One token: `qsMay` joins `qsDay`/`qsDay_qsUtter`/`qsUtter` in the `qsIt` entered-exit extension's `right.except` (`extend[1]`), in code-point order. No contact blessings needed (`defect_errors` empty — ·May's loop entry leaves no y1 body adjacency at the unextended corner, unlike ·Utter's foot). A/B (`tmp/iu2-runes` → `tmp/iu2-post`, then the applied tree byte-identical to the scratch audit): 139 rows / 23 windows moved, every one an E670:E665 adjacency, 0 off-scope; all → UNMATCHED (110 from `dangling-anchor-dropped`, 20 from `bare-name-live-join`, 9 from byte-MATCHED) = the embraced look change, oracle 8,284 → **8,423**. Probes: ·Tea/·No/·Pea·It·May lose `ex-ext-1` on the ·It cell in every config; bare ·It·May and ·Oy·It·May identical (the baseline-entered ·It exits x-height — the record never applied). Gates: boundary 885,808 runs / 0 divergences, Manual pins 17/17, `multi_matched` 0. Reflow side-note: `tools/reflow_yaml.py` also collapsed the DID1 refuse record to flow style (format-only; the previous session had left it un-reflowed).
- [x] **PIT1 + DID1 — ·Day always yields ·It to its forward ·Day/(Day+Utter) join, and the two-verticals rule now covers the ligature.** Honors the 19 e-0050 rejects ("the old way seems nicer to write out by hand", every left including the reversed ·Oy-led approval — u-6220 overturns the 2026-07-03 u-6607-family approvals at rule level) plus the two adjacency rejects u-14621/u-15136 ("·It·Day must never be right next to each other — they just look like an extra-thick line"). Landed as: `qsDay` prefer[0] (P-Indian) `then:` widened to `[qsDay, qsDay_qsUtter, qsUtter]`, the DT2b record (`left: qsTea` × `then: qsDay`) deleted as subsumed, and **two more family-vs-ligature traps fixed in `qsIt`** — refuse[2] (the x-height two-verticals refuse) gains `qsDay_qsUtter`, and refuse[7]'s `right.except` gains `qsDay_qsUtter` (without which unentered ·It after ·Day cannot reach the ligature at all; found via `settle_traces` — the exit-none candidate ranked prospect 0 and the yield stage was never reached). A/B: 139 rows, 0 off-scope: 96 `regrouping-floor-drift` → `dangling-anchor-dropped` (e-0050 restored), 12 UNMATCHED → `dangling-anchor-dropped` (both ·X·It·(Day+Utter) windows), 6 rows byte-MATCHED (·Day·It·Day·Tea), 24 boundary-echo reshapes, 1 ss04 UNMATCHED reshape (closer to old; the pass-through unlock doesn't cover the ligature, stays for review).
- [x] **DTIU1 — after ·It and ·Utter, an entered ·Day yields ·Tea exactly as it does after ·Tea.** Honors u-6190/u-6264/u-6267 (·It·Day·Tea·Day, ·Utter·Day·Tea·Day, ·Utter·Day·Tea·Utter). One new `qsDay` yielding prefer: `left: {family: [qsIt, qsUtter]}`, `right: {family: qsTea, then: {family: [qsDay, qsDay_qsUtter, qsUtter]}}` — **`qsIt` deliberately absent from the peek list** to protect the fresh ·Utter·Day·Tea·It approval (u-6266); the ·Oy-led ·Day·Tea chains stay greedy per their fresh approvals (u-6215–6218). A/B: 28 rows, exactly the {·It,·Utter}·Day·Tea·{·Day,·Utter} combos, 21 → `dangling-anchor-dropped`, 7 → `bare-name-live-join`, oracle unchanged.
- [x] **INO1 — ·Day·It·No·Tea regains the old ·It~y5~·No grouping (u-6153 reject, e-0088).** Two records: `qsNo` prefer[0] (the absolute flipped-after-·It stance prefer) narrowed with `joined_at: baseline` — it no longer forces flipped when the entered ·It cannot actually reach it (the predicate binds to the candidate's seam per design 5.9), so the tie falls to stance order and the loop's x-height join survives; plus a `qsIt` yielding prefer (`{cell: {exit: none}, over: {exit: x-height}, when: {left: {family: qsOy, joined_at: baseline}, right: {family: qsNo}}}`) preserving the fresh ·Oy·It·No·X approvals (u-6242/6243/6244) — **proven load-bearing by a deletion build (+21 UNMATCHED without it)**. A/B: 18 rows = ·Day·It·No·{Tea,May,Utter} × 6 non-ss04/ss10 configs, all → `bare-name-live-join` (the old grouping, name-grain only). The u-11139-style ss04 pass-through chains and every word-initial ·It·No baseline join stay put.
- [x] **NALT-TEA — word-initially before ·Utter, ·No takes the join over the break (u-14979: "this should be ·No.alt so it can join to the ·Utter after it").** One `qsNo` yielding prefer: `{cell: {exit: baseline}, over: {exit: none}, when: {left: {is: boundary}, right: {family: qsUtter}}}`. The tie it breaks exists only when the follower after ·Utter is ·Tea in a non-ss03 config (everywhere else the counts already decide), so it is inherently narrow. Verified via subagent: 56 rows, all word-initial ·No·Utter·Tea·X, 32 improving class (20 `regrouping-floor-drift` → `dangling-anchor-dropped`, 12 UNMATCHED → `dangling-anchor-dropped`), oracle 8,260 → 8,248, zero mid-word movement (probe-confirmed on ·May·No·Utter·Tea and ·Utter·No·Utter).
- [x] **PPN1 — the y6-entered second ·Pea chains into the flipped ·No (u-6080/6081/6082: "second pea should join to the ·No.alt" — the WHATNEXT parked ask, reaffirmed).** One token: `qsNo` joins `qsPea` refuse[1]'s `right.except` list (the Q4 `qsUtter` pattern; the record's why-count "five" → "six"). Verified via subagent: oracle 8,248 → 8,353 (+105 = the embraced chain extension over 15 windows — the 12 `pea-chain-regularized` ·X·Pea·Pea·No windows flip from the old y5-loop join to the baseline alt chain and go to review with the 3 prescribed windows, now `[y6, y0, y0]`); bare ·Pea·No windows and ·Pea·Pea·Pea/·Pea·Pea·Utter probe-identical; no contact blessings needed.
- [x] **SS10-LIG — ss10 join suppression also means ligation suppression (~160 fresh notes across ss10-isolation-completed, deferred-ss10, and the kern-channel ·Tea·Oy note). APPLIED (engine change).** Under ss10 a ligature-forming pair now renders as the two components' bare cmap glyphs: formation stays unconditional (stage order preserved), and the split happens post-settlement in a new type-2 lookup `m1_ss10_unligate` inside `feature ss10` (feaLib cannot mix substitution types in one lookup) covering every reachable ligature-derived glyph — settled cells, locked cells, the bare formation output, marker twins, and the `.noentry` chokepoint twins (24 rules). In lockstep: `conform.isolated_overlay_names` + `_compare_row` expand ligature-rune cells to per-component bare cells; a new **`ss10-ligature-suppressed`** ledger class (520 rows; ligation-grain kinds, routed ahead of `marker-staging-ligature-formation`) with its `m1-divergences.yaml` entry citing the ratification; `rebuild/review/enrich.py`/`drafts.py` gain the matching overlay expansion and the fused-cluster normalization (a GSUB multiple substitution cannot un-merge the formation's cluster — selection under ss10 treats the pair as one cluster, inherent to form-then-split); two CoreText smoke pins added (`E653 E67A`, `E650 E653 E67A E652` — 464/464 runs agree, GID- and position-identical on both shapers). Verified: HarfBuzz shows `['qsDay','qsUtter']` / `['qsTea','qsOy']` under ss10 (ligatures intact under default; no bare component carries a curs GPOS entry); the full conformance sweep ran fresh — 1,417,240 shaping runs, **0 divergences, 0 uncovered transitions**; oracle 8,353 → **8,284** (all 69 prior UNMATCHED ss10 pair rows resolved; +520 `ss10-ligature-suppressed`, `ss10-isolation-completed` 1,865 → 1,722, 22 post-ZWNJ rows _restored_ to byte-match — the old font never formed the pair there either); boundary gate 0, pins 17/17, `multi_matched` 0; zero non-ss10 rows moved. Suites: `make test` 6,753 green; rebuild suite failure set test-for-test identical to the session baseline (4 spec pins + 11 census/artifact pins) after +6 new passing tests and the 21 → 23 exemplar re-pin. **Known-state note: `conform_summary.json` now reads `uncovered_rules` 26 (was 6).** The sweep had not run fresh since 2026-07-03 — plain run_m1 does not run it — so 26 is the first measurement of the whole Phase-5 tree, not an SS10-LIG effect. The +20 are identified: the ·May×2 and ·Utter×2 settlement rules whose backtrack is the bare `qsNo.loop` cell, per non-ss03 config ×5 — contexts the session's ·No-side prefers (NALT-TEA and kin) made unreachable inside 5-token windows. Dead emitted rules, not behavior (0 divergences proves font==model everywhere reachable); the same documented-condition treatment as FT1's 2-per-ss03-config, and an emitter dead-rule-pruning cleanup candidate.
- [x] **SS10-FORM — the ligatures never form under ss10 at all (2026-07-04, off the vetting page: "the ligatures should not form under ss10 at all — even if there's just the two letters"). Supersedes SS10-LIG's form-then-split mechanism; the rendering was already right, but the pair still shaped as one fused cluster (`['qsDay','qsUtter']` clusters `[0, 0]`).** The new mechanism pre-empts the twins instead of splitting after the fact: `mint_raw_glyphs` mints an anchor-free `.ss10` twin per codepoint-bearing letter rune (same drawing, same advance, no curs anchors, no cmap; ligature runes and boundary tokens get none), and `emit_gsub` emits `lookup m1_ss10_isolated_input` (`sub qsDay by qsDay.ss10;` …) **defined before `m1_formation`** so its lower LookupList index makes it apply first on both shapers — the twins appear in no formation sequence, marker line, chokepoint class, or settlement input, so under ss10 no ligature ever exists in the buffer, nothing settles, and each letter keeps its own cluster (`[0, 1]`, `[0, 1, 2, 3]`). Both `m1_ss10_unligate` and the cell→isolated `m1_ss10_isolated` singles overlay became unreachable in every buffer state (no cell and no ligature-derived glyph can appear in an ss10 buffer) and were **removed**, along with the `isolated_cells` emit parameter (now `ss10_twins`). The namer dot keeps its lowering under ss10 via the Short twins joining `@m1_namer_short_followers`. In lockstep: `conform.isolated_overlay_names` expects the twin names (`model.ss10_twin_name` / `SS10_TWIN_SUFFIX` is the shared contract), and `drafts.validate_semantics` replaces SS10-LIG's fused-cluster re-clustering with a twin-suffix strip (clusters are per-letter natively now; the anchorless twins classify every gap as a break). Verified: the oracle audit is **byte-identical** pre/post (the audit is model-grain; zero rows moved anywhere, `multi_matched` 0, oracle 8,423), boundary gate 885,808 runs / 0 divergences, Manual pins 17/17, defect gates clean, the full conformance sweep fresh at 1,417,240 runs / **0 divergences / 0 uncovered transitions** (`uncovered_rules` 26 = the documented Phase-5 dead-rule condition, unchanged), CoreText smoke 464/464 (the ss10 pins now GID-match the twins on both shapers), no twin in cmap or in any curs coverage, ligatures byte-identical under default. Suites: rebuild 411 passed / the same 15 documented failures (4 spec pins + 11 census/artifact pins, test-for-test), `make test` 6,753 green. Tests re-pinned to the new shape: `test_ss10_preempt_defined_before_formation` + `test_ss10_twins_stay_out_of_the_join_pipeline` replace the two overlay-shape pins in `test_emit.py`; `test_isolated_overlay_names_render_ss10_twins` added in `test_conform.py`.
- **SS03-MAYTEA — RESOLVED (2026-07-04, user call: the full bar everywhere).** Presented with the contradiction below, the user chose the full bar without needing the rendered side-by-side: one ungated `{entry: x-height, feature: ss03, when: {left: {family: qsMay, joined_at: x-height}}}` unlock on full ·Tea (qsMay leaves the right-gated broad row), so every x-height-joined ·May·Tea takes the full bar under ss03, word-final included, superseding the ~55 half-stub approvals at rule level. Verified by scratch A/B + `run_m1`: 591 rows moved, every one an ss03-config ·May·Tea adjacency, zero off-scope; +207 newly divergent rows all absorbed by ledger classes (132 `boundary-echo`, 75 `bare-name-live-join` — same y5 seam, the bar is name/ink grain), oracle unchanged at 8,423, `multi_matched` 0, boundary/pins green; the Q3-approved ·Pea·Utter·May·Tea break and the ·May·Tea·Day chain untouched, default config unaffected. Ductus note: qsTea's third `full` bullet still reads "when the next letter joins ·Tea at the baseline" — after ·May the bar now also appears word-finally, so the wording is the user's to refresh if it grates. The original contradiction, kept for the record: The fresh rejects u-6431/6432/6433/6443 prescribe "under ss03, this should be `·May ~x~ ·Tea.!half`" (the full bar word-finally after ·Utter·May), but fresh same-day approvals sit on the identical local shape: u-6412 ·Utter·Tea·May·Tea and u-6357 ·May·Utter·May·Tea approved ~80 s _before_ the rejects, and u-15255 ·Utter·May·May·Tea approved 20 min _after_, plus ~27 carried approvals (·Tea·May·Tea, ·May·May·Tea, ·It·May·Tea, ·No·May·Tea…) all word-final half after baseline-entered ·May. The only expressible lever (a bare `{left: {family: qsMay, joined_at: x-height}}` ss03 unlock on full ·Tea with no right gate) flips every one of those ~55 approved windows; scoping to "·May entered from ·Utter" is the round-2 depth-2 left-context wall. Present both worlds rendered side-by-side and let the user pick.
- **UALT1 — PARKED for a fresh session (user call, 2026-07-04, reaffirmed when prompted post-commit).** Reaffirmed twice this pass (u-15341/15342/15344 "the first ·Utter should be able to connect to the ·Utter.alt if the ·Utter.alt reaches backwards for it"; u-6185–6189 "we should have an ·Utter.alt that can connect to the previous letter at the x-height"). The alternate ·Utter must grow a real x-height entry surface (today its x-height entry is from-scoped to ·May/·Fee via the reaches-way-back bitmap) — a drawing question before a policy one.

**Update (2026-07-04, the batch is COMMITTED and its artifact cycle is done).** The nine Phase-5 commits landed as `26d2927..f7cfae0` (IU1+IU2, PIT1+DID1, DTIU1, INO1, NALT-TEA, PPN1, SS10-FORM, plus the blessing-pass note and the rune-file `why:` cleanup), and the established commit-time cycle ran at HEAD `f7cfae0`: `run_m1` recompiled M1.otf (oracle **8,423** unmatched / `multi_matched` 0, boundary gate 885,808 runs / 0 divergences, Manual pins 17/17), and the review surface rebuilt in place into `rebuild/out/review` (stale shards cleared first, per the orphaned-shard wart) — **15,828 units / 81,660 rows / 16 batches / 2,045 echo groups** on manifest `2026-07-04T19:54:22Z` (from 15,349 / 81,199 / 14 / 1,709 on `2026-07-04T07:57:50Z`), 8,980 machine-approved / 4,628 human units, secondary seams 1,901 marked / 536 homed / 1,633 homeless / 381 suppressed, and the verdict families summing **1,579 windows**: `extension-non-summing` 529 (it absorbed the ME1/IU2 shortened-extension pairs — this cycle is the first surface realizing ME1, whose refresh was deliberately deferred), `seam-loss-withdrawal` 370, `deferred-ss04` 223, `unmatched-misc` 128, `no-chain-gains` 108, `deferred-ss03` 90, `may-utter-gains` 82, `tea-it-xheight` 33, `oy-it-baseline` 16, and `deferred-ss10` now **empty** — SS10-FORM resolved every ss10 window, and the `ss10-ligature-suppressed` ledger class replaced that family in the manifest's 22. The final export was carried onto the fresh surface (`rebuild/tools/carry_verdicts.py --source tmp/review-pre-final verdicts-01.46.12AM.json` → **`verdicts-carried-final.json`**, gitignored by design): **3,373 of 3,941 carried** (3,188 approve / 97 reject / 78 either / 8 neither / 2 identical, 11 via the merged-unit ink fallback), dropping the human queue **4,628 → 1,255** unverdicted — import it into the served surface to resume. The 568 uncarried verdicts sit exactly on the windows this cycle's changes moved (the ·It·Utter / ·It·May / ·Tea·May extension drops, the ·Day·Tea and ·Day·It regroups, ·It·No, word-initial ·No·Utter·Tea, the ·Pea·Pea·No chains, and the ss10 ligature windows — zero off-scope drops). Census pins re-baselined across `test_review_{audit,ink,families,build}` (audit 81,660 rows / 16,515 name-grain units; name-grain ink census 9,576 machine / 4,702 human; the built counts above), and the drafts contract-draft worked example re-pointed at ·Tea·Day·(Day+Utter)·May (`E652:E653:E67A:E665`) — its old exemplar ·Oy·It·May left `halves-entry-extension-restored` when ME1/IU2 dropped the ·It·May extension. Gates: rebuild suite 422 passed / only the 4 documented batch-1 spec pins failing, JS suite 89 passed, `make test` 6,753 green.

**Update (2026-07-04, the SS03-MAYTEA follow-up cycle).** The one-commit artifact cycle ran for `eeeca1e` (the full ·Tea bar after an x-height-joined ·May under ss03): the review surface rebuilt in place onto manifest `2026-07-04T20:22:20Z` — **15,897 units / 81,867 rows / 16 batches / 2,062 echo groups**, 8,929 machine-approved / 4,682 human units, secondary seams 2,000 marked / 556 homed / 1,721 homeless / 373 suppressed — with the movement confined to `bare-name-live-join` (+25 windows) and `boundary-echo` (+44), exactly the commit's 207 newly divergent rows; the verdict families are unchanged window-for-window. The carry re-ran onto the fresh surface (`--source tmp/review-pre-maytea verdicts-carried-final.json` → **`verdicts-carried-final2.json`**): **3,345 of 3,373 carried**, the 28 dropped all ss03-config ·May·Tea-adjacency windows (23 whole windows plus 5 ss03-config halves whose default-config siblings carried), zero off-scope — import it to resume with **1,337 units** left to verdict. Census pins re-baselined across `test_review_{audit,ink,enrich,build}` plus the qsTea unlock-list pin in `test_surface.py` (the commit's third ss03 unlock on full ·Tea); gates: rebuild suite 422 passed / only the 4 documented spec pins, JS suite 89 passed, `make test` 6,753 green.

### Remaining Phase-2 piles (after seam-loss-withdrawal)

Mostly-neither: `regrouping-floor-drift` (24n), `deferred-ss04` (44r but ssNN-parked). Approve-heavy (likely confirm-only no-ops): `no-chain-gains` 160, `kern-channel-out-of-scope` 187, `may-utter-gains` 86, `ss03-chain-join-gains` 70, `entered-it-baseline-join-gain` 87, `tea-it-xheight` 39, `pea-chain-regularized` 37, `oy-it-baseline` 20.

## Open follow-ups / questions

- ~~**qsDay_qsUtter ligature after ·It.**~~ Resolved in E1: the exclusion covers both `qsDay` and the `qsDay_qsUtter` ligature.
- ~~After the extension phase, **rebuild the review surface** to re-baseline.~~ Done 2026-07-03 — see "Gates / known state".

## Gates / known state

- **Settle probe** (per-change spot check): see commands below.
- **Rebuild suite:** `uv run pytest rebuild/ -n auto --dist worksteal -q`. **Updated baseline (2026-07-03, post-re-baseline): a clean HEAD fails only the 4 documented spec pins** (`test_surface::test_real_cell_bindings_all_match`, `test_spec_load::{test_loads_all_six_runes, test_predicate_class_membership, test_group_resolution}`) — those pin the M1-batch-1 spec shape (six runes, batch-1 predicate classes) and predate this pass; re-baseline them when the batch-2 migration formally closes. The phase-end re-baseline (2026-07-03) rebuilt the surface in place at HEAD (stale shards cleared first, per the orphaned-shard wart) and re-pinned every review census test to the new build: 15,972 units / 80,990 rows / 20 batches; 10,083 machine-approved / 5,889 human (`boundary-echo` 4,465, `dangling-anchor-dropped` 4,148, `bare-name-live-join` 1,470); secondary seams 1,735 marked / 363 homed / 1,651 homeless / 344 suppressed; verdict families sum 1,149 windows (`seam-loss-withdrawal` 373, `deferred-ss04` 212, `unmatched-misc` 114, `no-chain-gains` 108, `extension-non-summing` 89, `may-utter-gains` 70, `deferred-ss10` 69, `deferred-ss03` 64, `tea-it-xheight` 33, `oy-it-baseline` 17). The drafts tests' worked examples re-point at `boundary-echo` (the class that absorbed `zwnj-word-initial-unification`). A future rune edit again trips the artifact-pinned tests until `run_m1` + `review.build` re-run. Distinguish real regressions by diffing the failure set against this clean-HEAD baseline.
- **`make test`** (main font, `test/ site/`) does not read the rune files; runs green independently (6753 passed).
- **The Manual-pin gate (2026-07-03):** `rebuild/pipeline/manual_pins.py` replays every corpus data-expect pin whose text falls inside the migrated alphabet (17 of 530 today) against the built M1.otf — trait assertions resolve through stance `traits:` in the spec, no waiver channel. Wired twice: a hard gate in `run_m1.main()` between the boundary gate and the oracle (writes `manual_pins_summary.json`; its `top_blocking_letters` is batch-prioritization signal), and `rebuild/test_manual_pins.py` in the rebuild suite. Artifact-pinned like the review census tests: a rune edit trips it until `run_m1` recompiles `rebuild/out/m1/M1.otf`.
- **Space and ZWNJ rows are gone from UNMATCHED as of 2026-07-03** — the ratified boundary-equals-word-boundary rule (see WHATNEXT.md) absorbs every non-position row whose window contains a run-splitting boundary (space or ZWNJ) into the `boundary-echo` ledger class (renamed from `zwnj-boundary-echo` when the space half landed) at top precedence, so per-config UNMATCHED diffs against pre-2026-07-03 audits will show space and ZWNJ windows vanishing; that is the rule working, not a regression. `run_m1.main()` now also runs the boundary-equals-text-edge split-buffer gate (hard-fails if any text renders differently around a space or ZWNJ than its segments do standing alone) before the oracle.
- **Collateral verification harness (this pass):** `rebuild/tools/seam_loss_diff.py <candidate_runes_dir>` re-settles the real runes and a candidate dir under the default config and reports per-group target restoration + any approved-window movement (real collateral). `rebuild/tools/seam_loss_probe.py` is the older shard-comparison probe — prefer the differ; the shard `after` seams are config-shadowed and produce phantom regressions.

## Resume commands

```bash
# Verdict ↔ unit ↔ class/draft join (counts by class, by keypath, by right-neighbor):
python3 - <<'PY'
import json, glob, collections
units={}
for f in glob.glob('rebuild/out/review/units/*.json'):
    for u in json.load(open(f)): units[u['id']]=u
V=json.load(open('verdicts-11.33.00PM.json'))['verdicts']
vbyu={}
for v in V:
    uid=v['unit']
    if uid not in vbyu or v['at']>vbyu[uid]['at']: vbyu[uid]=v
tab=collections.defaultdict(collections.Counter)
for uid,v in vbyu.items():
    u=units.get(uid); cls=u['class'] if u else '<MISSING>'
    tab[cls][v['verdict']]+=1
for cls in sorted(tab, key=lambda c:-sum(tab[c].values())):
    print(f'{cls:40s} {dict(tab[cls])}')
PY

# Per-change settle probe (edit the cases dict to the windows under review):
PYTHONPATH=. uv run python - <<'PY'
from rebuild.review.enrich import load_spec
from rebuild.pipeline.settle import settle, cell_label
spec = load_spec()
cases = {"ZWNJ Tea It Day":[0x200C,0xE652,0xE670,0xE653], "Tea It It":[0xE652,0xE670,0xE670]}
for label, cps in cases.items():
    print(f"{label:24s} -> {[cell_label(spec, s.cell) for s in settle(spec, cps, frozenset())]}")
PY

# Reflow a rune to canonical flow-vs-block formatting after editing:
uv run python tools/reflow_yaml.py glyph_data/runes/qsIt.yaml
```

### Full-rebuild verification (the real arbiter — the settle probe alone is not enough)

The served review surface (`rebuild/out/review/`, port 7294) is built from a **pre-compiled font** (`rebuild/out/m1/M1.otf`) plus **frozen unit shards**, so it never reflects an uncommitted or just-committed rune edit. The settle probe shows cell labels but not the rendered pixels or the oracle match/unmatched verdict. To truly verify a change (and catch divergences-from-old the probe hides), rebuild and diff:

```bash
uv run python -m rebuild.pipeline.run_m1                       # recompile M1.otf from the runes (writes rebuild/out/m1/, ~minutes)
uv run python -m rebuild.review.build --out tmp/review-preview # rebuild the surface non-destructively (leaves :7294 + verdicts untouched)
(cd tmp/review-preview && python3 -m http.server 7295)         # eyeball at http://localhost:7295/#class=...&unit=u-NNNN
```

Gotcha: `run_m1` exits nonzero whenever unmatched rows exist (i.e., always, mid-migration), so `run_m1 && review.build` silently skips the surface rebuild — run the two commands separately and check that `review.build` actually printed its `Wrote …` line before diffing.

Then compare UNMATCHED windows per config, keyed by codepoints (stable across rebuilds), preview vs the served surface:

```bash
python3 - <<'PY'
import json, glob, collections
def load(d):
    us={}
    for f in glob.glob(d+'/units/*.json'):
        for u in json.load(open(f)): us[u['codepoints']]=u
    return us
old, new = load('rebuild/out/review'), load('tmp/review-preview')
def bycfg(us):
    c=collections.Counter()
    for u in us.values():
        for cfg,cc in (u.get('config_classes') or {}).items():
            if cc=='UNMATCHED': c[cfg]+=1
    return c
co, cn = bycfg(old), bycfg(new)
for cfg in sorted(set(co)|set(cn)): print(f'{cfg:16s} {co[cfg]:4d} -> {cn[cfg]:4d}  {cn[cfg]-co[cfg]:+d}')
PY
```

**Lesson banked (2026-06-29):** the **stable old-font connector for a window lives in the config's `before` field, and it varies by config** — reading `before.glyphs` across mixed configs led me to a false "87 regressions" alarm for E1; the per-config rebuild diff is what set it straight (the true number was +1). Always confirm a divergence-from-old with the rebuild diff, per config, not the raw `before` field.

## Keeping this file honest

Update this file in the same change that lands a rune edit: tick the checklist item, note the commit, record any new override or follow-up. `WHATNEXT.md`'s M1 thread links here once the first edit commits.
