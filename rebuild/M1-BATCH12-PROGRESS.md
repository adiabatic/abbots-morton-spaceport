# M1 batch 12 — qsJai (and qsJai_qsUtter)

Scratch for the ·J'ai migration. Delete when the batch closes, lifting any surviving forward-pointer into `WHATNEXT.md`.

## Parked

Nothing yet.

## State

The batch is built and verified: the rune, the ligature rune (qsJai_qsUtter, the batch's formation-closure obligation), the alphabet, the aliases — including the already-migrated-neighbor names that only ever appear beside ·J'ai (the woken contracts' `ex-con` spellings on qsNo/qsLow/qsI/qsAh/qsUtter and the `*_qsUtter` ligatures, plus qsIt's `ex-y5.ex-ext-1` family) — the neighbor edits, the two ledger classes, the contact-allow and dangle blessings, the smoke block, and the suite-count pins are all in. `run_m1` is green, the artifact cycle's gates are green, and `make verdict-ready` reports READY with the fresh ·J'ai questions among the blanks. `rebuild/out/m1/oracle_summary.json` is the unmatched census; what remains of it around ·J'ai is standing-family echoes, the ss10 Junior-oracle candidates, and the sitting's open questions below.

## Shape of the letter, as modeled

·J'ai is Deep. One `hapax` stance — the old font's two stances share the prop bitmap, so they were anchor-availability variants, not motions — with an x-height entry at the crown's left end ([1, 5]) and a baseline exit at the bar's right end ([6, 0]), both allowlisted from oracle evidence. The old derives transcribe as: the exit extend by 1 (the old 12-family list plus qsVie_qsUtter, oracle-witnessed), the entry extend by 2 after qsGay/qsMay, and the entry contract by 1 after qsPea/qsTea/qsThey/qsHe. One yielding prefer carries the ·Tea taste: ·J'ai surrenders its baseline exit to ·Tea whenever ·Tea gains a forward join by it — the old font keeps `·J'ai ~b~ ·Tea` only at run ends and before ·Pea/·Tea/·Fee, and every keep-case except ·Pea already wins on join-count, so the record is the ordinary yielding prefer scoped `then: {except: [{family: qsPea}]}`.

The ligature is a single `hapax` stance with ·J'ai's entry (same from-list and entry-side records) and ·Utter's x-height exit at [10, 5]: extend before qsFee and the ss03-gated reach to half-·Tea (the qsVie_qsUtter pattern, with qsTea's half unlock left-list gaining qsJai_qsUtter), and the by-2 exit contract before ·J'ai — which every `*_qsUtter` ligature turns out to share.

## What the neighbors already knew

qsAh's baseline from-list named qsJai from the recon tail — now oracle-verified (the `·J'ai ~b~ ·Ah` pair is real). qsRoe's x-height toward-list, qsMay's and qsIt's extends, and the six dormant contracts (qsUtter and qsAh by 2, qsNo/qsLow and qsI's two per-stance records by 1) all named qsJai and now fire; every one of those scopes also fires before the ligature, so each `when.right` widened to `[qsJai, qsJai_qsUtter]`. The batch's other neighbor edits: qsFee's and qsOut's toward-lists gain the letter (both demonstrably serve it), qsOut's and qsRoe's gain the ligature, and qsVie_qsUtter/qsSee_qsUtter/qsDay_qsUtter each gain the by-2 contract toward `[qsJai, qsJai_qsUtter]` (per-stance on qsDay_qsUtter) — the old font witnesses `ex-con-2` on all four ligatures, matching qsUtter's own magnitude, so qsThey_qsUtter's by-1 override stays a qsThey-batch question.

## Recorded design overrides

- **Oracle-verified-only lists**, as every batch: the from/toward lists transcribe what the subset baselines witness, nothing more. Policy-record family lists carry the old font's full target lists (qsThaw/qsYe/qsIng/qsExam and qsGay/qsThey/qsHe stay as deferred-partner names, the qsVie precedent).
- **Every old exit contraction before ·J'ai is a tuck, re-spelled as ·J'ai's own entry contraction.** The old `ex-con-1`/`ex-con-2` names (qsNo/qsLow/qsI by 1; qsAh/qsUtter and the `*_qsUtter` ligatures by 2) keep the left glyph's ink and move only the anchor — the ·See·Out lesson again — and M1's contract arithmetic cannot take two pixels from the ·Utter crown's two-pixel stroke. The dormant left-side contract records are gone; qsJai and qsJai_qsUtter carry the receiver-side records instead (by 1 after qsNo/qsLow/qsI, by 2 after qsAh/qsUtter with the ligatures riding trail transparency), the aliases fold the old spellings onto the M1 identities, and the `jai-entry-contraction-respelled` ledger class absorbs the name grain, position-verified. When qsThey_qsUtter migrates its by-1 override needs a carve-out here, because trail transparency will otherwise hand it the by-2.
- **The ·May·J'ai extension is consolidated on ·May's side.** The old split (·May ex-ext-1 + ·J'ai en-ext-2) cannot survive the same-seam non-summing rule, so qsMay carries one by-2 exit record toward the qsJai family (its self-entry extend gains the matching except), while ·J'ai's en-ext-2 record survives only for the deferred qsGay. The sitting's spacing ruling lives in qsMay's record and its `why:`.
- **·J'ai's entry adjustment after ·Pea/·Tea is independent of its exit.** Their shared by-1 contract persists regardless of the follower, including when ·J'ai's exit extends. The run-final ·May·Tea·J'ai window is the deliberate exception: qsTea's left/right/boundary-scoped x-height extension carries the ruling in its `why:`. The deferred ·They/·He arm retains the old `right: except` guard over the exit-extend follower list, and the ligature composes both sides freely (`qsJai_qsUtter.ex-ext-1.en-con-1` is real).
- **ss10 membership**: bare qsJai is fully isolated by the old overlay (its anchors live on calt variants; the old base record has no anchors) and stays out of `SS10_UNCOVERED_BY_OLD_FONT`; qsJai_qsUtter's ss10 rows diverge at ligation grain (M1 never forms ligatures under ss10) and ride the Junior-equivalence machine approval like qsVie_qsUtter's and qsSee_qsUtter's, so the ligature stays out of the set too.

## Open questions for the sitting

- **·J'ai·Tea·No and kin**: the old font takes half-·Tea's x-height group (`·J'ai | ·Tea.half ~x~ ·No`) where M1's floor may prefer the two-baseline chain through ·No.alt; if it diverges, it queues in the regrouping family with the closed-list answer on record.
- **The fresh strict-gain and regrouping windows** the oracle census now carries (the ·Pea/·Tea ~x~ ·No ~x~ ·J'ai chains, the alt-·Utter arms, the qsPea `can-enter-at-x-height` class edge) — the review surface's echo grouping and the standing families fold most; whatever queues is the sitting's docket.

## The WHATNEXT items this batch answered mechanically

- **qsIt's baseline exit-extend toward qsJai**: dead as predicted (·J'ai has no baseline entry); the qsJai arm came off the record, leaving qsCheer/qsOwe as deferred partners.
- **qsAh's recon-derived from-list, qsJai arm**: oracle-verified real (`·J'ai ~b~ ·Ah` joins at the baseline).
- **qsFee's forward-preference left scope**: ·J'ai implicates nothing — its exit is baseline-only, so the ·J'ai·Fee seam is qsFee's forward x-height join with no competition, as predicted.
- **The ·Tea·Day tight bond**: ·J'ai's yielding prefer toward ·Tea covers it (`then: except qsPea`), oracle-shaped rather than the single-pair idiom — the old font yields ·J'ai's ·Tea join to every follower ·Tea can actually serve.

## Verification recipe

```zsh
uv run pytest rebuild/test_spec_load.py -n auto --dist worksteal   # rune loads
uv run python -m rebuild.pipeline.run_m1 --jobs 6                   # build + gates + oracle (refreshes the baseline subset itself)
PYTHONPATH=. uv run python rebuild/tools/probe.py E665:E65D         # one window, all configs
make test-rebuild
make test
make artifact-cycle ARGS='--update-pins'
make verdict-ready
```

## Resume

```zsh
make review-cycle ARGS='--update-pins'
```
