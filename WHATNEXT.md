# What next

The living punch list for the Abbots Morton Spaceport rebuild — the forward-looking companion to the per-milestone reports under `rebuild/`. It records what ought to happen next, the open forks in the road, and the deferred work we have consciously parked. Keep it current: see "Keeping this file honest" at the bottom.

## Open forks in the road

### Fork: the round-2 extension rejects — engine work or accept the drift

Round 2 (`rebuild/POLICY-ROUND-2-REPORT.md`) found that all 29 reviewer rejects are a single phenomenon — ·May's `en-ext-1` baseline-entry extension — and an exhaustive, build-tested lever hunt (25 candidate records across 5 independent lever families, every one scratch-built against a harness proven byte-identical to the oracle; zero cleared the bar) proved that **no closed-vocabulary policy record can honor them**. The wall is a depth-2 left-context predicate the frozen `when:` grammar cannot express (the predecessor's own entry state for the ss03 "pea-may" windows; the predecessor's predecessor for the "it-may" windows), compounded by a token grammar in which `en-ext-1` is un-droppable by any `extend` (always re-emits it), `contract` (coexists without netting), or `refuse` (collapses the whole join).

The two paths:

- **Engine work** — extend the same-seam non-summing rule so it also fires on ·May's _own_ summed exit (the ss03 "pea-may" case), and add a depth-2 left-context predicate (the "it-may" case). Touches `rebuild/pipeline/settle.py`, the rune schema, and tests. The 29 rejects then become honorable. Tracked below as the extension-suppression milestone.
- **Accept the drift** — keep the composed extension everywhere and retire the 29 rejects as taste-accepted divergence.

**Current lean (2026-06-12): accept the drift and postpone the engine work**, on the expectation that a more capable model will make the engine change cheaper and safer to attempt later. The drift is fully documented and recoverable: the 27 + 2 windows are recorded as engine-limited residue in the two reviewed ledger entries (`halves-entry-extension-restored`, `same-seam-extension-non-summing` in `rebuild/m1-divergences.yaml`), the proof and structural analysis are in `rebuild/POLICY-ROUND-2-REPORT.md`, and the verdict pile is `verdicts-08.19.24PM.json`. The lever-hunt evidence is the workflow at `tmp/lever-hunt-wf.js` and its result `tmp/wf2-result.json`; the build harness that produced it is `tmp/scratch_build.py` (parameterized `load_spec(runes_dir, ...)` build into a scratch out-dir).

To revisit: re-read `rebuild/POLICY-ROUND-2-REPORT.md` ("Why it is structurally impossible"), then decide engine-vs-drift. If engine, the work is the extension-suppression milestone below.

## The main thread — M1 rune migration

**In progress (paused at a checkpoint): the qsDay + qsUtter + qsNo batch.** All four rune files (qsDay, qsUtter, qsNo, and the qsDay_qsUtter ligature) plus the qsIt Q2 fix and the alphabet/alias/contact wiring are authored; all hard gates are green; the oracle is down to **2,535 unmatched rows (from 6,189)** after the 2026-06-13 triage session, with zero regressions. The ss10 isolation overlay (−2,823, intended), the qsDay_qsUtter ligature receivers, and the ss04 entered-·It over-extension are resolved as fix-to-match. **The remaining 2,535 (645 unique windows) are verdict-gated** — new joins the engine makes that the old font did not — and the decision (2026-06-13) is to **checkpoint, then build the round-3 review surface and present every family fresh** (no fast-tracking the look-alikes of approved classes). Resume from **`rebuild/M1-BATCH2-PROGRESS.md`** — it now has the verdict-family table, the session's resolved-vs-residue split, the known-pending wiring tests (not regressions), the methodology learnings, and the re-run/probe commands. Nothing is committed.

**Round-3 review surface — BUILT (2026-06-24), pending the actual adjudication.** The round-3 surface now presents all 721 UNMATCHED triples (645 windows) grouped into 10 verdict families for review. It is a **presentation-only** grouping — no rune, ledger predicate, or font change; the oracle stays at 2,535 until the families are adjudicated. Build it with `uv run python -m rebuild.review.build`, serve on 7294 with `uv run python -m rebuild.review.serve`. The families, default-config first (the ordinary-writing joins to adjudicate now) then the deferred stylistic-set buckets: `no-chain-gains` (67), `tea-it-xheight` (39), `oy-it-baseline` (22), `may-utter-gains` (24), `seam-loss-withdrawal` (154, the context-dependent / partly engine-limited losses), `extension-non-summing` (15), `unmatched-misc` (3), then `deferred-ss04` (253), `deferred-ss10` (76), `deferred-ss03` (68) — the 321 stylistic-set-only windows parked for a later pass per the decision to defer ssNN-gated work. The 56 windows blessed under ss03 but novel under default carry a per-config blessed/novel strip and are adjudicated on their default behavior. Design + build details in `rebuild/REVIEW-REPORT.md` (Round 3 section) and `rebuild/M1-BATCH2-PROGRESS.md`. The change (rebuild/review/{audit,build,enrich,families}.py + static/{app.js,app.css} + the re-baselined review tests) is uncommitted; `make test` is green (6753), all 107 review tests pass. **Next: the human adjudication — review each default family and record verdicts, deferring the ssNN buckets.**

**Update (2026-06-27): pulled-back removed, the new joins embraced.** Mid-adjudication, the exit-side `withdrawal: pulled-back` was found to be a rebuild-rune invention (the legacy font has no pulled-back shape and draws these letters' exit nub unconditionally) that was _also_ implicitly suppressing joins. It was stripped from qsUtter, qsNo, qsDay, qsDay_qsUtter, and qsMay's exit (·May keeps its entry-side receiving stub); 26 boundary terminals were blessed as `dangle:` entries in `m1-contact-allow.yaml`; and the now-obsolete `may-exit-withdrawal-generalized` ledger entry was removed, surfacing its 220 rows / 61 windows for review. The engine now joins pairs the old font left unjoined (·Utter·No, ·No·No at the x-height; ·No·Tea flipping to the baseline). **Oracle 2,535 → 4,169 unmatched**, all within the five touched families. The review surface was rebuilt (15,645 units; new family census in `rebuild/M1-BATCH2-PROGRESS.md`'s 2026-06-27 section) and the census tests re-baselined; `make test` green (6,753), rebuild suite 388 passed / 4 pre-existing failures. In-progress verdicts were preserved by a content-keyed remap (`verdicts-10.53.42AM.json` → `verdicts-10.53.42AM.remapped.json`, 120/147 carried, the 27 dropped being the rewritten pulled-back windows). Full detail + resume commands: the 2026-06-27 section of `rebuild/M1-BATCH2-PROGRESS.md`.

**Update (2026-06-28): the ss10-isolation-completed family is fully adjudicated (2,823/2,823), most of it auto-classified.** Validated against all 2,457 prior ss10 verdicts, the rule "the new font breaks every join and forms no ligature → approve" separated perfectly (2,301/2,301 prior approvals, zero counterexamples) — the canonical isolation intent — so the 349 remaining no-ligature windows plus the lone qsTea_qsOy window (u-14286, 18/18 prior approvals, its judged pair off the ligature seam) were auto-approved: 350 additive `approve` records that reuse the live manifest's `u-NNNN` IDs and `manifest_generated_at`, so nothing rebuilt and no ID renumbered (`tmp/auto_classify_ss10.py` → `verdicts-ss10-auto.json`, with distinctive auto-notes for filterability). The 16 qsDay_qsUtter ligature windows resisted every structural discriminator (windows identical on all measurable features split the prior verdicts ~50/50; `reject` requires the old font to have joined a neighbor into the ligature, but the converse fails — it is a genuinely visual call), so they were hand-adjudicated in the live surface and came back 2 approve / 14 neither. A jitter audit (`tmp/jitter_audit.py` → `tmp/jitter-candidates.md`) surfaced 7 likely-misclick off-votes — minority verdicts inside otherwise ≥90%-unanimous families — for a re-check pass, correctly leaving the legitimately-mixed families (marker-staging, the qsDay_qsUtter ~50/50 group) unflagged. The working master is now `verdicts-merged-07.30.00PM.json` (3,719 records = 3,353 prior + 350 auto + 16, all on the `2026-06-28T20:49:09Z` manifest). To make that re-check — and any future curated set — reviewable on one page, the review surface gained a `units=` worklist filter: a deep-link like `#units=u-1163,u-2224,…` renders exactly those units, stacked and grouped, with canonical-ID verdict export (`rebuild/review/static/{state,render,app}.js` + jstests; copied into `rebuild/out/review/` so no rebuild was needed). Gates: `make test` green (6,753), rebuild suite 388 passed / 4 pre-existing failures, JS suite 76 passed. **All uncommitted** — the verdict artifacts are gitignored; the worklist filter is 5 tracked source files awaiting a commit.

**Update (2026-06-29): a verdict-application pass is in flight — applying the round-3 verdicts to the rune data, and it is the active work to resume before this main migration thread.** Working from the exported verdicts `verdicts-11.33.00PM.json` (4,828 unique-unit verdicts, on the `2026-06-29T04:45:25Z` manifest — newer than the `verdicts-merged-07.30.00PM.json` master above), we are translating the recorded verdicts into `glyph_data/runes/*.yaml` edits one logical change at a time. **Resume from `rebuild/VERDICT-APPLICATION-PROGRESS.md`** — it carries the phase plan, the per-change ledger, the recorded mind-change overrides, the verification recipe, and the resume commands. Status: the **extensions/contractions phase is COMPLETE** — E1 (`8749048`, ·It·Day → uniform 1px) and E3 (`5ffff0e`, ·May·(Day+Utter) extension restored) committed; E2 (`halves-entry-extension-restored`, 172 rejects) parked as the documented round-2 engine wall; `same-seam-extension-non-summing` and the `contract:` records confirmed no-op for this batch. **Phase 2 (`seam-loss-withdrawal`, 83 rejects) is now IN PROGRESS — Group A is COMMITTED** (`e300b13` + the `flipped`→`alternate` rename; word-initial ·Utter·No keeps its x-height join, framed as "'un-' shouldn't use alternate ·Utter", The Manual p21 — restores 43–44 windows/config, zero collateral across all 8 configs, `make test` green). The 83 rejects triage into three root causes / five logical changes; **Group B (qsIt, ·It·It·Utter, 16 windows) and Group D-1 (qsNo, ·No·May·May·May, 1 window) landed 2026-07-02 in one fully verified commit** — D-1 needed a `left: {is: boundary}` guard the original triage missed (unguarded, it regresses 15 of Group A's restored ·Utter·No·May windows). **Phase 3 (2026-07-03) followed from 32 fresh verdicts recorded against the post-B/D-1 preview surface**: five user-adjudicated design changes across qsIt/qsUtter/qsMay/qsPea/qsNo (the flagship: an entered ·Utter now reaches ·May at the x-height into a stubless pulled-back ·May), all browser-eyeballed via side-by-side candidate fonts, verified, and detailed in `rebuild/VERDICT-APPLICATION-PROGRESS.md`'s Phase-3 section. The new-join families (·Pea·No chains, ·Utter·May reaches, ·Pea·Pea chain gains) sit as intended UNMATCHED divergence awaiting the phase-end re-baseline blessing. **Group C is held** (its lever also turns 4 ◊ZWNJ·Pea/·Tea·No windows break→x-height) and **D-2 + 6 windows are parked** (the 6 engine-limited at the round-2 depth-2/3 wall). The full triage table, exact edits, and per-group status live in `rebuild/VERDICT-APPLICATION-PROGRESS.md`'s "Phase 2 — seam-loss-withdrawal" section. Note for the next agent: a rune edit's true effect is only visible after the full-rebuild verification (recompile `M1.otf`, rebuild the surface, diff UNMATCHED per config) — the served surface and the bare settle probe both hide divergences-from-old; and the committed review surface is stale vs HEAD, so a clean tree already shows ~13 rebuild-suite failures (see the progress doc's updated "Gates / known state").

**Update (2026-07-03): the ZWNJ-equals-word-boundary rule is ratified and mechanized — no window containing a ZWNJ ever needs a verdict again.** The rule: post-ZWNJ is definitionally word-initial and pre-ZWNJ definitionally word-final, so every segment of a ZWNJ window must render exactly as that segment standing alone, and a ZWNJ unit therefore carries no adjudicable information its boundary-siblings don't (every segment of a length-≤5 window is itself an enumerated window). Motivation: 23% of review units (3,570/15,645) contained a ZWNJ, and the old font's shadow-universe inconsistency made the surface ask the same design question against two different old-font backdrops — producing at least one direct contradiction (u-15220 `·No·It·Day` approved vs u-15230 `·No·It·Day ◊ZWNJ` rejected on identical new ink); an audit of all 44 past ZWNJ rejects confirmed none was about the ZWNJ context itself. The guarantee lives at three levels, two of them permanent vetting code rather than transition ledger: (1) the rune schema no longer admits `is: zwnj` (`boundaryValue` enum, with `test_zwnj_not_addressable_in_when`), so no record can ever distinguish a ZWNJ context from a text edge; (2) `check_split_buffer` + `check_zwnj_structure` in `run_conformance` enforce the rendering half per build over the exhaustive enumeration, now with **zero carve-outs** — the namer dot no longer lowers through a ZWNJ (an `ignore sub periodcentered' uni200C` ahead of the lowering rule in `m1_namer_dot_word_start`; HarfBuzz otherwise skips the default-ignorable ZWNJ in contextual matching), which removed the one codified cross-ZWNJ behavior and let `_slot_signature` drop its dot special-case; (3) the `zwnj-boundary-echo` ledger class absorbs every non-position divergent row containing a ZWNJ at top precedence, subsuming the ZWNJ share of `zwnj-word-initial-unification` / `zwnj-follower-exit-restored` / `zwnj-word-initial-seam-moved` and discharging the seam-moved entry's pending human-verdict pass. Measured on the rebuilt audit (M1.otf recompiled with the dot fix): the gate proves the invariant over 66,045 ZWNJ sequences × 8 configs (528,360 shaping runs, 0 divergences, ~9 s — it now runs in `run_m1.main()` as a hard gate before the oracle, writing `zwnj_equivalence_summary.json`); the blanket took 17,995 rows / 3,415 units, the three subsumed `zwnj-*` classes came back **empty** (no namer-dot shadow remainder exists in the current alphabet; entries kept at count 0 for the documentation trail), the 188 position-only ZWNJ rows stay on `kern-channel-out-of-scope`, `multi_matched` stayed 0 (the ss10 predicate now excludes ZWNJ rows so the partition stays exact), and **zero UNMATCHED rows contain a ZWNJ** — a preview surface rebuild (`tmp/review-preview`, 15,808 units) confirms every verdict family is ZWNJ-free (the 41 unverdicted regrouping-floor-drift stragglers included). What this forecloses, by design: the old font's post-ZWNJ renderings are unrecoverable — preferring an old ZWNJ-only behavior means changing the word-initial form, which changes both. The dot-through-ZWNJ divergence from the old font is intended and rides the blanket class; a `00B7 200C E670` row joined the CoreText smoke set. Existing ZWNJ verdicts in the masters become inert history. One wart for the phase-end rebuild: `rebuild.review.build` does not delete orphaned class shards, so clear the target `units/` dir (or prune shards for classes absent from the manifest) when rebuilding in place. **Same-day follow-up: the rule now covers space too — every run-splitting boundary equals a text edge.** The gate was generalized to split on every `splits_runs` boundary token (renamed `run_boundary_equivalence`, writing `boundary_equivalence_summary.json`; 110,726 boundary-containing sequences × 8 configs = 885,808 shaping runs, 0 divergences, ~15 s per build), and `is: space` was removed from the schema's `boundaryValue` enum alongside `is: zwnj` (`test_run_splitting_boundaries_not_addressable_in_when`), so no rune record can render a pre- or post-space context differently from a text edge either. `is: namer-dot` deliberately survives — the dot does not split runs (position stays medial per BETTERRUNESCHEMA R32) and keeps its own semantics. Note the asymmetry left standing: space windows still go through the ordinary ledger/review machinery (no space analogue of `zwnj-boundary-echo` was authored), because the old font kerns across spaces and the review dedup for space windows was not part of this ratification — extend the blanket to space only with a fresh decision.

**Update (2026-07-03, ledger follow-up): the boundary blanket now covers space — no window containing a space or ZWNJ ever needs a verdict.** The fresh decision the previous update called for was made, closing the asymmetry it left standing: the `zwnj-boundary-echo` ledger class is renamed **`boundary-echo`**, and its predicate now absorbs every non-position divergent row whose window contains either run-splitting boundary (a `0020` or `200C` token in the colon-split codepoints), with the matching `0020` exclusion added to the ss10 predicate so the partition stays exact. Ratification evidence for the space half (recorded in the ledger entry's `why:`): of the 744 space units verdicted on the pre-blanket surface (679 approve / 32 reject / 31 neither / 2 either), only 2 rejects were not mirrored by a boundary-sibling reject — u-15228 `·No·It·Day ␣` and u-15241 `·No·It·Utter ␣` (deferred-ss04) — and both contradict their approved bare siblings u-15220/u-15221, the same double-ask pathology that motivated the ZWNJ rule; no space reject was about the space context itself, and the rule retires those two rejects with no other action. Measured on the refreshed oracle (no font rebuild — nothing here touches runes, emit, or geometry): the blanket now takes 33,508 rows (from 17,995 ZWNJ-only), UNMATCHED dropped 5,557 → 4,508 (the absorbed space windows — the point, not a bug), `multi_matched` stayed 0, zero UNMATCHED rows contain a `0020` or `200C` token, and the 188 position-only boundary rows stay on `kern-channel-out-of-scope` (the old font kerns across spaces; the position channel remains exempt by design). A preview-surface rebuild (`tmp/review-preview`, 15,808 units, stale shards cleared first per the known wart) confirms every verdict family is now space-free as well as ZWNJ-free — the only boundary-containing units left are `boundary-echo` (6,252) and the position-only kern channel (188). As with the ZWNJ half, the old font's space-context-only renderings are foreclosed by design, and existing space verdicts in the masters become inert history.

**Update (2026-07-03, the Manual-pin gate): every corpus data-expect pin inside the migrated alphabet is now a standing hard gate against M1, and its first catch is fixed.** `rebuild/pipeline/manual_pins.py` replays every senior data-expect run whose text the migrated alphabet can express (17 of 530 today, all from `site/the-manual.html`) against the freshly built M1.otf using the validation suite's black-box shaper and GPOS seam classifier, with M1-fidelity semantics: `.half`/`.alt` assertions resolve through stance `traits:` in the loaded spec rather than glyph-name substrings (M1 stance keys like `flipped` need not spell the trait), `.∅` accepts the bare cmap glyph or the settled isolated cell's label, and compat-only variant assertions are skipped-and-counted exactly as in the baseline replay. **There is deliberately no waiver channel**: a disagreement means either the runes break a Manual mandate (fix the runes) or the pin mistranscribes The Manual (fix the pin, which `make test` cross-checks against the shipped font). Wired twice — a hard gate in `run_m1.main()` between the boundary gate and the oracle (writes `manual_pins_summary.json`, whose `top_blocking_letters` census doubles as batch-prioritization signal: ·Roe blocks 167 of the 513 out-of-scope pins, ·Low 119, ·See 97, ·Key 84), and `rebuild/test_manual_pins.py` in the rebuild suite (artifact-pinned to `rebuild/out/m1/M1.otf`, so it trips after a rune edit until `run_m1` recompiles). The gate's first catch was The Manual's "Indian" (`·It ~b~ ·No ~b~ ·Day | ·It ~b~ ·Utter ~x~ ·No`, the-manual.html:3828): the engine's floor tiebreak joined ·Day·It at the baseline, foreclosing the mandated ·It·Utter join (·It cannot join at the baseline on both sides). Fixed by a `qsDay` yielding prefer (the qsDay mirror of the N1 qsIt record — full details, A/B oracle diff, and the one 6-row ·Day·It·Utter·May residue now headed to review live in `rebuild/VERDICT-APPLICATION-PROGRESS.md`, entry P-Indian). Oracle at HEAD moves 4,508 → 4,514 unmatched: 108 Manual-restored rows leave the review queue for the cell-name ledger classes, 6 arrive as intended divergence.

**Update (2026-07-03, Group C resolved — the seam-loss-withdrawal phase is fully discharged; committed later that day as `6ab6309`).** The last held item of Phase 2 closed with a user-ratified design decision: **·Tea·No now takes the baseline alt chain — `·Tea ~b~ ·No.alt` — absent other considerations**, mirroring Q2's ·Pea decision, instead of the old font's default `·Tea.half ~x~ ·No`. The landed change is one line: `qsTea` added to flipped ·No's baseline-entry `from:` list in `qsNo.yaml` (it was simply missing; the `qsTea_qsOy` ligature and Q2's `qsPea` were already there — no depth-2 predicate was ever needed, so the two ·Oy·Tea·No windows the triage had misfiled as engine-limited restore byte-exact). Standing exceptions the engine keeps: before ·Day/·It/·Oy the pair stays at the matched both-sides-joined `[y5, y5]` shape (`qsNo.prefer[1]`), and after an entered ·Tea the chain structurally cannot form. Verified by full A/B scratch oracles (`tmp/t7-pre` 4,514 → `tmp/t1-post` 5,477 unmatched) + `run_m1` gates: 980 rows / 152 windows enter as embraced divergence, every one containing a ·Tea·No adjacency (perfect scoping), 690 of them the deliberate look-change on previously-matched windows, 290 completing already-divergent regrouping drift; 17 rows restore byte-exact; the 8 previously-approved y5 trailing pairs flip by design (recorded as a rule-level override in the progress doc); `multi_matched` 0, boundary gate and Manual pins green. With this, **every one of the 83 seam-loss rejects is discharged** (A/B/C/D-1 landed; D-2 and the ZWNJ share retired by the boundary rule; the ·Oy pair restored) — the "6 engine-limited" park dissolved entirely. Details: the "Group C resolved" section of `rebuild/VERDICT-APPLICATION-PROGRESS.md`.

**Update (2026-07-03, the phase-end re-baseline is DONE — next: the blessing adjudication.)** With every Phase-2 and Phase-3 rune edit committed, the phase-end rebuild ran at HEAD: `run_m1` recompiled M1.otf (oracle **5,477 unmatched** as the Group C verification predicted, `multi_matched` 0, boundary gate 885,808 runs / 0 divergences, Manual pins 17/17), the review surface rebuilt in place into `rebuild/out/review` (**15,972 units / 80,990 rows / 20 batches**, stale shards cleared first per the orphaned-shard wart), and every review census pin re-baselined (`test_review_{audit,build,families,ink,drafts}` — new census: 10,083 machine-approved / 5,889 human units; verdict families sum **1,149 windows**, the big movers being `seam-loss-withdrawal` at 373 — it absorbed the ·Tea·No chain windows — plus `deferred-ss04` 212, `unmatched-misc` 114, `no-chain-gains` 108, `extension-non-summing` 89). The rebuild suite is back to exactly the 4 documented batch-1 spec pins (401 passed otherwise); `make test` and the 80-test JS suite are green. Prior verdict exports (`verdicts-11.33.00PM.json` and earlier masters) are now inert history against the fresh manifest — all applied or superseded. **Next: the human blessing adjudication on the fresh surface** (serve with `uv run python -m rebuild.review.serve`), reviewing the 1,149-window verdict families — the embraced ·Tea·No / ·Pea·No chains, the Q1★/Q4 new-join gains, and the P-Indian residue — with the ssNN buckets still deferred. After that, the main migration thread resumes below.

The rebuild is migrating the cursive-join engine rune by rune and validating each batch against the old shipped font via the oracle (`rebuild/M1-REPORT.md`). The next batch (M1-REPORT §11.3) is **qsDay + qsUtter, plus qsNo or qsFee**:

- **qsDay + qsUtter** — dominate the deferred-partner list (§4.6) and adjudicate the two recorded expressiveness questions (qsIt's ss04 before-·Utter stance nulling inherited derives — the closed vocabulary has no negated-feature condition; the trait-qualified group widening in `utter-pass-through-vetoes`).
- **qsNo** — exercises prefer competition (the design's worked prefer examples, §5.2/§5.3).
- **qsFee** — exercises the `bind:` contract at settlement level.

Together qsNo and qsFee retire the two largest unexercised-semantics blocks in §7 with real records instead of synthetic fixtures.

## Deferred and tracked follow-ups

- **Extension-suppression engine milestone** — the round-2 fork's engine path: a feature-scoped settlement change (self-exit non-summing + a depth-2 left-context predicate). Parked pending the fork decision above.
- **Ink-comparator widening** — there are now 8 `identical` verdicts sitting on pixel-identical dangling-exit windows (round 1 had 3: u-0401, u-0414, u-0415). This is the evidence pile for widening the ink comparator so dangling-exit-only deltas auto-classify as machine-approved instead of landing in the human review queue.
- **qsMay loop stance before ·Tea·Oy** — round-2 decision #1's logged note: u-0243/u-0244/u-0245/u-0250 were approved as an improvement, but the user flagged that ·May should arguably use the loop stance there. A future `qsMay` stance item, not a blocker.
- **Unresolved design flags** (M1-REPORT §11.4) — the inexpressible `reaches_up_and_way_over` resolved-extension scope on qsMay's x-height entry (needs either a design extension or proof the partners' own extend records cover it when qsAh/qsUtter migrate), and the ss05/ss04 row-grain refusal coverage notes. Re-check at the qsEt/qsDay/qsLow/qsUtter migration.
- **ss03 full-size ·Tea when a baseline-joiner follows** — the clarified meaning of ss03: under ss03 an x-height exiter joins ·Tea at the x-height, and ·Tea should take its **full-size** form (entered at the x-height, exiting at the baseline) **when the letter after ·Tea joins ·Tea at the baseline** — only the full bar descends far enough to offer that exit. In isolation the **half** stub is correctly chosen, so the distinction is invisible without a baseline-joining follower. This is a **gap**: today `qsTea.yaml`'s `full` stance unlocks an x-height entry only for `left: qsFee` (ss03) / `left: qsI` (ss02); the broad exiter set unlocks only the `half` stance (no baseline exit), so e.g. `·May·Tea·Utter` renders the half stub and ·Utter fails to baseline-join. Honoring it is a **deliberate divergence from the legacy oracle** (legacy has only `half_entry_xheight_ss03` for the broad set plus `entry_xheight_after_fee` for ·Fee), so it needs the oracle/review-surface re-blessing path, not a parity fix. The docs (README ss03 blurb, `rebuild/script.yaml` description, `FEATURE_DESCRIPTIONS`) were corrected on 2026-06-27 to describe this intent; the engine change is deferred. **Mechanism when built:** add one right-gated unlock to `qsTea.yaml`'s `full` stance mirroring the half stance's broad left-set, gated `right: {class: can-enter-at-baseline}` (the predicate class already exists in `script.yaml`); `order: [full, half]` then yields "full when a baseline-joiner follows, half otherwise" (in true isolation the right-class predicate fails against the EDGE token, so half is still chosen), and the full stance already permits the `{entry: x-height, exit: baseline}` pairing. **Caveat the frozen `when:` grammar cannot fence:** `can-enter-at-baseline` is a _static_ capability predicate, so it over-triggers on followers that have a selectable baseline entry but do not actually accept ·Tea's baseline join (qsTea, qsIt, qsPea, qsNo) — for those, full's _non-joining_ cell would win the order tiebreak and render a full bar where the half stub is wanted, so the build also needs a suppressing `refuse` on full's non-joining cell for that follower set, or an explicit decision to accept the full bar there.

## Round 2 — status

Closed (`rebuild/POLICY-ROUND-2-REPORT.md`). Two tracked files changed — `rebuild/m1-divergences.yaml` (ledger prose) and the report — with no rune or font change; all gates green (oracle 0 unmatched / 0 multi-matched, `make test` 6753 passed, old Senior Sans OTF byte-identical by construction). Uncommitted pending the user's approval as of 2026-06-12.

## Keeping this file honest

This is a living document. Update it in the same change whenever you finish a migration batch, resolve a fork in the road, surface new deferred work, or close out a milestone report under `rebuild/`. `AGENTS.md` points here.
