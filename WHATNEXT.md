# What next

The living punch list for the Abbots Morton Spaceport rebuild — the forward-looking companion to the per-milestone plans under `rebuild/`. It records what ought to happen next, the open forks in the road, and the deferred work we have consciously parked. Keep it current: see “Keeping this file honest” at the bottom.

## The main thread — M1 rune migration

**Current state: the verdict-application phases are committed; the immediate work is finishing the blessing adjudication.** Phase 5 and the SS03-MAYTEA full-·Tea-bar follow-up are committed, and their commit-time artifact cycles have run. That cycle is now mechanized as one command — `make artifact-cycle ARGS='--verdicts <master>.json'`, driven by `rebuild/tools/artifact_cycle.py`. **~1,280 units remain to verdict** on the current review surface (manifest `2026-07-04T20:22:20Z`; 595 of them outside the deferred ssNN buckets). The prior rounds’ verdicts are already in the app — the served surface restores them from `verdicts-autosave.json` — and the matching sitting export `verdicts-sitting-2026-07-07.json` is the frontier verdicts file the docket tooling reads. Resume with the `/review-docket` skill (backed by `rebuild/tools/review_docket.py`): it runs the echo prefill, clusters the blanks across echo groups into per-delta briefs at `docket.html`, and stages bulk candidates for the classes the ledger has already ruled. Serve the surface with `uv run python -m rebuild.review.serve` on port 7294, import the docket’s fill/bulk files through the app’s import control, and adjudicate the remaining units, with the ssNN buckets still deferred.

The rebuild is migrating the cursive-join engine rune by rune, validating each batch against the old shipped font via the oracle. The immediate next work is finishing the ~1,280-unit blessing adjudication on the current surface; once those verdicts are recorded and applied, the rune migration resumes with the next batch.

## Deferred and tracked follow-ups

- **qsDay baseline-entry extension drop — unclassified** — the ·May baseline-entry reclassification scoped its intended `may-baseline-entry-extension-dropped` class to the `-en-ext-1:qsMay` phenomenon, so the same phenomenon on qsDay is left as the unclaimed `-en-ext-1:qsDay`: the half-·Day baseline entry extension the old font drew after the ·Tea·Oy ligature, which qsDay’s `left: [qsTea, qsYe]`-scoped baseline extend record doesn’t reach (the ligature isn’t bare qsTea). Most such rows fall to UNMATCHED; the word-final ones (exit also dropped) currently ride `dangling-anchor-dropped`. Adjudicate it with qsDay’s own family — either widen qsDay’s baseline extend record to cover the ligature, or record the drop as intended.
- **The x-height-halves entry-extension siblings — parked deletion test** — the `{entry: x-height, by: 1, when: {left: {class: halves-that-exit-at-x-height, joined_at: x-height}}}` extend records on qsDay, qsOy, qsNo, qsPea, and qsMay (`qsDay.extend[0]`, `qsOy.extend[0]`, `qsNo.extend[0]`, `qsPea.extend[0]`, `qsMay.extend[4]`) are the same transcription-artifact shape round-1 deleted from qsIt (“the shipped font never realized it”), and their windows are the surviving 0-approve reject pile in `halves-entry-extension-restored` (the ledger entry in `rebuild/m1-divergences.yaml` carries the full history; the round-2 engine-wall residue that used to share the class dissolved with the ·May baseline-entry removal). Hypothesis: cleanly deletable parity-reverts. Unconfirmed — before deleting anything, run the empirical oracle test (delete each record on a scratch build via `rebuild/tools/scratch_build.py`, confirm UNMATCHED drops with zero collateral on currently-matched windows), because the review surface holds only divergent windows and cannot by itself prove a record never fires in a matched one.
- **Ink-comparator widening** — there are now 8 `identical` verdicts sitting on pixel-identical dangling-exit windows (round 1 had 3: u-0401, u-0414, u-0415). This is the evidence pile for widening the ink comparator so dangling-exit-only deltas auto-classify as machine-approved instead of landing in the human review queue.
- **Unresolved design flags** — the inexpressible `reaches_up_and_way_over` resolved-extension scope on qsMay’s x-height entry (needs either a design extension or proof the partners’ own extend records cover it when qsAh/qsUtter migrate), and the ss05/ss04 row-grain refusal coverage notes. Re-check at the qsEt/qsDay/qsLow/qsUtter migration.
- **qsZoo Manual-pin flag** — when qsZoo migrates, the Manual pin `·Tea ~b~ ·May.en-ext-1 ~x~ ·Zoo` will trip the no-waiver Manual-pin gate; re-transcribe the pin or fix the runes at that point.
- **Dormant `contract:` records** — the `contract:` policy records on qsNo/qsUtter/qsTea/qsMay never fired in this batch’s corpus; re-adjudicate them when qsJai/qsZoo/qsJay/qsFee migrate and can actually exercise them.
- **DirectWrite CI harness — the prototype’s one unabsorbed follow-up** — the de-risking prototype verified cross-shaper semantics on HarfBuzz (exhaustive) and CoreText (curated) only; DirectWrite was deferred with a written caveat, and the filed close-out is a `windows-latest` CI job (a ~60-line harness around `IDWriteTextAnalyzer::GetGlyphs` / `GetGlyphPlacements`) asserting the same GID-and-position table as the CoreText harness. Priority assertions: lock-fires-join-doesn’t, ZWNJ in every rule slot, and the leak-demo witness shapes (·It ZWNJ ·Tea ·Oy; ·It ·May ZWNJ ·Oy; ·Tea ·It ·May ZWNJ ·Oy) — DirectWrite is the one shaper whose default-ignorable-in-context behavior we have never observed. The prototype’s commitment was that this lands before the rebuild ships; the full deferral rationale and harness design are in `prototype/directwrite.md`, which now lives only in git history (the `prototype/` tree was deleted once its go/no-go decision closed).
- **Conform-gate horizon** — `gate:conform` (the exhaustive font-vs-settle sweep, `run_m1 --conform-only`, sharded per config) now runs in every artifact cycle at the length-5 horizon, queued behind the pytest gates; `--skip-conform` opts a cycle out. The sweep grows with the fifth power of the alphabet (~50 s sharded at today’s 8 migrated letters, ~15 min by ~17, hours beyond that), so when a migration batch makes this gate the cycle’s long pole, drop `--conform-horizon` below 5 — witness top-ups keep rule and transition coverage exact at any horizon; only off-corpus diff density shrinks.
- **Primary-pair picking counts annotation-grain renames — re-derive at the next surface boundary** — `_pick_pair` in `rebuild/review/enrich.py` takes the first adjacent run of divergent positions, and a position that diverges only on the annotation grain (a bare-glyph alias carrying no entry/exit fact, e.g. bare `qsNo` vs `qsNo/loop/None/x-height/`) counts the same as real ink, so it can drag the judged pair off the ink entirely: u-6845 (·No·It·May·Day) marks ·No·It as its pair and homes ·May·Day at u-15427, while the dropped-extension ink of its `same-seam-extension-non-summing` phenomenon sits at the unmarked ·It·May junction between them. The fix is to pick the pair (and `_secondary_pairs` coverage) over ink-visible divergent positions only, letting annotation-grain renames ride along instead of anchoring. Deferred because pairs key `group`, the echo keys, and seam-home resolution, so re-deriving them reshuffles a live surface mid-adjudication; land it at a surface-rebuild boundary — most naturally the batch-2 close, alongside the spec-pin re-baseline below — where the next `review.build` mints a fresh universe anyway. Applied verdicts survive the re-keying only through `carry_verdicts.py`’s ink-key fallback (`content_key` includes `pair` and `group`), so after the change, verify the carried-verdict count holds and triage any ink-key conflicts it reports.
- **Batch-1 spec-pin re-baseline** — the rebuild suite fails exactly 4 tests on a clean HEAD by design: `test_surface.py::test_real_cell_bindings_all_match` and `test_spec_load.py`’s `test_loads_all_six_runes` / `test_predicate_class_membership` / `test_group_resolution` pin the M1-batch-1 spec shape (six runes, batch-1 predicate classes), and the artifact-cycle driver subtracts them as its expected `baseline` bucket. Re-baseline them when the batch-2 migration formally closes, and retire the driver’s baseline bucket at the same time. Distinguish real regressions in any rebuild-suite run by diffing the failure set against these four (plus the artifact-pinned census/Manual-pin tests, which go stale after any rune edit until `run_m1` + `review.build` re-run).

## Keeping this file honest

This is a punch list of what’s _left_ — not a changelog of what happened. When work lands, edit the current-state description in place and delete the status it supersedes; never append a dated `Update (…)` paragraph. What got done — with what counts, and under which commits — is already in the git log, so this file records only the live frontier: the open forks, the next batch, and the consciously-parked follow-ups. When a landed change leaves a genuinely-open residue, record the residue as a forward bullet, not the change that produced it. The broader workflow rules — no after-the-fact reports, bounded progress files, evidence that outlives its decision — live in `AGENTS.md`’s “Note-taking and the rebuild logs” section; `AGENTS.md` points back here.
