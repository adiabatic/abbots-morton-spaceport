# What next

The living punch list for the Abbots Morton Spaceport rebuild — the forward-looking companion to the per-milestone plans under `rebuild/`. It records what ought to happen next, the open forks in the road, and the deferred work we have consciously parked. Keep it current: see “Keeping this file honest” at the bottom.

## The main thread — M1 rune migration

**Current state: the qsFee batch is closed — the ·Fee sitting's verdicts are in the runes and ·Pea·No now always joins low — the ·Oy withdraw-twins sitting has landed, and the surface is fully adjudicated: zero blanks. The next batch is the qsAh migration.** ·Pea·No renders `·Pea.!half ~b~ ·No.alt` in every window: half-·Pea's x-height exit no longer reaches ·No (a `qsNo` carve-out on the `toward:` scope), paired with the matching ·Pea carve-out in qsNo's x-height-exit prefer, which otherwise wins the seam back before ·Day/·No/·It/·Oy/·Fee and — with only the qsPea half — turns ·Pea·No·Oy into a break rather than a low join. The accepted price is ·Pea·No·Fee and ·Pea·No·Oy, which now stop after ·No instead of chaining on at the x-height. `glyph_data/runes/qsFee.yaml` is live — the clockwise `loop` (top-entry stub, x-height exit) plus the counter-clockwise join-born `reversed-loop` (`require: [entry]`, no exits), so "·Fee cannot join both to and from at the x-height" is structural rather than policied. The sitting's 94 rejects landed as two rune changes: qsFee now prefers its forward x-height join over the backward one when entered from the newly-widened sources (·May/·No/·Low) — except before ·Tea and ·Utter, where the approved taste keeps the backward join — paired with the matching qsFee carve-out in qsMay's follower prefer (the engine raises E-INCOMPARABLE if either widens without the other); and qsFee's exit row admits the qsX_qsUtter ligatures (extend-by-1 before ·Day·Utter via the lead), so ·X·Fee·Day·Utter joins again. The ·Pea·May-family windows deliberately keep the un-extended baseline join (the sitting's standing note) and ride the may-baseline-entry-extension-dropped class instead of matching the old font's en-ext-1. The alphabet is 13 symbols; the conform sweep is exact (402,233 sequences, 0 divergences, full rule and transition coverage), the boundary gate is exact over 225,078 sequences, Manual pins replay 32/32, and the budget gate keeps its yellow steady state (headroom ~34 KB). The oracle stands at 13,984 unmatched rows; the deliberate ss03 ·Fee·Tea divergence stands (the rune extends by 1 per the old derive's stated intent; it lands through verdicts, no ledger entry). The surface is 31,924 units (5,312 human) in 597 echo groups — echo groups key on the localized ink delta (unchanged flanking letters, including followers that merely slid over by an advance change, are outside the grain), and the artifact cycle lands the echo prefill itself after its carry+merge, so only genuinely new questions queue. The store holds 5,312 effective verdicts (5,085 approve / 216 either / 7 identical / 4 reject); the frontier carry is `verdicts-carried-6cd9dcf.json` at 5,310, the two newest verdicts riding the journal until the next cycle's carry, and the docket shows no fresh complaints (4 standing in 2 groups). Next: the qsAh batch — author `glyph_data/runes/qsAh.yaml` and run the standing per-migration checks that name it (qsMay's `reaches_up_and_way_over` scope, qsDay's depth-3 withdraw list, the qsFee own-prefer audit).

The batch-3-era design facts that remain live: the 17 `[echo-harmonize]` rejects (·X·Day·Tea·Day, ·X·Day·Tea·It, ·X·No·Tea·It) regroup like the old font, the 11 ·X·Day·Tea·No windows render their third form (`·X·Day | ·Tea.!half ~b~ ·No.alt`) with approved verdicts (·Day·Tea·No·dot rides along since `boundary` includes the namer dot), and word-initial ·No·Utter·May joins on both sides via ·Utter.alt (strict gain; the mechanism is qsUtter's before-·May follower prefer paired with the matching carve-out in qsNo's boundary prefer — the engine raises E-INCOMPARABLE if either widens without the other) while ·No·Utter·Day stays with the old font, restoring the plain "alt-·Utter only before ·Low or on strict gain" principle. qsDay's yielding prefer is one unscoped record over followers {·Day, ·Day·Utter, ·May, ·Low, ·It}; qsNo carries the scoped baseline twin ({·Tea·Oy, ·Day, ·It, ·Oy} joined at the baseline, before ·Tea·It); ·Day also yields before ·Tea·No at a run end; full ·Tea joins ·No.alt at the baseline through qsTea's `order: [full, half]`. The depth-4 fork stands as landed on three inputs — qsDay, and qsOy/qsTea_qsOy since the ·Oy sitting: X₀·Day·Tea·Utter·Tea·X₁ and ·Oy·Tea·Utter·Tea·X₁ with X₁ ∈ {·Day, ·Day·Utter, ·May, ·No, ·Low, ·It, ·Utter} render the predecessor yielded and ·Tea~·Utter joined in the default config (·Pea/·Tea/boundary followers keep the rescue; a following ·Oy does not, because ·Tea·Oy ligates and the chain's third hop stops matching; ss03 keeps its y5 escape and joins ·Utter on both sides); the chain grammar is depth-4 (nested `then:` chains, capped at three hops past `right`) with the cap and the table's fourth slot derived from `model.RIGHT_WINDOW_SLOTS` and an import-time arity assert. ·Oy takes the shape without qsDay's `self: {entry: live}` scope, so word-initial ·Oy·Tea·Utter·Tea moves off the old font as well — the entry-liveness split was built at the sitting and passed over, and the divergence stands adjudicated with “either” verdicts on both arms (`u-11066`/`u-11067`). ·Day·Tea·No·Utter keeps the ·Day·Tea join per the recorded follower-of-·No scope; its approve-with-note is still worth setting in the app (currently carried as either). The standing rejects are ·No·Utter·Tea·Day, ·No·No·Utter·Low, and ·No·No·Tea·Day (group `g-442f1362`) plus ·Tea·Day·Utter·Fee (`g-1a4c5489`). The M1 GSUB rides type-7 Extension offsets — `m1_settle` is `useExtension` and the budget gate's yellow flag is the steady state. `make verdict-ready` — and the same status as a banner in the app — answers whether the surface, gates, and verdict store are ready.

The rebuild is migrating the cursive-join engine rune by rune, validating each batch against the old shipped font via the oracle.

## Deferred and tracked follow-ups

### Doable anytime

Nothing gates these — act on any cycle, whenever it’s worth it.

### Do when a gate takes too long

Not gated on a named event — these come due when a cycle step gets slow enough that acting pays off.

- **Conform-gate cost** — `gate:conform`, the exhaustive font-vs-settle sweep, runs at horizon 5 on every cycle whose conform inputs moved (`--conform-horizon` on the cycle driver, passed through to `run_m1 --conform-only`; `--skip-conform` opts out; on unchanged inputs a green record auto-skips it, like every heavy cycle stage — `--fresh` forces a full rerun). The sweep term grows with the fifth power of the alphabet (the whole gate measured ~367 s at 10 letters, 8-way — up from ~135 s at 9, more than the ~1.7× the alphabet alone predicts because the ·Tea·Day fix widened `depth3_inputs` from 3 runes to 7 and each deep input enumerates a third slot; project past ~15 min by ~17 letters, hours beyond) and shrinks with `--conform-horizon` — witness top-ups keep rule and transition coverage exact at any horizon; only off-corpus diff density shrinks. The past-horizon top-ups (~559K streams per config) are dominated by committed-left prefixes deeper than the sweep; the depth-4 fourth slot stays the cheap part (its three inputs — qsDay, qsOy, qsTea_qsOy — carry 1,995 live-`right4` transitions per config between them, 2,025 under ss03, and 14 emitted rules with a `lookahead4`), because `table.fourth_slot_filter` prunes don’t-care right4 fan-out at the source — a fourth token can only decide a window whose own-rune depth-4 chain is still unknown over the three nearer slots, so pruning the rest loses nothing. Each stream costs one shape plus one settle on a per-config warm engine (`settle_with_engine`); when the gate becomes the cycle’s long pole again, the lever is `--conform-horizon`, decided deliberately.

### Waiting on a specific migration or milestone

Each is blocked until a named event lands — a particular rune’s migration, or the batch-2 close — and can’t meaningfully start before then.

- **Unresolved design flags** — the inexpressible `reaches_up_and_way_over` resolved-extension scope on qsMay’s x-height entry (needs either a design extension or proof the partners’ own extend records cover it; re-check at the qsAh migration, qsUtter’s half already landed), and the ss05 row-grain refusal coverage notes (re-check at the qsEt migration; the ss04 half went live and conform-verified with qsLow).
- **qsZoo Manual-pin flag** — when qsZoo migrates, the Manual pin `·Tea ~b~ ·May.en-ext-1 ~x~ ·Zoo` will trip the no-waiver Manual-pin gate; re-transcribe the pin or fix the runes at that point.
- **The ·Low·Oy·Tea·Oy standing approval is blocked on M1 shipping, not on picking a mechanism** — the durable home is the corpus pin the review surface already drafts for the window (`data-expect-noncanonically="·Low ~x~ ·Oy | ·Tea+Oy"`, syntax- and semantics-validated against the M1 font, no duplicate), because a pin is the one channel that fails loudly rather than silently re-queueing — the legacy shaping suite and the no-waiver Manual-pin gate both replay it. It cannot land yet: `test/test_shaping.py` replays every corpus pin against the _shipped_ Senior font, and this is exactly a window where the two engines disagree — the old cleanup over-reach drops the ·Low~·Oy join when ·Tea·Oy follows (`break, break`) while M1 keeps it (`y5, break`) — so the pin fails `make test` until M1 is the shipped engine. A census pin is not a substitute and never was: `rebuild/review-census-pins.json` is aggregate integers with no key space for a window, and its `--check` is informational anyway. The interim exposure is one window: carry is keyed on the render (`content_key`, then the ink signature), so any change to it strands the verdict, and echo group `e-0033` is a singleton so the prefill cannot cover it. The verdict's only tracked copy is `rebuild/evidence/verdicts-carried-fb4f7d6.json` under the superseded id `u-9148` — keep that evidence file until the pin lands, notwithstanding the usual retire-the-evidence rule. When it does land, the home decides which gates enforce it, so choose deliberately: only the three corpus HTML files are read by `collect_pin_runs`, so a pin lands in the M1 Manual-pin gate only from there — but the drafter's hardcoded `site/the-manual.html` is a transcription of Read's manual and this window is in neither it nor the extra-words list, and the corpus hosts no `data-expect-noncanonically` pins at all today, the earlier twenty having become `_assert_expect_any` tests in `test/test_calt_regressions.py`, which the M1 gate never sees.
- **Dormant `contract:` records** — the qsJai-keyed `contract:` records on qsNo/qsUtter/qsLow and the qsZoo/qsJay-keyed ones on qsTea never fired in the migrated corpus; re-adjudicate them when qsJai/qsZoo/qsJay migrate and can actually exercise them. (qsMay's after-·Fee contract was retired at the ·Fee migration — the entry row's `pulled-back-stubless` binding already covers every enterer.)
- **qsFee's forward-preference left scope** — the prefer covers the three newly-widened backward sources (·May/·No/·Low). When qsI/qsAh/qsOut/qsOwe migrate, their backward joins into ·Fee match the old font and need no widening — but re-audit any own-prefer on a newly-migrated left family that fires with a qsFee follower and demands the opposite seam (only qsMay needed the paired carve-out so far).
- **The ·Tea·Day tight bond is per-rune and must be re-checked at every migration** — the standing taste is that a ·Tea·Day pair keeps its forward join, so nothing upstream may spend ·Tea’s one baseline slot backward (qsTea.full’s `pairings.never` makes that slot exclusive). Every ·Tea→·Day seam in the migrated alphabet now joins; holding that as letters arrive takes records in two shapes, and a new rune may need either. **The ordinary shape** is the yielding prefer already idiomatic on qsDay and qsNo — `{cell: {exit: none}, over: {exit: baseline}, when: {right: {family: qsTea, then: {family: qsDay}}}}`, default `yields-to-joins`, no `why` demanded — which any newly-migrated family that can exit into ·Tea will want. **The ·Utter shape** is the `mode: absolute` twin on qsFee/qsNo/qsMay/qsUtter naming `{right: {family: qsUtter, then: {family: qsTea, then: {family: qsDay}}}}`: ·Utter’s `alternate` stance is the one place whose exit is mandatory (`require: [exit]`), so an x-height join into ·Utter forces the stranding from two positions away, and only the letter _before_ ·Utter can prevent it. That copy is needed by every family qsUtter’s x-height entry `from:` list admits — re-check that list at each migration — and `absolute` is load-bearing there, because the joining candidate outscores the yielding one on join-count and a default-mode record is a total no-op.
- **Open fork: should run-final ·Utter join backward at the x-height?** — deferred out of the ·Tea·Day fix, and worth its own sitting. Today `alternate` carries `require: [exit]`, so it is unavailable at a run end and word-final ·Utter sits on `mono` taking no backward join. Giving qsUtter a non-exiting twin stance (`alternate-stopped`: same x-height entry and `from:` list, `exits: {}`, `require: [entry]`, the withdrawn bitmap, ordered last, elevated by one own-rune prefer before ·Tea·Day) moves 647 of 11,100 swept windows for **+354 joined seams** — 322 of them run-final ·Utter gaining a backward join, 274 medial windows where ·Utter’s join hops from its right side to its left. It also does the ·Tea·Day job strictly better than what shipped: same 321/321, but it _keeps_ the upstream x-height join the `absolute` records throw away, and it retires those four records and their growth problem. The costs are 15 windows losing one join (all the same shape — a medial ·Fee stranded between two joinable neighbors) and `qsDay_qsUtter` decomposing in 15 windows, though every one still joins the seam. Only ~10 of the 647 are about ·Tea·Day at all, which is why it was parked rather than ridden in.
- **Three ·Utter-local expressions of the ·Tea·Day fix, all measured and rejected** — so nobody re-derives them. Dropping `require: [exit]` outright: 641 windows, and it decomposes the ·Day·Utter ligature. Relaxing to `require: [entry]` plus an own-rune prefer: 1,035 windows, and it loses the ·Utter·Low joins. Gating the `alternate-stopped` twin with `{stance: alternate-stopped, when: {right: {except: [{family: qsTea}]}}}` so ordinary ranking can’t reach it: 120 windows and **−65 joins**, because merely offering the stance as a candidate changes lookahead-closure upstream before the refuse fires — ·Fee flips from `reversed-loop` to `loop` across the `·X·Fee·Utter` windows and drops its backward join. The wall underneath all three: `prefer` can see two letters ahead but only breaks ties, `refuse` can suppress but is confined to the immediate right neighbor (design §3.3), so nothing ·Utter-local can say “be available here and nowhere else.” Unlocking qsTea’s `(baseline, baseline)` pairing is a fourth shape, but `unlock` demands a `feature: ssNN` and so can never be the default-config answer.
- **The ·Day·Tea-before-·No follower set** — qsDay’s depth-3 withdraw list ({·Tea, ·May, ·Low}) mirrors where ·No.flipped engages today; when more baseline-entry followers migrate (qsVie, qsFee, qsRoe…), ·No.flipped will engage before them too and the list needs re-adjudication.
- **Batch-1 spec-pin re-baseline** — the rebuild suite fails exactly 4 tests on a clean HEAD by design: `test_surface.py::test_real_cell_bindings_all_match` and `test_spec_load.py`’s `test_loads_all_six_runes` / `test_predicate_class_membership` / `test_group_resolution` pin the M1-batch-1 spec shape (six runes, batch-1 predicate classes), and the artifact-cycle driver subtracts them as its expected `baseline` bucket. Re-baseline them when the batch-2 migration formally closes, and retire the driver’s baseline bucket at the same time. Distinguish real regressions in any rebuild-suite run by diffing the failure set against these four (plus the artifact-pinned census/Manual-pin tests, which go stale after any rune edit until `run_m1` + `review.build` re-run).

## Keeping this file honest

This is a punch list of what’s _left_ — not a changelog of what happened. When work lands, edit the current-state description in place and delete the status it supersedes; never append a dated `Update (…)` paragraph. What got done — with what counts, and under which commits — is already in the git log, so this file records only the live frontier: the open forks, the next batch, and the consciously-parked follow-ups. When a landed change leaves a genuinely-open residue, record the residue as a forward bullet, not the change that produced it. The broader workflow rules — no after-the-fact reports, bounded progress files, evidence that outlives its decision — live in `AGENTS.md`’s “Note-taking and the rebuild logs” section; `AGENTS.md` points back here.
