Add an entry for a letter in @glyph_data/quikscript.yaml. Do not try to determine what the letter should look like. The only thing that should be in the `bitmap` array is a list of strings with only five spaces in it. The number of elements in the `bitmap` array is determined by whether the letter is Tall, Short, or Deep. Tall and Deep letters have 9 items in `bitmap`, while Short letters have 6. Short letters have a y_offset of -3. Both Tall and Deep letters need an empty comment next to either the baseline (for Deep letters) or the x-height (for Tall letters). After adding the letter to the glyph data, add an entry to `Every character` in @test/test.html. The letter to add is:
